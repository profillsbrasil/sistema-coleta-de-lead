---
phase: 04-lead-management
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - apps/web/src/app/leads/page.tsx
  - apps/web/src/app/leads/lead-list.tsx
  - apps/web/src/app/leads/[id]/page.tsx
  - apps/web/src/app/leads/[id]/lead-detail.tsx
  - apps/web/src/components/header.tsx
autonomous: false
requirements:
  - LEAD-01
  - LEAD-02
  - LEAD-03
  - LEAD-04
  - LEAD-05

must_haves:
  truths:
    - "Vendedor ve lista dos seus leads ordenada por recencia em /leads"
    - "Vendedor filtra leads por tag de interesse via toggle bar"
    - "Vendedor toca em card e navega para /leads/[id] com formulario de edicao"
    - "Vendedor edita campos e salva alteracoes via Dexie + syncQueue"
    - "Vendedor exclui lead via AlertDialog e lead some da lista"
    - "Infinite scroll carrega 20 leads por vez conforme vendedor scrolla"
    - "Tudo funciona offline via Dexie"
  artifacts:
    - path: "apps/web/src/app/leads/page.tsx"
      provides: "Lead list page with server-side auth guard"
      exports: ["default"]
    - path: "apps/web/src/app/leads/lead-list.tsx"
      provides: "Client component with useLiveQuery + infinite scroll + tag filter"
      exports: ["default"]
    - path: "apps/web/src/app/leads/[id]/page.tsx"
      provides: "Lead detail/edit page with server-side auth guard"
      exports: ["default"]
    - path: "apps/web/src/app/leads/[id]/lead-detail.tsx"
      provides: "Client component with LeadForm edit mode + AlertDialog delete"
      exports: ["default"]
  key_links:
    - from: "apps/web/src/app/leads/lead-list.tsx"
      to: "apps/web/src/lib/lead/queries.ts"
      via: "useLiveQuery calling queryLeads"
      pattern: "queryLeads|useLiveQuery"
    - from: "apps/web/src/app/leads/lead-list.tsx"
      to: "apps/web/src/components/lead-card.tsx"
      via: "renders LeadCard for each lead"
      pattern: "LeadCard"
    - from: "apps/web/src/app/leads/[id]/lead-detail.tsx"
      to: "apps/web/src/components/lead-form.tsx"
      via: "renders LeadForm with lead prop"
      pattern: "LeadForm.*lead="
    - from: "apps/web/src/app/leads/[id]/lead-detail.tsx"
      to: "apps/web/src/lib/lead/delete-lead.ts"
      via: "calls deleteLead on confirm"
      pattern: "deleteLead"
---

<objective>
Wire the lead list page (/leads) with infinite scroll and the detail/edit page (/leads/[id]) with edit form and delete confirmation.

Purpose: This is the integration plan that connects the data layer (Plan 01) and UI components (Plan 02) into working pages. After this plan, the full lead management flow is functional.
Output: 2 route pages + 2 client components + header nav update
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-lead-management/04-CONTEXT.md
@.planning/phases/04-lead-management/04-RESEARCH.md
@.planning/phases/04-lead-management/04-UI-SPEC.md
@.planning/phases/04-lead-management/04-01-SUMMARY.md
@.planning/phases/04-lead-management/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From apps/web/src/lib/lead/queries.ts:
```typescript
export type FilterTag = "todos" | "quente" | "morno" | "frio";
export async function queryLeads(userId: string, tag: FilterTag, limit: number): Promise<Lead[]>;
```

From apps/web/src/lib/lead/update-lead.ts:
```typescript
export async function updateLead(localId: string, data: Partial<LeadFormData>, photo?: Blob | null): Promise<void>;
```

From apps/web/src/lib/lead/delete-lead.ts:
```typescript
export async function deleteLead(localId: string): Promise<void>;
```

From apps/web/src/lib/lead/relative-time.ts:
```typescript
export function relativeTime(isoDate: string): string;
```

<!-- From Plan 02 outputs -->
From apps/web/src/components/lead-card.tsx:
```typescript
interface LeadCardProps { lead: Lead; onClick: () => void; }
export default function LeadCard({ lead, onClick }: LeadCardProps);
```

From apps/web/src/components/tag-filter.tsx:
```typescript
export default function TagFilter({ value, onChange }: { value: FilterTag; onChange: (tag: FilterTag) => void });
```

From apps/web/src/components/lead-form.tsx:
```typescript
interface LeadFormProps { lead?: Lead; onDelete?: () => void; }
export default function LeadForm({ lead, onDelete }: LeadFormProps);
```

<!-- Existing code needed -->
From apps/web/src/lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient;
```

From apps/web/src/components/fab.tsx:
```typescript
export default function FAB(); // Links to /leads/new
```

From packages/ui/src/components/alert-dialog.tsx:
```typescript
export { AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogAction, AlertDialogCancel };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lead list page with infinite scroll and tag filter</name>
  <files>
    apps/web/src/app/leads/page.tsx,
    apps/web/src/app/leads/lead-list.tsx,
    apps/web/src/components/header.tsx
  </files>
  <read_first>
    apps/web/src/app/leads/new/page.tsx,
    apps/web/src/app/dashboard/page.tsx,
    apps/web/src/components/header.tsx,
    apps/web/src/components/fab.tsx,
    apps/web/src/lib/db/index.ts,
    .planning/phases/04-lead-management/04-UI-SPEC.md
  </read_first>
  <action>
    1. Create apps/web/src/app/leads/page.tsx (server component, auth guard):
       - Same pattern as dashboard/page.tsx: server-side getUser() check, redirect to "/" if not authenticated
       - Import and render LeadList client component
       - Pass userId to LeadList as prop

    2. Create apps/web/src/app/leads/lead-list.tsx ("use client"):
       - Props: { userId: string }
       - State: activeTag (FilterTag, default "todos"), limit (number, default 20)
       - useLiveQuery from dexie-react-hooks: call queryLeads(userId, activeTag, limit) with deps [userId, activeTag, limit]
       - Handle undefined return (Research pitfall 2): `const leads = useLiveQuery(...) ?? undefined` -- show skeleton when undefined
       - Compute hasMore: leads !== undefined && leads.length === limit (heuristic: if we got exactly limit items, there may be more)
       - IntersectionObserver sentinel per D-02:
         - useRef<HTMLDivElement> for sentinel element
         - useEffect with IntersectionObserver, rootMargin "200px"
         - When entry.isIntersecting && hasMore: setLimit(prev => prev + 20)
         - Cleanup: observer.disconnect()
         - Wrap loadMore in useCallback to avoid effect re-runs
       - Reset limit to 20 when activeTag changes (useEffect on activeTag)
       - Layout per UI-SPEC:
         - Container: max-w-[480px] w-full mx-auto px-4 py-8
         - Header row: flex items-center justify-between
         - Title "Meus Leads" (text-xl font-semibold) + lead count "{n} leads" (text-sm text-muted-foreground, aria-live="polite")
         - TagFilter below title: mt-4
         - List below filter: mt-6, flex flex-col gap-4
         - Each lead: <LeadCard lead={lead} onClick={() => router.push(`/leads/${lead.localId}` as unknown as "/")} />
         - typedRoutes cast per Phase 3 pattern and Research pitfall 5
         - Sentinel div at end: ref={sentinelRef}, className="h-px" (invisible)
         - Loading spinner between last card and sentinel when hasMore: Loader2 animate-spin centered
       - FAB component at the end (outside the list container, inside the page)
       - States per UI-SPEC:
         - Loading (leads === undefined): 3 Skeleton cards, aria-busy="true" on container
         - Empty (leads.length === 0 && activeTag === "todos"): empty state with icon, "Nenhum lead ainda", "Comece coletando seu primeiro lead no evento.", CTA Link to /leads/new
         - Empty filtered (leads.length === 0 && activeTag !== "todos"): "Nenhum lead {tag} encontrado"
         - Normal: cards rendered
       - Import: useLiveQuery from "dexie-react-hooks", queryLeads from @/lib/lead/queries, LeadCard, TagFilter, FAB, Skeleton, Loader2

    3. Update apps/web/src/components/header.tsx:
       - Add { to: "/leads", label: "Leads" } to the links array (per UI-SPEC navigation)
       - Use typedRoutes cast: `"/leads" as unknown as "/"`
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useLiveQuery" apps/web/src/app/leads/lead-list.tsx
    - grep -q "queryLeads" apps/web/src/app/leads/lead-list.tsx
    - grep -q "IntersectionObserver" apps/web/src/app/leads/lead-list.tsx
    - grep -q "LeadCard" apps/web/src/app/leads/lead-list.tsx
    - grep -q "TagFilter" apps/web/src/app/leads/lead-list.tsx
    - grep -q "FAB" apps/web/src/app/leads/lead-list.tsx
    - grep -q "Meus Leads" apps/web/src/app/leads/lead-list.tsx
    - grep -q "Nenhum lead ainda" apps/web/src/app/leads/lead-list.tsx
    - grep -q "Skeleton" apps/web/src/app/leads/lead-list.tsx
    - grep -q "Leads" apps/web/src/components/header.tsx
    - grep -q "sentinel" apps/web/src/app/leads/lead-list.tsx
  </acceptance_criteria>
  <done>Lead list page renders at /leads with auth guard, shows leads as cards via useLiveQuery, supports infinite scroll via IntersectionObserver sentinel, filters by tag via TagFilter, shows empty/loading states, includes FAB. Header nav includes Leads link.</done>
</task>

<task type="auto">
  <name>Task 2: Lead detail/edit page with delete confirmation</name>
  <files>
    apps/web/src/app/leads/[id]/page.tsx,
    apps/web/src/app/leads/[id]/lead-detail.tsx
  </files>
  <read_first>
    apps/web/src/app/leads/new/page.tsx,
    apps/web/src/app/dashboard/page.tsx,
    apps/web/src/components/lead-form.tsx,
    .planning/phases/04-lead-management/04-UI-SPEC.md
  </read_first>
  <action>
    1. Create apps/web/src/app/leads/[id]/page.tsx (server component):
       - Next.js 16 params: `{ params }: { params: Promise<{ id: string }> }` (await params)
       - Server-side getUser() check, redirect to "/" if not authenticated
       - Render LeadDetail client component with localId and userId props

    2. Create apps/web/src/app/leads/[id]/lead-detail.tsx ("use client"):
       - Props: { localId: string; userId: string }
       - Use useLiveQuery to fetch single lead: `db.leads.get(localId)` with dep [localId]
       - Handle states per UI-SPEC:
         - Loading (lead === undefined from useLiveQuery initial): Skeleton form placeholder
         - Not found (lead === null after query resolves, or lead.userId !== userId): toast.error("Lead nao encontrado.") + redirect to /leads
         - Found: render LeadForm with lead prop
       - Delete flow per D-06, D-07:
         - State: showDeleteDialog (boolean)
         - Render AlertDialog from packages/ui per UI-SPEC:
           - Title: "Excluir lead?"
           - Description: "O lead sera removido da sua lista. Esta acao sera sincronizada quando voce estiver online."
           - Cancel: "Cancelar"
           - Action: "Excluir" -- onClick calls handleDelete
         - handleDelete async function:
           - await deleteLead(localId) from @/lib/lead/delete-lead
           - toast.success("Lead excluido!")
           - router.push("/leads" as unknown as "/")
         - Pass onDelete={() => setShowDeleteDialog(true)} to LeadForm
       - The AlertDialog wraps around the page content (not inside LeadForm)
       - Import: useLiveQuery from "dexie-react-hooks", db from @/lib/db, deleteLead from @/lib/lead/delete-lead, LeadForm, AlertDialog components, toast from sonner, useRouter from next/navigation
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useLiveQuery" apps/web/src/app/leads/[id]/lead-detail.tsx
    - grep -q "LeadForm" apps/web/src/app/leads/[id]/lead-detail.tsx
    - grep -q "lead=" apps/web/src/app/leads/[id]/lead-detail.tsx
    - grep -q "deleteLead" apps/web/src/app/leads/[id]/lead-detail.tsx
    - grep -q "AlertDialog" apps/web/src/app/leads/[id]/lead-detail.tsx
    - grep -q "Excluir lead" apps/web/src/app/leads/[id]/lead-detail.tsx
    - grep -q "Lead excluido" apps/web/src/app/leads/[id]/lead-detail.tsx
    - grep -q "Lead nao encontrado" apps/web/src/app/leads/[id]/lead-detail.tsx
    - grep -q "onDelete" apps/web/src/app/leads/[id]/lead-detail.tsx
    - test -f apps/web/src/app/leads/[id]/page.tsx
  </acceptance_criteria>
  <done>Detail page at /leads/[id] loads lead from Dexie via useLiveQuery, renders LeadForm in edit mode with lead prop, handles delete via AlertDialog with confirmation copy, soft-deletes via deleteLead, shows appropriate error/loading states. All operations work offline via Dexie.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete lead management flow</name>
  <files>none</files>
  <action>
    Human verifies the complete lead management flow end-to-end: list, filter, edit, delete -- all offline-first.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types && bun run test</automated>
  </verify>
  <what-built>Complete lead management flow: list with infinite scroll and tag filter at /leads, detail/edit at /leads/[id] with delete confirmation. All offline-first via Dexie.</what-built>
  <how-to-verify>
    1. Start dev server: `bun run dev:web`
    2. Navigate to http://localhost:3001/leads
    3. Verify "Meus Leads" title appears with tag filter toggles (Todos, Quente, Morno, Frio)
    4. If no leads: verify empty state with "Nenhum lead ainda" message and "Novo Lead" CTA
    5. Create a lead via FAB -> /leads/new (existing Phase 3 form)
    6. Go back to /leads -- verify new lead appears as card with name, contact, tag badge, relative time
    7. Click tag filter -- verify filtering works (e.g., click "Quente" shows only hot leads)
    8. Click on a lead card -- verify navigation to /leads/[id] with edit form pre-filled
    9. Edit the lead name, tap "Salvar alteracoes" -- verify toast "Lead atualizado!" and return to list
    10. Go back to detail, tap "Excluir Lead" -- verify AlertDialog appears
    11. Confirm deletion -- verify toast "Lead excluido!" and lead disappears from list
    12. Test infinite scroll: if many leads, scroll down and verify more cards load
    13. Open DevTools > Network > Offline -- verify all above still works
  </how-to-verify>
  <done>Human confirms full lead management flow works: list, filter, edit, delete, all offline-first.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types && bun run check && bun run test
</verification>

<success_criteria>
- /leads page shows lead list with infinite scroll, tag filter, empty states, FAB
- /leads/[id] page shows edit form pre-filled, save updates via Dexie, delete with AlertDialog confirmation
- Header nav includes "Leads" link
- All operations work offline (Dexie-first, syncQueue enqueues changes)
- Type check, Biome check, and tests all pass
- Human verification confirms full flow works
</success_criteria>

<output>
After completion, create `.planning/phases/04-lead-management/04-03-SUMMARY.md`
</output>
