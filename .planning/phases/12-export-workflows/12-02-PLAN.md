---
phase: 12-export-workflows
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - apps/web/src/lib/lead/export-csv.ts
  - apps/web/src/lib/lead/export-csv.test.ts
  - apps/web/src/app/(app)/leads/lead-list.tsx
  - apps/web/src/app/(app)/admin/leads/leads-panel.tsx
  - .planning/phases/12-export-workflows/12-VALIDATION.md
autonomous: false
requirements:
  - ENH-01
  - ENH-07
must_haves:
  truths:
    - "Downloaded CSV files open as UTF-8 with readable pt-BR headers and values, including accents such as `Data de Criação`, `Ações`, and `Negociação`, in Excel and Google Sheets."
    - "User-controlled cells are neutralized before spreadsheet open so formulas do not execute."
    - "Seller and admin exports confirm the exported scope and row count after the download starts per D-06."
  artifacts:
    - path: "apps/web/src/lib/lead/export-csv.ts"
      provides: "Centralized CSV formatting, BOM, spreadsheet sanitization, and deterministic filename helpers."
      exports:
        - "generateCsvContent"
        - "exportLeadsCsv"
    - path: "apps/web/src/lib/lead/export-csv.test.ts"
      provides: "Coverage for BOM, accented text, multiline notes, formula injection mitigation, and filename generation."
    - path: "apps/web/src/app/(app)/leads/lead-list.tsx"
      provides: "Seller success feedback naming the exported scope and row count."
    - path: "apps/web/src/app/(app)/admin/leads/leads-panel.tsx"
      provides: "Admin success feedback naming the exported vendor scope and row count."
  key_links:
    - from: "apps/web/src/app/(app)/leads/lead-list.tsx"
      to: "apps/web/src/lib/lead/export-csv.ts"
      via: "handleExport success path"
      pattern: "toast\\.success|aria-live"
    - from: "apps/web/src/app/(app)/admin/leads/leads-panel.tsx"
      to: "apps/web/src/lib/lead/export-csv.ts"
      via: "handleExport success path"
      pattern: "toast\\.success|aria-live"
    - from: "apps/web/src/lib/lead/export-csv.ts"
      to: "apps/web/src/lib/masks/phone.ts"
      via: "formatPhone before serialization"
      pattern: "formatPhone"
---

<objective>
Harden the shared CSV contract for real spreadsheet handoff and add explicit export feedback, then pause for manual spreadsheet verification in Excel and Google Sheets.

Purpose: Complete the user-facing Phase 12 contract after dataset correctness is in place.
Output: Safe CSV formatter, deterministic filenames, explicit row-count feedback in both screens, and a blocking manual spreadsheet verification checkpoint.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-export-workflows/12-CONTEXT.md
@.planning/phases/12-export-workflows/12-RESEARCH.md
@.planning/phases/12-export-workflows/12-VALIDATION.md
@apps/web/src/lib/lead/export-csv.ts
@apps/web/src/lib/lead/export-csv.test.ts
@apps/web/src/app/(app)/leads/lead-list.tsx
@apps/web/src/app/(app)/admin/leads/leads-panel.tsx
@apps/web/src/lib/masks/phone.ts
@.planning/phases/12-export-workflows/12-01-PLAN.md

<interfaces>
Expected output from Plan 12-01:
```typescript
export async function queryLeadExportScope(input: {
	userId: string;
	tag: FilterTag;
	searchTerm: string;
}): Promise<{ leads: Lead[]; total: number }>;

const adminLeadFilters = {
	userId: selectedVendor,
};

trpc.admin.leads.exportByFilters.queryOptions(adminLeadFilters);
```

Current shared CSV API:
```typescript
export function generateCsvContent(leads: ExportableLead[]): string;
export function exportLeadsCsv(leads: ExportableLead[]): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize spreadsheet-safe CSV formatting, deterministic filenames, and compatibility coverage</name>
  <files>apps/web/src/lib/lead/export-csv.ts, apps/web/src/lib/lead/export-csv.test.ts, .planning/phases/12-export-workflows/12-VALIDATION.md</files>
  <read_first>apps/web/src/lib/lead/export-csv.ts, apps/web/src/lib/lead/export-csv.test.ts, apps/web/src/lib/masks/phone.ts, .planning/phases/12-export-workflows/12-RESEARCH.md, .planning/phases/12-export-workflows/12-VALIDATION.md, .planning/phases/12-export-workflows/12-CONTEXT.md</read_first>
  <behavior>
    - Test 1: CSV output still starts with `\uFEFF` and still uses the exact header `Nome,Telefone,Email,Empresa,Cargo,Segmento,Interesse,Follow-up,Notas,Data de Criação`.
    - Test 2: accented text such as `Ações` and `Negociação`, plus the accented header `Data de Criação`, survive serialization without dropping UTF-8 characters or line breaks.
    - Test 3: cells beginning with `=`, `+`, `-`, `@`, tab, carriage return, or newline are prefixed so spreadsheet software renders them as text instead of formulas.
    - Test 4: filename generation is deterministic and scope-aware for seller and admin exports, e.g. `leads-vendedor-quente-2026-03-29.csv` or `leads-admin-<vendorSlug>-2026-03-29.csv`.
  </behavior>
  <action>Refactor `apps/web/src/lib/lead/export-csv.ts` per D-03 and D-04 to keep one CSV-only export contract and add explicit helpers instead of ad hoc string building. Introduce a top-level `DANGEROUS_SPREADSHEET_PREFIX` regex matching `=`, `+`, `-`, `@`, tab, carriage return, and newline. Add a `sanitizeSpreadsheetCell(value: string): string` helper that normalizes `\r\n` to `\n` and prefixes dangerous leading characters with a single apostrophe `'` before CSV escaping. Keep `formatPhone()` and `formatDate()` behavior intact. Replace the inline filename string with an exported deterministic helper that receives `{ scope: "seller" | "admin"; scopeLabel: string; date: Date; }` and returns `leads-${scope}-${scopeLabel}-${yyyy}-${mm}-${dd}.csv` after slugging `scopeLabel` to lowercase ASCII with hyphens. Expand `apps/web/src/lib/lead/export-csv.test.ts` with fixtures that use actual accented strings such as `Ações`, `Negociação`, and a header assertion for `Data de Criação`, alongside multiline notes, dangerous leading characters, and filename helper coverage. Update `.planning/phases/12-export-workflows/12-VALIDATION.md` by marking the new CSV hardening references as covered and setting `nyquist_compliant: true` once the task list has automated commands for every task.</action>
  <acceptance_criteria>
    - `apps/web/src/lib/lead/export-csv.ts` contains `DANGEROUS_SPREADSHEET_PREFIX`, `sanitizeSpreadsheetCell`, and `buildExportFilename`.
    - `apps/web/src/lib/lead/export-csv.ts` still contains the exact CSV header string `Nome,Telefone,Email,Empresa,Cargo,Segmento,Interesse,Follow-up,Notas,Data de Criação`.
    - `apps/web/src/lib/lead/export-csv.test.ts` contains assertions covering `Ações`, `Negociação`, `Data de Criação`, multiline notes, dangerous prefixes, and `buildExportFilename`.
    - `.planning/phases/12-export-workflows/12-VALIDATION.md` contains `nyquist_compliant: true`.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/web test src/lib/lead/export-csv.test.ts</automated>
  </verify>
  <done>The shared CSV utility is the single spreadsheet-safe formatter for both roles, filenames are deterministic, and the hardening contract is locked by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit seller/admin export feedback with scope and row count</name>
  <files>apps/web/src/app/(app)/leads/lead-list.tsx, apps/web/src/app/(app)/admin/leads/leads-panel.tsx</files>
  <read_first>apps/web/src/app/(app)/leads/lead-list.tsx, apps/web/src/app/(app)/admin/leads/leads-panel.tsx, apps/web/src/lib/lead/export-csv.ts, apps/web/src/lib/lead/export-scope.ts, .planning/phases/12-export-workflows/12-CONTEXT.md, .planning/phases/12-export-workflows/12-RESEARCH.md, .planning/phases/12-export-workflows/12-VALIDATION.md</read_first>
  <behavior>
    - Test 1: seller export announces the exported scope with row count, for example `Exportados 37 leads do filtro Quente`.
    - Test 2: admin export announces the selected vendor scope with row count, for example `Exportados 87 leads de Maria Silva`.
    - Test 3: both screens keep export in their existing header action surface per D-05 and do not introduce a new page, modal, or background job UI.
  </behavior>
  <action>Update `apps/web/src/app/(app)/leads/lead-list.tsx` so `handleExport()` uses the Plan 12-01 full-scope result to call the hardened `exportLeadsCsv()` with a deterministic filename built from the seller scope label (`todos`, `quente`, `morno`, `frio`, or `busca`). After `exportLeadsCsv()` returns, show lightweight success feedback per D-06 using the existing Sonner stack or the page's `aria-live` text with the exact row count and scope label, e.g. `Exportados 12 leads do filtro Quente`. Update `apps/web/src/app/(app)/admin/leads/leads-panel.tsx` so its export handler passes the selected vendor name as the filename scope label and shows success feedback `Exportados ${total} leads de ${selectedVendorName}` after download starts. Keep both export actions in the current header row and do not add a separate export center, modal, or `.xlsx` branch.</action>
  <acceptance_criteria>
    - `apps/web/src/app/(app)/leads/lead-list.tsx` contains a success message string with both `Exportados` and `filtro`.
    - `apps/web/src/app/(app)/admin/leads/leads-panel.tsx` contains a success message string with both `Exportados` and `selectedVendorName`.
    - Neither file contains `.xlsx` or a new route path for export.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/web test src/lib/lead/export-csv.test.ts src/lib/lead/export-scope.test.ts && bun run --cwd packages/api test src/__tests__/admin-leads.test.ts</automated>
  </verify>
  <done>Both screens make scope explicit, start the CSV download with the hardened shared contract, and immediately tell the user what was exported and how many rows were included.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the exported CSV in real spreadsheet apps before closing Phase 12</name>
  <files>.planning/phases/12-export-workflows/12-VALIDATION.md, apps/web/src/lib/lead/export-csv.ts, apps/web/src/app/(app)/leads/lead-list.tsx, apps/web/src/app/(app)/admin/leads/leads-panel.tsx</files>
  <read_first>.planning/phases/12-export-workflows/12-VALIDATION.md, apps/web/src/lib/lead/export-csv.test.ts, apps/web/src/app/(app)/leads/lead-list.tsx, apps/web/src/app/(app)/admin/leads/leads-panel.tsx</read_first>
  <action>Use the Phase 12 implementation to export one seller CSV containing accented text and multiline notes, and one admin CSV containing enough leads to span more than one page. Then open the generated files in Excel and Google Sheets to confirm the UTF-8, formula-safety, and full-scope behavior from D-03, D-04, and D-06.</action>
  <acceptance_criteria>
    - Excel and Google Sheets show accented strings such as `Ações` and `Negociação`, plus the header `Data de Criação`, without mojibake.
    - Cells that originally started with `=`, `+`, `-`, `@`, tab, carriage return, or newline render as text and do not execute as formulas.
    - The seller export row count matches the exported-scope feedback, and the admin export row count exceeds the visible page length when total rows are greater than 20.
  </acceptance_criteria>
  <verify>
    <automated>bun run check && bun run check-types && bun run test</automated>
  </verify>
  <what-built>Spreadsheet-safe CSV exports with deterministic filenames and explicit row-count feedback in seller and admin flows.</what-built>
  <how-to-verify>
    1. In `/leads`, create or select leads whose text includes `Ações`, `Negociação`, multiline notes, and one malicious value such as `=HYPERLINK("http://example.com")`.
    2. Export from the seller screen using a non-`todos` filter and confirm the success feedback reports the exact exported row count.
    3. Open the downloaded seller CSV in Excel and Google Sheets and confirm the header `Data de Criação` plus the values `Ações` and `Negociação` render correctly, multiline notes stay readable, and the malicious cell is plain text.
    4. In `/admin/leads`, select a vendor with more than 20 leads, export, and confirm the success feedback row count is greater than the current visible page length.
    5. Open the admin CSV in Excel or Google Sheets and confirm the row count matches the feedback message rather than the current page length.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the spreadsheet issue that still fails.</resume-signal>
  <done>Human spreadsheet verification confirms that the exported files are safe, complete, and open correctly in the target apps.</done>
</task>

</tasks>

<verification>
Run the targeted package tests after Tasks 1 and 2. Do not close this plan until the phase-close gate `bun run check && bun run check-types && bun run test` is green and the blocking spreadsheet checkpoint is approved. If the manual spreadsheet check fails, update the CSV formatter or feedback strings and rerun the targeted tests before retrying.
</verification>

<success_criteria>
The shared CSV utility stays the only export format, dangerous spreadsheet cells are neutralized, seller/admin downloads announce exact scope and row count, and a human has verified the files in Excel and Google Sheets.
</success_criteria>

<output>
After completion, create `.planning/phases/12-export-workflows/12-02-SUMMARY.md`
</output>
