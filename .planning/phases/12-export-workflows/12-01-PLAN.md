---
phase: 12-export-workflows
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/lead/export-scope.ts
  - apps/web/src/lib/lead/export-scope.test.ts
  - apps/web/src/app/(app)/leads/lead-list.tsx
  - packages/api/src/routers/admin/leads.ts
  - packages/api/src/__tests__/admin-leads.test.ts
  - apps/web/src/app/(app)/admin/leads/leads-panel.tsx
autonomous: true
requirements:
  - ENH-01
  - ENH-07
must_haves:
  truths:
    - "Seller export in /leads uses the full active scope instead of the currently loaded infinite-scroll slice."
    - "Admin export in /admin/leads uses the full current admin filter scope instead of the current paginated page."
    - "Both export entry points keep using the existing seller and admin screens per D-05."
  artifacts:
    - path: "apps/web/src/lib/lead/export-scope.ts"
      provides: "Dexie-backed seller export scope helpers returning full-scope leads plus total."
      exports:
        - "queryLeadExportScope"
    - path: "packages/api/src/routers/admin/leads.ts"
      provides: "Dedicated admin export procedure that accepts the current admin filter object without pagination leakage."
      contains: "exportByFilters"
    - path: "apps/web/src/app/(app)/leads/lead-list.tsx"
      provides: "Seller export trigger wired to the full-scope export helper."
    - path: "apps/web/src/app/(app)/admin/leads/leads-panel.tsx"
      provides: "Admin export trigger wired to the live admin filter state and dedicated export procedure."
  key_links:
    - from: "apps/web/src/app/(app)/leads/lead-list.tsx"
      to: "apps/web/src/lib/lead/export-scope.ts"
      via: "export button click handler"
      pattern: "queryLeadExportScope"
    - from: "apps/web/src/app/(app)/admin/leads/leads-panel.tsx"
      to: "packages/api/src/routers/admin/leads.ts"
      via: "trpc.admin.leads.exportByFilters"
      pattern: "exportByFilters"
    - from: "packages/api/src/routers/admin/leads.ts"
      to: "@dashboard-leads-profills/db/schema/leads"
      via: "full-scope SQL query ordered by createdAt DESC"
      pattern: "orderBy\\(sql`\\$\\{leads\\.createdAt\\} DESC`\\)"
---

<objective>
Separate export datasets from render datasets so seller and admin exports are complete, scope-aware, and no longer tied to infinite scroll or pagination.

Purpose: Fix the core correctness bug behind Phase 12 before hardening the CSV contract.
Output: Seller export-scope helper, admin export procedure, and both screen actions wired to the new full-scope paths.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-export-workflows/12-CONTEXT.md
@.planning/phases/12-export-workflows/12-RESEARCH.md
@.planning/phases/12-export-workflows/12-VALIDATION.md
@apps/web/src/lib/lead/queries.ts
@apps/web/src/app/(app)/leads/lead-list.tsx
@apps/web/src/lib/lead/export-csv.ts
@apps/web/src/app/(app)/admin/leads/leads-panel.tsx
@packages/api/src/routers/admin/leads.ts
@packages/api/src/__tests__/admin-leads.test.ts

<interfaces>
From apps/web/src/lib/lead/queries.ts:
```typescript
export type FilterTag = "todos" | "quente" | "morno" | "frio";

export async function queryLeads(
	userId: string,
	tag: FilterTag,
	limit: number
): Promise<Lead[]>;
```

From apps/web/src/lib/lead/export-csv.ts:
```typescript
export function generateCsvContent(leads: ExportableLead[]): string;
export function exportLeadsCsv(leads: ExportableLead[]): void;
```

From packages/api/src/routers/admin/leads.ts:
```typescript
const adminLeadFilterSchema = z.object({
	userId: z.string().uuid(),
});

listByUser: adminProcedure
	.input(adminLeadFilterSchema.extend({
		limit: z.number().min(1).max(100).default(20),
		offset: z.number().min(0).default(0),
	}))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the seller full-scope export helper and test it independently from the render limit</name>
  <files>apps/web/src/lib/lead/export-scope.ts, apps/web/src/lib/lead/export-scope.test.ts</files>
  <read_first>apps/web/src/lib/lead/export-scope.ts (new file), apps/web/src/lib/lead/queries.ts, apps/web/src/lib/db/types.ts, .planning/phases/12-export-workflows/12-CONTEXT.md, .planning/phases/12-export-workflows/12-RESEARCH.md, .planning/phases/12-export-workflows/12-VALIDATION.md</read_first>
  <behavior>
    - Test 1: queryLeadExportScope({ userId: "user-1", tag: "todos", searchTerm: "" }) returns every non-deleted lead for that seller in createdAt desc order, with total equal to leads.length, even when the rendered list would have been limited to 20 rows.
    - Test 2: queryLeadExportScope applies the same filter vocabulary as /leads: tag values "todos" | "quente" | "morno" | "frio" and a trimmed case-insensitive searchTerm over name, company, and email.
    - Test 3: soft-deleted rows and rows from other users are excluded from both leads and total.
  </behavior>
  <action>Create `apps/web/src/lib/lead/export-scope.ts` with an exported `LeadExportScopeInput` type `{ userId: string; tag: FilterTag; searchTerm: string; }`, an exported `LeadExportScopeResult` type `{ leads: Lead[]; total: number; }`, and an exported async function `queryLeadExportScope(input: LeadExportScopeInput): Promise<LeadExportScopeResult>`. Per D-01, this helper must query the full Dexie dataset for `input.userId`, exclude rows where `deletedAt !== null`, optionally narrow by `interestTag`, apply a trimmed lower-cased `searchTerm` against `lead.name`, `lead.company ?? ""`, and `lead.email ?? ""`, sort by `createdAt` descending, and return the complete matching array without any `limit` or pagination argument. Add `apps/web/src/lib/lead/export-scope.test.ts` using `fake-indexeddb` + the existing `db` test setup to cover full-scope behavior, search filtering, tag filtering, and deleted/other-user exclusion. Do not change `queryLeads()` yet; this task creates the dedicated export path instead of repurposing the render helper.</action>
  <acceptance_criteria>
    - `apps/web/src/lib/lead/export-scope.ts` contains `export async function queryLeadExportScope(` and `searchTerm: string`.
    - `apps/web/src/lib/lead/export-scope.ts` does not contain `limit:` or `.slice(`.
    - `apps/web/src/lib/lead/export-scope.test.ts` contains test names covering full scope, search term filtering, and deleted/other-user exclusion.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/web test src/lib/lead/export-scope.test.ts</automated>
  </verify>
  <done>`queryLeadExportScope()` exists, is fully covered by Vitest, and can produce the seller's export dataset independently from infinite-scroll rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a dedicated admin export procedure that accepts the current admin filter object and prove it does not inherit pagination</name>
  <files>packages/api/src/routers/admin/leads.ts, packages/api/src/__tests__/admin-leads.test.ts</files>
  <read_first>packages/api/src/routers/admin/leads.ts, packages/api/src/__tests__/admin-leads.test.ts, .planning/phases/12-export-workflows/12-CONTEXT.md, .planning/phases/12-export-workflows/12-RESEARCH.md, .planning/phases/12-export-workflows/12-VALIDATION.md</read_first>
  <behavior>
    - Test 1: the router exposes a dedicated `exportByFilters` procedure separate from `listByUser`.
    - Test 2: `exportByFilters` accepts the current admin filter object per D-02; today that schema is `{ userId: z.string().uuid() }`, but the contract is modeled as the admin filter input rather than a vendor-only shortcut.
    - Test 3: the procedure query orders by `leads.createdAt DESC` and returns `{ leads, total }` for every non-deleted row matching the current admin filters without `limit` or `offset`.
  </behavior>
  <action>Update `packages/api/src/routers/admin/leads.ts` to keep `listByUser` paginated for the UI and add a new `exportByFilters` adminProcedure per D-02. Extract a shared `adminLeadFilterSchema = z.object({ userId: z.string().uuid() })` (or an equivalently named schema) that represents the current admin filter object, use it directly for `exportByFilters`, and extend it for `listByUser` pagination inputs so the export contract is explicitly "selected vendor + active filters" even though today the live filter object only contains `userId`. The `exportByFilters` query must select from `leads`, filter with `and(eq(leads.userId, input.userId), isNull(leads.deletedAt))`, order by `sql\`${leads.createdAt} DESC\``, and return `{ leads: rows, total: rows.length }` without `.limit()` or `.offset()`. Expand `packages/api/src/__tests__/admin-leads.test.ts` beyond router-shape smoke tests so the file asserts the router exposes `exportByFilters`, and that the shared admin filter contract is reused for export while `listByUser` remains the paginated procedure. Do not widen `listByUser.limit`; the export contract must stay separate.</action>
  <acceptance_criteria>
    - `packages/api/src/routers/admin/leads.ts` contains `exportByFilters:` and a shared admin filter schema rooted in `z.object({ userId: z.string().uuid() })`.
    - The `exportByFilters` block in `packages/api/src/routers/admin/leads.ts` does not contain `.limit(` or `.offset(`.
    - `packages/api/src/__tests__/admin-leads.test.ts` contains an assertion that `adminLeadsRouter.exportByFilters` is defined.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd packages/api test src/__tests__/admin-leads.test.ts</automated>
  </verify>
  <done>The admin router exposes an export-only full-scope procedure keyed off the current admin filter object, and the test suite locks that contract in place without changing UI pagination behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire seller and admin export buttons to the new full-scope data paths</name>
  <files>apps/web/src/app/(app)/leads/lead-list.tsx, apps/web/src/app/(app)/admin/leads/leads-panel.tsx</files>
  <read_first>apps/web/src/app/(app)/leads/lead-list.tsx, apps/web/src/app/(app)/admin/leads/leads-panel.tsx, apps/web/src/lib/lead/export-scope.ts, apps/web/src/lib/lead/export-csv.ts, packages/api/src/routers/admin/leads.ts, .planning/phases/12-export-workflows/12-CONTEXT.md, .planning/phases/12-export-workflows/12-RESEARCH.md</read_first>
  <behavior>
    - Test 1: seller export no longer passes the rendered `leads` array to `exportLeadsCsv`; it fetches the full seller scope for `{ userId, activeTag, searchTerm }` first.
    - Test 2: admin export no longer passes `leadsQuery.data?.leads` to `exportLeadsCsv`; it requests `trpc.admin.leads.exportByFilters` with the live admin filter object derived from the current panel state.
    - Test 3: both export buttons stay in the existing page header surfaces per D-05.
  </behavior>
  <action>Update `apps/web/src/app/(app)/leads/lead-list.tsx` so the header export button calls an async handler `handleExport()` that invokes `queryLeadExportScope({ userId, tag: activeTag, searchTerm })`, then passes `result.leads` to `exportLeadsCsv`. Keep the export action in the page header per D-05. Update `apps/web/src/app/(app)/admin/leads/leads-panel.tsx` so the export button builds the current admin filter object from live UI state, e.g. `const adminLeadFilters = { userId: selectedVendor }` today, and executes `trpc.admin.leads.exportByFilters.queryOptions(adminLeadFilters)` through React Query or a matching imperative tRPC client path, then passes the returned `leads` to `exportLeadsCsv`. The admin export button must remain disabled or absent until a vendor is selected, and it must no longer use the paginated `leads` array from `listByUser` or hard-code an inline `{ userId }` object at the call site.</action>
  <acceptance_criteria>
    - `apps/web/src/app/(app)/leads/lead-list.tsx` contains `queryLeadExportScope({` and does not contain `exportLeadsCsv(leads)`.
    - `apps/web/src/app/(app)/admin/leads/leads-panel.tsx` contains `exportByFilters` plus a named admin filter object derived from live state, and does not contain `exportLeadsCsv(leads)`.
    - Both files still contain a `Download` icon button in the existing header action area.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/web test src/lib/lead/export-scope.test.ts && bun run --cwd packages/api test src/__tests__/admin-leads.test.ts</automated>
  </verify>
  <done>Both export buttons now gather full-scope datasets before download, eliminating the render-slice and page-slice correctness bugs.</done>
</task>

</tasks>

<verification>
Run the targeted seller and admin tests after each task. Before closing the plan, verify that neither screen still passes its currently rendered array directly into `exportLeadsCsv`.
</verification>

<success_criteria>
Seller export uses a Dexie full-scope helper that respects the active `/leads` scope, admin export uses a dedicated non-paginated router procedure, and both buttons remain in the existing screens while exporting complete datasets.
</success_criteria>

<output>
After completion, create `.planning/phases/12-export-workflows/12-01-SUMMARY.md`
</output>
