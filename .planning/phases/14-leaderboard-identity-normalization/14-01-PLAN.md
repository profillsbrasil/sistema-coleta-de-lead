---
phase: 14-leaderboard-identity-normalization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/api/src/__tests__/leaderboard.test.ts
  - packages/api/src/routers/leaderboard.ts
  - packages/api/src/routers/admin/leads.ts
  - packages/api/src/routers/admin/stats.ts
  - apps/web/src/components/leaderboard-entry.tsx
autonomous: true
requirements:
  - ENH-06
  - ENH-09

must_haves:
  truths:
    - "leaderboard.getRanking retorna name sempre non-null com fallback 'Vendedor #N' (N = rank 1-indexed)"
    - "leaderboard.getRanking expoe campo rank: number no payload para cada entrada"
    - "leaderboard.getRanking mantem isCurrentUser no payload mas nao usa para substituir o nome"
    - "admin/leads.listVendors retorna name sempre non-null com fallback = email prefix (SPLIT_PART)"
    - "admin/stats.getRanking retorna name sempre non-null com fallback = email prefix (SPLIT_PART)"
    - "LeaderboardEntry nao exibe '(voce)' apos o nome do usuario atual"
  artifacts:
    - path: "packages/api/src/__tests__/leaderboard.test.ts"
      provides: "Cobertura de testes para ENH-06: fallback 'Vendedor #N', isCurrentUser flag, sem '(voce)'"
      exports: []
    - path: "packages/api/src/routers/leaderboard.ts"
      provides: "Query SQL com CTE + ROW_NUMBER() + COALESCE para nome non-null; JS layer simplificado"
      exports: ["leaderboardRouter"]
    - path: "packages/api/src/routers/admin/leads.ts"
      provides: "listVendors com COALESCE(name, SPLIT_PART(email,'@',1)) e ORDER BY 'name' correto"
      exports: ["adminLeadsRouter"]
    - path: "packages/api/src/routers/admin/stats.ts"
      provides: "getRanking com COALESCE(name, SPLIT_PART(email,'@',1)) e u.email no GROUP BY"
      exports: ["adminStatsRouter"]
    - path: "apps/web/src/components/leaderboard-entry.tsx"
      provides: "Exibe name real sem sufixo '(voce)'; isCurrentUser usado apenas para estilo"
      exports: ["LeaderboardEntry"]
  key_links:
    - from: "packages/api/src/routers/leaderboard.ts"
      to: "auth.users"
      via: "JOIN + COALESCE + ROW_NUMBER() OVER (...)"
      pattern: "COALESCE.*rawName.*Vendedor #.*rank"
    - from: "packages/api/src/routers/admin/leads.ts"
      to: "auth.users"
      via: "COALESCE(u.raw_user_meta_data->>'name', SPLIT_PART(u.email,'@',1))"
      pattern: "SPLIT_PART.*email.*@.*1"
    - from: "packages/api/src/routers/admin/stats.ts"
      to: "auth.users"
      via: "COALESCE(u.raw_user_meta_data->>'name', SPLIT_PART(u.email,'@',1)) + u.email in GROUP BY"
      pattern: "SPLIT_PART.*email.*@.*1"
---

<objective>
Corrigir os nomes exibidos no leaderboard e nas superficies admin de forma que nenhuma query retorne null para o campo `name`. Para o leaderboard publico, o fallback e `"Vendedor #N"` (N = posicao no ranking, 1-indexed, computado via SQL `ROW_NUMBER()`). Para superficies admin (`listVendors`, `stats.getRanking`), o fallback e o email prefix (`SPLIT_PART(email, '@', 1)`). O sufixo `" (voce)"` no componente `LeaderboardEntry` e removido — o destaque visual via `isCurrentUser` permanece.

Purpose: ENH-06 e ENH-09 — nome legivel e canonico em todas as superficies relacionadas ao vendedor.
Output: 3 routers corrigidos, 1 componente com 1 linha removida, 1 novo arquivo de testes, suite verde.
</objective>

<execution_context>
@/home/othavio/Work/profills/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/profills/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-leaderboard-identity-normalization/14-CONTEXT.md
@.planning/phases/14-leaderboard-identity-normalization/14-RESEARCH.md

<interfaces>
<!-- Estado atual dos arquivos — extraido para referencia do executor. -->

<!-- packages/api/src/routers/leaderboard.ts — estado ANTES da correcao: -->
```typescript
// Linha 7: currentUserId extraido do ctx.user.sub — MANTER
const currentUserId = (ctx.user as Record<string, unknown>).sub as string;

// Query SQL atual (sem CTE, sem ROW_NUMBER, com name nullable):
SELECT
  l.user_id AS "userId",
  u.raw_user_meta_data->>'name' AS "name",   -- pode ser NULL
  COUNT(*)::int AS "totalLeads",
  SUM(CASE WHEN ... END)::int AS score
FROM leads l
JOIN auth.users u ON u.id = l.user_id::uuid
WHERE l.deleted_at IS NULL
GROUP BY l.user_id, u.raw_user_meta_data->>'name'
ORDER BY score DESC, "totalLeads" DESC

// JS layer atual com fallback condicional — REMOVER:
.map((row) => ({
  ...row,
  name: row.userId === currentUserId ? (row.name ?? "Voce") : (row.name ?? "Vendedor"),
  isCurrentUser: row.userId === currentUserId,
}))
```

<!-- packages/api/src/routers/admin/leads.ts — listVendors atual: -->
```typescript
// Linha 122-136: listVendors sem fallback para null
SELECT DISTINCT
  l.user_id AS "userId",
  u.raw_user_meta_data->>'name' AS "name"   -- pode ser NULL
FROM leads l
JOIN auth.users u ON u.id = l.user_id::uuid
WHERE l.deleted_at IS NULL
ORDER BY "name" ASC
```

<!-- packages/api/src/routers/admin/stats.ts — getRanking atual (linha 114-145): -->
```typescript
SELECT
  leads.user_id AS "userId",
  u.raw_user_meta_data->>'name' AS "name",   -- pode ser NULL
  COUNT(leads.id)::int AS "totalLeads",
  COALESCE(SUM(CASE ... END), 0)::int AS "score"
FROM leads
JOIN auth.users u ON u.id = leads.user_id::uuid
WHERE ${whereClause}
GROUP BY leads.user_id, u.raw_user_meta_data->>'name'
ORDER BY "score" DESC
// GROUP BY nao inclui u.email — deve ser adicionado junto com SPLIT_PART
```

<!-- apps/web/src/components/leaderboard-entry.tsx — linha 37-38 a remover: -->
```tsx
{name}
{isCurrentUser ? " (voce)" : ""}   // <- REMOVER esta linha inteira
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Criar leaderboard.test.ts com casos RED antes da correcao</name>
  <files>packages/api/src/__tests__/leaderboard.test.ts</files>
  <read_first>
    - packages/api/src/__tests__/admin-leads.test.ts (padrao de mock com vi.doMock + vi.resetModules — replicar exatamente)
    - packages/api/src/routers/leaderboard.ts (estado atual — entender o que vai mudar)
  </read_first>
  <behavior>
    - Test 1: getRanking retorna name = "Vendedor #1" para usuario com raw_user_meta_data.name = null na posicao 1
    - Test 2: getRanking retorna name = "Vendedor #2" para usuario com raw_user_meta_data.name = null na posicao 2
    - Test 3: getRanking retorna o nome real para usuario com raw_user_meta_data.name = "Maria Silva"
    - Test 4: getRanking inclui campo isCurrentUser: true para o usuario com userId === currentUserId
    - Test 5: getRanking inclui campo isCurrentUser: false para outros usuarios
    - Test 6: getRanking nao inclui a string "(voce)" em nenhum campo name retornado
    - Test 7: getRanking expoe campo rank: number em cada entrada (rank = posicao SQL, nao index JS)
  </behavior>
  <action>
Criar `packages/api/src/__tests__/leaderboard.test.ts` seguindo o padrao de `admin-leads.test.ts`:

1. Setup de mocks no topo do arquivo com `vi.mock` para `@dashboard-leads-profills/env/server` (mesmos valores que os outros test files).

2. Criar funcao `loadLeaderboardRouter(mockRows)` que usa `vi.doMock` + `vi.resetModules()` no `beforeEach` para isolar modulos entre testes. O mock de `@dashboard-leads-profills/db` deve expor `db.execute` como `vi.fn()`.

3. A funcao helper deve retornar uma instancia do router com `createCaller({ supabase: {} as never, user: { sub: "user-123" } })`.

4. O mock de `db.execute` deve retornar `{ rows: mockRows }` onde `mockRows` tem o formato final esperado pos-SQL (com `rank` e `name` ja resolvidos pelo COALESCE).

5. O mock de `drizzle-orm` deve incluir `sql` como tagged template que retorna um objeto simples (mesmo padrao de `admin-leads.test.ts`).

6. Estrutura dos casos:

```typescript
// Row format para o mock (simula saida SQL pos-COALESCE):
interface MockLeaderboardRow {
  userId: string;
  name: string;        // COALESCE ja aplicado pelo SQL mock
  totalLeads: number;
  score: number;
  rank: number;        // ROW_NUMBER() ja aplicado pelo SQL mock
}

// Test 1: fallback "Vendedor #1" para null name na posicao 1
it("retorna 'Vendedor #1' para usuario sem nome na posicao 1", async () => {
  const { router, caller } = await loadLeaderboardRouter([
    { userId: "user-abc", name: "Vendedor #1", totalLeads: 5, score: 10, rank: 1 },
  ]);
  const result = await caller.getRanking();
  expect(result.ranking[0].name).toBe("Vendedor #1");
  expect(result.ranking[0].rank).toBe(1);
});

// Test 4: isCurrentUser = true para userId === currentUserId (ctx.user.sub = "user-123")
it("marca isCurrentUser: true para o usuario autenticado", async () => {
  const { caller } = await loadLeaderboardRouter([
    { userId: "user-123", name: "Joao", totalLeads: 8, score: 20, rank: 1 },
    { userId: "user-456", name: "Maria", totalLeads: 3, score: 7, rank: 2 },
  ]);
  const result = await caller.getRanking();
  expect(result.ranking[0].isCurrentUser).toBe(true);
  expect(result.ranking[1].isCurrentUser).toBe(false);
});

// Test 6: nome nunca contem "(voce)"
it("nunca inclui '(voce)' no campo name mesmo para o usuario atual", async () => {
  const { caller } = await loadLeaderboardRouter([
    { userId: "user-123", name: "Joao", totalLeads: 8, score: 20, rank: 1 },
  ]);
  const result = await caller.getRanking();
  expect(result.ranking[0].name).not.toContain("(voce)");
  expect(result.ranking[0].name).not.toContain("Voce");
});

// Test 7: rank presente no payload
it("inclui campo rank em cada entrada do ranking", async () => {
  const { caller } = await loadLeaderboardRouter([
    { userId: "user-abc", name: "Vendedor #1", totalLeads: 5, score: 10, rank: 1 },
    { userId: "user-xyz", name: "Pedro", totalLeads: 2, score: 4, rank: 2 },
  ]);
  const result = await caller.getRanking();
  expect(result.ranking[0].rank).toBe(1);
  expect(result.ranking[1].rank).toBe(2);
});
```

**Fase RED:** Rodar os testes — DEVEM FALHAR porque `leaderboard.ts` ainda tem a logica antiga com `name: row.userId === currentUserId ? (row.name ?? "Voce") : (row.name ?? "Vendedor")` e nao retorna o campo `rank`.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun vitest run packages/api/src/__tests__/leaderboard.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Arquivo `packages/api/src/__tests__/leaderboard.test.ts` existe com pelo menos 7 test cases; testes falham (RED) confirmando que a correcao e necessaria.</done>
</task>

<task type="tdd">
  <name>Task 2: Corrigir leaderboard.ts — CTE + ROW_NUMBER + remover JS fallback (GREEN)</name>
  <files>packages/api/src/routers/leaderboard.ts</files>
  <read_first>
    - packages/api/src/routers/leaderboard.ts (estado atual completo — ler antes de editar)
    - packages/api/src/__tests__/leaderboard.test.ts (testes RED criados na Task 1)
  </read_first>
  <behavior>
    - Query SQL usa CTE `WITH ranked AS (...)` que computa `ROW_NUMBER() OVER (ORDER BY score DESC, totalLeads DESC)` como `rank`
    - Outer SELECT usa `COALESCE("rawName", 'Vendedor #' || rank) AS "name"`
    - JS `.map()` nao tem mais `?? "Voce"` nem `?? "Vendedor"` — apenas computa `isCurrentUser`
    - Tipo da row anotado como `{ userId: string; name: string; totalLeads: number; score: number; rank: number }` (name non-null, rank adicionado)
    - `currentUserId` ainda extraido de `ctx.user.sub` — necessario para `isCurrentUser`
    - Retorno incluiu `rank` em cada entrada do ranking
  </behavior>
  <action>
Substituir o conteudo de `packages/api/src/routers/leaderboard.ts` com a versao corrigida:

```typescript
import { db } from "@dashboard-leads-profills/db";
import { sql } from "drizzle-orm";
import { protectedProcedure, router } from "../index";

export const leaderboardRouter = router({
	getRanking: protectedProcedure.query(async ({ ctx }) => {
		const currentUserId = (ctx.user as Record<string, unknown>).sub as string;

		const result = await db.execute(sql`
			WITH ranked AS (
				SELECT
					l.user_id AS "userId",
					u.raw_user_meta_data->>'name' AS "rawName",
					COUNT(*)::int AS "totalLeads",
					SUM(CASE
						WHEN l.interest_tag = 'quente' THEN 3
						WHEN l.interest_tag = 'morno' THEN 2
						ELSE 1
					END)::int AS score,
					ROW_NUMBER() OVER (
						ORDER BY
							SUM(CASE
								WHEN l.interest_tag = 'quente' THEN 3
								WHEN l.interest_tag = 'morno' THEN 2
								ELSE 1
							END) DESC,
							COUNT(*) DESC
					) AS rank
				FROM leads l
				JOIN auth.users u ON u.id = l.user_id::uuid
				WHERE l.deleted_at IS NULL
				GROUP BY l.user_id, u.raw_user_meta_data->>'name'
			)
			SELECT
				"userId",
				COALESCE("rawName", 'Vendedor #' || rank) AS "name",
				"totalLeads",
				score,
				rank
			FROM ranked
			ORDER BY score DESC, "totalLeads" DESC
		`);

		const ranking = (
			result.rows as Array<{
				userId: string;
				name: string;
				totalLeads: number;
				score: number;
				rank: number;
			}>
		).map((row) => ({
			...row,
			isCurrentUser: row.userId === currentUserId,
		}));

		return {
			ranking,
			serverTimestamp: new Date().toISOString(),
		};
	}),
});
```

**Fase GREEN:** Rodar os testes — DEVEM PASSAR.

**Nota sobre o mock em testes:** O mock de `db.execute` ja retorna rows com `rank` e `name` pre-resolvidos (simulando o SQL COALESCE). O JS layer agora apenas passa o `rank` adiante via spread `...row`. Os testes validam o comportamento do JS layer (mapeamento de `isCurrentUser`), nao o SQL em si.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun vitest run packages/api/src/__tests__/leaderboard.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Todos os 7 testes em `leaderboard.test.ts` passam. `leaderboard.ts` nao contem `?? "Voce"` nem `?? "Vendedor"`. Arquivo contem `COALESCE` e `ROW_NUMBER` na query SQL.</done>
</task>

<task type="auto">
  <name>Task 3: Corrigir admin/leads.ts (listVendors) e admin/stats.ts (getRanking) + remover "(voce)" de leaderboard-entry.tsx</name>
  <files>
    packages/api/src/routers/admin/leads.ts
    packages/api/src/routers/admin/stats.ts
    apps/web/src/components/leaderboard-entry.tsx
  </files>
  <read_first>
    - packages/api/src/routers/admin/leads.ts (ler completo — editar apenas listVendors, linhas 122-136)
    - packages/api/src/routers/admin/stats.ts (ler completo — editar apenas getRanking, linhas 114-145)
    - apps/web/src/components/leaderboard-entry.tsx (ler completo — remover linha 38 com "(voce)")
    - packages/api/src/__tests__/admin-leads.test.ts (ver padrao de mock existente — reutilizar para novo test case)
    - packages/api/src/__tests__/admin-stats.test.ts (ver padrao atual — adicionar test case)
  </read_first>
  <action>
**1. Corrigir `packages/api/src/routers/admin/leads.ts` — procedure `listVendors`:**

Substituir o bloco `listVendors` (linhas 122-136) por:

```typescript
listVendors: adminProcedure.query(async () => {
    const rows = await db.execute(
        sql`
            SELECT DISTINCT
                l.user_id AS "userId",
                COALESCE(
                    u.raw_user_meta_data->>'name',
                    SPLIT_PART(u.email, '@', 1)
                ) AS "name"
            FROM leads l
            JOIN auth.users u ON u.id = l.user_id::uuid
            WHERE l.deleted_at IS NULL
            ORDER BY "name" ASC
        `
    );

    return rows.rows as Array<{ userId: string; name: string }>;
}),
```

Mudancas: adicionado `COALESCE(u.raw_user_meta_data->>'name', SPLIT_PART(u.email, '@', 1))` no SELECT. O resto da procedure e inalterado.

**2. Corrigir `packages/api/src/routers/admin/stats.ts` — procedure `getRanking`:**

Substituir o bloco da query SQL em `getRanking` (linhas 120-137). Mudar:
- SELECT: `u.raw_user_meta_data->>'name' AS "name"` -> `COALESCE(u.raw_user_meta_data->>'name', SPLIT_PART(u.email, '@', 1)) AS "name"`
- GROUP BY: `leads.user_id, u.raw_user_meta_data->>'name'` -> `leads.user_id, u.raw_user_meta_data->>'name', u.email`

Query resultante:
```sql
SELECT
  leads.user_id AS "userId",
  COALESCE(
    u.raw_user_meta_data->>'name',
    SPLIT_PART(u.email, '@', 1)
  ) AS "name",
  COUNT(leads.id)::int AS "totalLeads",
  COALESCE(SUM(CASE
    WHEN leads.interest_tag = 'quente' THEN 3
    WHEN leads.interest_tag = 'morno' THEN 2
    ELSE 1
  END), 0)::int AS "score"
FROM leads
JOIN auth.users u ON u.id = leads.user_id::uuid
WHERE ${whereClause}
GROUP BY leads.user_id, u.raw_user_meta_data->>'name', u.email
ORDER BY "score" DESC
```

O cast do retorno muda de `name: string` (que ja era incorreto — podia ser null) para continuar como `name: string` (agora garantido por COALESCE).

**3. Corrigir `apps/web/src/components/leaderboard-entry.tsx` — remover sufixo "(voce)":**

Remover apenas a linha 38:
```tsx
{isCurrentUser ? " (voce)" : ""}
```

O paragrafo resultante fica:
```tsx
<p className="font-semibold text-sm">
  {name}
</p>
```

O `isCurrentUser` continua sendo usado na linha 26-31 para o estilo da Card (`border-2 border-primary bg-primary/5`) e no atributo `aria-current`. Nao remover essas referencias.

**4. Adicionar test case em `packages/api/src/__tests__/admin-leads.test.ts`:**

Adicionar ao final do `describe("adminLeadsRouter")`:

```typescript
it("listVendors retorna name nao-nulo usando SPLIT_PART como fallback", async () => {
  // Simula saida SQL com COALESCE ja aplicado (mock retorna row com name resolvido)
  const executeResult = {
    rows: [
      { userId: "user-with-name", name: "Ana Costa" },
      { userId: "user-no-name", name: "joao.silva" },  // email prefix fallback
    ],
  };

  const { adminLeadsRouter } = await loadAdminLeadsRouter([]);
  // Override execute mock para listVendors
  const { db } = await import("@dashboard-leads-profills/db");
  (db.execute as ReturnType<typeof vi.fn>).mockResolvedValue(executeResult);

  const caller = adminLeadsRouter.createCaller({
    supabase: {} as never,
    user: { sub: "admin-user" },
    userRole: "admin",
  });

  const result = await caller.listVendors();

  expect(result).toHaveLength(2);
  expect(result[0].name).toBe("Ana Costa");
  expect(result[1].name).toBe("joao.silva");
  for (const vendor of result) {
    expect(vendor.name).toBeTruthy();
    expect(typeof vendor.name).toBe("string");
  }
});
```

**5. Adicionar test cases em `packages/api/src/__tests__/admin-stats.test.ts`:**

Refatorar o arquivo para usar `vi.doMock` + `vi.resetModules()` no `beforeEach` (igual ao padrao de `admin-leads.test.ts`). Adicionar:

```typescript
it("getRanking retorna name nao-nulo para usuario sem metadata de nome", async () => {
  const executeResult = {
    rows: [
      { userId: "user-1", name: "Maria", totalLeads: 10, score: 25 },
      { userId: "user-2", name: "pedro.alves", totalLeads: 5, score: 12 },  // email prefix
    ],
  };

  // mock db.execute para retornar dados simulados
  vi.doMock("@dashboard-leads-profills/db", () => ({
    db: { execute: vi.fn().mockResolvedValue(executeResult) },
  }));
  // ... imports e caller setup
  const result = await caller.getRanking({});
  expect(result[1].name).toBe("pedro.alves");
  for (const entry of result) {
    expect(entry.name).toBeTruthy();
  }
});
```
  </action>
  <verify>
    <automated>cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun vitest run packages/api 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - packages/api/src/routers/admin/leads.ts contem a string "SPLIT_PART" na procedure listVendors
    - packages/api/src/routers/admin/leads.ts contem "COALESCE" na procedure listVendors
    - packages/api/src/routers/admin/stats.ts contem "SPLIT_PART" na procedure getRanking
    - packages/api/src/routers/admin/stats.ts contem "u.email" no GROUP BY do getRanking
    - apps/web/src/components/leaderboard-entry.tsx NAO contem a string "(voce)"
    - apps/web/src/components/leaderboard-entry.tsx ainda contem "border-2 border-primary" (estilo isCurrentUser intacto)
    - `bun vitest run packages/api` sai com codigo 0 (todos os testes passam)
  </acceptance_criteria>
  <done>
    - `admin/leads.ts` usa COALESCE + SPLIT_PART em listVendors
    - `admin/stats.ts` usa COALESCE + SPLIT_PART em getRanking, com u.email no GROUP BY
    - `leaderboard-entry.tsx` nao tem o sufixo "(voce)" — apenas o nome real e exibido
    - Suite `packages/api` verde com os novos test cases passando
  </done>
</task>

</tasks>

<verification>
Apos completar as 3 tasks:

```bash
# Suite completa da API
cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun vitest run packages/api

# Type check para garantir que as assinaturas estao corretas
cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run check-types
```

Verificacoes manuais de grep:
- `grep -n "Voce\|Vendedor" packages/api/src/routers/leaderboard.ts` deve retornar zero linhas
- `grep -n "voce\|Voce" apps/web/src/components/leaderboard-entry.tsx` deve retornar zero linhas
- `grep -n "COALESCE" packages/api/src/routers/leaderboard.ts` deve retornar linha com `COALESCE("rawName", 'Vendedor #'`
- `grep -n "SPLIT_PART" packages/api/src/routers/admin/leads.ts` deve retornar resultado
- `grep -n "SPLIT_PART" packages/api/src/routers/admin/stats.ts` deve retornar resultado
- `grep -n "u.email" packages/api/src/routers/admin/stats.ts` deve retornar linha com GROUP BY
</verification>

<success_criteria>
1. `leaderboard.ts` usa CTE + ROW_NUMBER() + COALESCE; nenhum `?? "Voce"` ou `?? "Vendedor"` no JS layer
2. `admin/leads.ts listVendors` usa COALESCE + SPLIT_PART; name sempre non-null
3. `admin/stats.ts getRanking` usa COALESCE + SPLIT_PART; u.email no GROUP BY; name sempre non-null
4. `leaderboard-entry.tsx` sem "(voce)" — destaque visual via isCurrentUser intacto
5. `packages/api/src/__tests__/leaderboard.test.ts` existe com >= 7 casos cobrindo ENH-06
6. `bun vitest run packages/api` sai com codigo 0
7. `bun run check-types` sai sem erros em packages/api e apps/web
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/14-leaderboard-identity-normalization/14-01-SUMMARY.md` com:
- Arquivos modificados e o que mudou em cada
- SQL patterns usados (CTE, COALESCE, ROW_NUMBER, SPLIT_PART)
- Decisoes de implementacao tomadas (helper inline vs shared — D-07)
- Resultado dos testes
</output>
