---
phase: 14-leaderboard-identity-normalization
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx
autonomous: false
requirements:
  - ENH-06
  - ENH-09

must_haves:
  truths:
    - "leaderboard-tab.tsx usa rank do SQL (row.rank) em vez de i+1 para o Dexie cache — evita rank drift"
    - "Cache offline mostra nomes corretos apos sync (overwrite automatico via clear + bulkPut)"
    - "Usuario atual tem destaque visual (border/bg) no leaderboard SEM o sufixo '(voce)'"
    - "Toda a suite de testes passa antes da verificacao visual"
  artifacts:
    - path: "apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx"
      provides: "Mapeamento de rank usa row.rank do servidor em vez de i+1"
      exports: ["LeaderboardTab (default)"]
  key_links:
    - from: "apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx"
      to: "packages/api/src/routers/leaderboard.ts"
      via: "trpc.leaderboard.getRanking retorna rank: number em cada entrada"
      pattern: "row\\.rank"
    - from: "apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx"
      to: "apps/web/src/lib/db/index.ts (Dexie leaderboardCache)"
      via: "clear() + bulkPut() com rank: r.rank (em vez de i+1)"
      pattern: "rank: r\\.rank"
---

<objective>
Atualizar `leaderboard-tab.tsx` para consumir o campo `rank` retornado pelo servidor (resultado de `ROW_NUMBER()` no SQL) em vez de recomputar via `i+1` no JS. Isso elimina o rank drift descrito no Pitfall 3 do RESEARCH.md: o nome `"Vendedor #3"` seria calculado pelo SQL com rank=3, mas o cache Dexie poderia gravar rank=4 se usasse `i+1` com alguma diferenca de ordenacao. Apos a correcao, verificacao visual confirma nomes corretos no browser e destaque do usuario atual sem "(voce)".

Purpose: ENH-09 — consistencia do cache offline; ENH-06 — rank exibido bate com rank no nome fallback.
Output: `leaderboard-tab.tsx` corrigido com rank do servidor; verificacao visual aprovada.
</objective>

<execution_context>
@/home/othavio/Work/profills/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/profills/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-leaderboard-identity-normalization/14-CONTEXT.md
@.planning/phases/14-leaderboard-identity-normalization/14-01-SUMMARY.md

<interfaces>
<!-- Estado atual de leaderboard-tab.tsx antes desta correcao: -->
```typescript
// Linha 35-42 — caching para Dexie (rank calculado via i+1):
const entries = serverData.ranking.map((r, i) => ({
  userId: r.userId,
  name: r.name,
  totalLeads: r.totalLeads,
  score: r.score,
  rank: i + 1,            // <- SUBSTITUIR por r.rank (vem do SQL pos-Plan 01)
  lastSyncAt: serverData.serverTimestamp,
}));

// Linha 63-67 — displayEntries para render (rank calculado via i+1):
const displayEntries = serverData
  ? serverData.ranking.map((r, i) => ({
      ...r,
      rank: i + 1,          // <- SUBSTITUIR por r.rank
      lastSyncAt: serverData.serverTimestamp,
    }))
  : (cachedEntries ?? []);
```

<!-- Tipo retornado por leaderboard.getRanking (pos-Plan 01): -->
```typescript
// Cada entrada em serverData.ranking agora inclui:
{
  userId: string;
  name: string;       // COALESCE garantido pelo SQL
  totalLeads: number;
  score: number;
  rank: number;       // ROW_NUMBER() do SQL — NOVO campo apos Plan 01
  isCurrentUser: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar leaderboard-tab.tsx para usar rank do servidor</name>
  <files>apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx</files>
  <read_first>
    - apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx (ler completo — entender estrutura atual)
    - .planning/phases/14-leaderboard-identity-normalization/14-01-SUMMARY.md (confirmar que rank: number esta no payload)
  </read_first>
  <action>
Fazer duas substituicoes cirurgicas em `apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx`:

**Substituicao 1 — bloco de cache Dexie (useEffect, linha ~35):**

ANTES:
```typescript
const entries = serverData.ranking.map((r, i) => ({
  userId: r.userId,
  name: r.name,
  totalLeads: r.totalLeads,
  score: r.score,
  rank: i + 1,
  lastSyncAt: serverData.serverTimestamp,
}));
```

DEPOIS:
```typescript
const entries = serverData.ranking.map((r) => ({
  userId: r.userId,
  name: r.name,
  totalLeads: r.totalLeads,
  score: r.score,
  rank: r.rank,
  lastSyncAt: serverData.serverTimestamp,
}));
```

Nota: remover o parametro `i` do callback — nao e mais usado. Biome flagra parametros inutilizados.

**Substituicao 2 — displayEntries para render (linha ~62):**

ANTES:
```typescript
const displayEntries = serverData
  ? serverData.ranking.map((r, i) => ({
      ...r,
      rank: i + 1,
      lastSyncAt: serverData.serverTimestamp,
    }))
  : (cachedEntries ?? []);
```

DEPOIS:
```typescript
const displayEntries = serverData
  ? serverData.ranking.map((r) => ({
      ...r,
      lastSyncAt: serverData.serverTimestamp,
    }))
  : (cachedEntries ?? []);
```

Nota: `rank: i + 1` removido porque `r.rank` ja e incluido via spread `...r`. Remover tambem o parametro `i` do callback.

**Nao alterar nada mais no arquivo.** O `isCurrentUser={entry.userId === userId}` na linha ~98 ja usa comparacao correta no client (D-04 e Pitfall 5 do RESEARCH.md).
  </action>
  <verify>
    <automated>cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx NAO contem "rank: i + 1" nem ", i)" em callbacks de mapping
    - apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx contem "rank: r.rank" no bloco de cache Dexie
    - apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx ainda contem "db.leaderboardCache.clear()" e "db.leaderboardCache.bulkPut" (cache intacto)
    - `bun run check-types` sai sem erros em apps/web
  </acceptance_criteria>
  <done>
    `leaderboard-tab.tsx` usa `r.rank` (valor SQL) no lugar de `i+1` para o cache Dexie e para displayEntries. Type check passa.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rodar suite completa e verificar Biome antes do checkpoint</name>
  <files></files>
  <read_first>
    - apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx (confirmar estado final)
  </read_first>
  <action>
Executar em sequencia:

1. Suite completa de testes:
```bash
cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run test
```

2. Biome check no escopo dos arquivos alterados na fase:
```bash
cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun x biome check packages/api/src/routers/leaderboard.ts packages/api/src/routers/admin/leads.ts packages/api/src/routers/admin/stats.ts apps/web/src/components/leaderboard-entry.tsx apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx
```

Se Biome retornar erros autocorrigiveis, rodar:
```bash
cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun x biome check --write packages/api/src/routers/leaderboard.ts packages/api/src/routers/admin/leads.ts packages/api/src/routers/admin/stats.ts apps/web/src/components/leaderboard-entry.tsx apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx
```

3. Verificar que `leaderboard-tab.tsx` nao tem parametros `i` inutilizados (Biome flagra):
```bash
grep -n ", i)" /home/othavio/Work/profills/sistema-coleta-de-lead/apps/web/src/app/\(app\)/dashboard/leaderboard-tab.tsx
```
Deve retornar 0 linhas.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test` sai com codigo 0 (suite completa verde)
    - Biome nao reporta erros nos 5 arquivos modificados na fase
    - Sem parametros inutilizados em callbacks de mapping em leaderboard-tab.tsx
  </acceptance_criteria>
  <done>Suite completa verde. Biome limpo nos arquivos da fase. Pronto para verificacao visual.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual — nomes corretos no leaderboard e admin, cache offline funcional</name>
  <what-built>
    Correcao completa de identidade no leaderboard e superficies admin:
    - leaderboard.ts: query SQL com CTE + ROW_NUMBER() + COALESCE — fallback "Vendedor #N"
    - admin/leads.ts (listVendors): COALESCE + SPLIT_PART para email prefix fallback
    - admin/stats.ts (getRanking): idem, com u.email adicionado ao GROUP BY
    - leaderboard-entry.tsx: sufixo "(voce)" removido — destaque visual via border/bg permanece
    - leaderboard-tab.tsx: rank do servidor usado no cache Dexie (sem rank drift)
  </what-built>
  <how-to-verify>
    1. Iniciar o app: `bun run dev:web` (porta 3001)
    2. Abrir http://localhost:3001 e fazer login com um vendedor
    3. Ir para a aba Ranking no Dashboard
    4. Verificar:
       - [ ] Todos os vendedores mostram nomes legiveis (nao "Vendedor" generico)
       - [ ] Vendedores sem nome configurado mostram "Vendedor #1", "Vendedor #2", etc.
       - [ ] Seu usuario atual tem destaque visual (borda + fundo destacados) MAS nao mostra "(voce)" no nome
       - [ ] O numero do fallback (#N) bate com a posicao exibida no badge de rank
    5. Testar offline cache:
       - Com o ranking carregado online, abrir DevTools -> Network -> Offline
       - Recarregar a pagina
       - Verificar que o ranking ainda aparece com nomes corretos (do cache Dexie)
    6. Ir para /admin/dashboard (se disponivel) e verificar:
       - [ ] Seletor de vendedor mostra nomes legiveis (nao null/undefined)
       - [ ] Breakdown de stats por vendedor mostra nomes legiveis
  </how-to-verify>
  <resume-signal>Digite "aprovado" se tudo estiver correto, ou descreva o problema encontrado</resume-signal>
</task>

</tasks>

<verification>
Verificacoes finais da fase:

```bash
# Suite completa
cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run test

# Type check completo
cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run check-types

# Grep: confirmar remocao de fallbacks JS antigos
grep -n '"Voce"\|"Vendedor"\|(voce)' \
  packages/api/src/routers/leaderboard.ts \
  apps/web/src/components/leaderboard-entry.tsx

# Grep: confirmar presenca dos novos patterns SQL
grep -n "COALESCE\|ROW_NUMBER\|SPLIT_PART" \
  packages/api/src/routers/leaderboard.ts \
  packages/api/src/routers/admin/leads.ts \
  packages/api/src/routers/admin/stats.ts

# Grep: confirmar rank do servidor no tab
grep -n "rank: r\.rank\|rank: i" \
  apps/web/src/app/\(app\)/dashboard/leaderboard-tab.tsx
```
</verification>

<success_criteria>
1. `bun run test` verde (suite completa)
2. `bun run check-types` sem erros
3. `leaderboard-tab.tsx` usa `r.rank` — zero ocorrencias de `rank: i + 1`
4. Verificacao visual aprovada: nomes legiveis, destaque sem "(voce)", rank badge consistente com nome fallback
5. Cache offline funciona: ranking aparece offline com nomes corretos
</success_criteria>

<output>
Apos conclusao e aprovacao visual, criar `.planning/phases/14-leaderboard-identity-normalization/14-02-SUMMARY.md` com:
- Correcao no leaderboard-tab.tsx (rank drift eliminado)
- Resultado da verificacao visual (aprovado/problemas encontrados)
- Estado final da suite de testes
- Observacoes sobre o comportamento offline do cache
</output>
