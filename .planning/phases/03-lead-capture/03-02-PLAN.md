---
phase: 03-lead-capture
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/app/leads/new/page.tsx
  - apps/web/src/components/lead-form.tsx
  - apps/web/src/components/tag-selector.tsx
  - apps/web/src/components/fab.tsx
  - apps/web/src/app/dashboard/page.tsx
autonomous: true
requirements:
  - CAPT-01
  - CAPT-02
  - CAPT-07
  - CAPT-08

must_haves:
  truths:
    - "Pagina /leads/new renderiza formulario de captura de lead"
    - "Formulario tem campos nome, telefone, email visiveis e campos opcionais colapsados"
    - "Tag selector com 3 opcoes (quente/morno/frio) e default morno pre-selecionado"
    - "Submit salva lead no Dexie via saveLead (offline-first, sem network call)"
    - "FAB no dashboard leva para /leads/new"
    - "Toast 'Lead salvo!' exibido apos sucesso e redirect para pagina anterior"
  artifacts:
    - path: "apps/web/src/app/leads/new/page.tsx"
      provides: "Server component wrapper for LeadForm"
      min_lines: 5
    - path: "apps/web/src/components/lead-form.tsx"
      provides: "Complete lead capture form with validation and Dexie write"
      min_lines: 80
    - path: "apps/web/src/components/tag-selector.tsx"
      provides: "3-button toggle for interest tag with semantic colors"
      min_lines: 30
    - path: "apps/web/src/components/fab.tsx"
      provides: "Floating action button linking to /leads/new"
      min_lines: 10
  key_links:
    - from: "apps/web/src/components/lead-form.tsx"
      to: "apps/web/src/lib/lead/save-lead.ts"
      via: "saveLead() call on form submit"
      pattern: "saveLead"
    - from: "apps/web/src/components/lead-form.tsx"
      to: "apps/web/src/lib/lead/validation.ts"
      via: "leadFormSchema.parse on submit"
      pattern: "leadFormSchema"
    - from: "apps/web/src/components/lead-form.tsx"
      to: "apps/web/src/lib/sync/engine.ts"
      via: "requestSync or syncCycle trigger after save"
      pattern: "sync"
    - from: "apps/web/src/components/fab.tsx"
      to: "apps/web/src/app/leads/new/page.tsx"
      via: "Link href /leads/new"
      pattern: "/leads/new"
---

<objective>
Build the core lead capture form UI: LeadForm component with validation, TagSelector with semantic colors, FAB button, and the /leads/new page. Form saves to Dexie offline-first.

Purpose: Deliver the primary user flow — vendedor opens form, fills required fields, selects tag, saves lead in under 3 touches.
Output: Working lead capture form at /leads/new, FAB on dashboard, tag selector, offline-first save.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-lead-capture/03-CONTEXT.md
@.planning/phases/03-lead-capture/03-UI-SPEC.md
@.planning/phases/03-lead-capture/03-01-SUMMARY.md

<interfaces>
<!-- Utility modules from Plan 01 -->

From apps/web/src/lib/lead/validation.ts:
```typescript
export const leadFormSchema: ZodSchema; // name, phone, email, interestTag, company, position, segment, notes
export type LeadFormData = z.infer<typeof leadFormSchema>;
```

From apps/web/src/lib/lead/save-lead.ts:
```typescript
export function saveLead(data: LeadFormData, userId: string, photo: Blob | null): Promise<string>;
```

From apps/web/src/lib/db/types.ts:
```typescript
export interface Lead {
  interestTag: "quente" | "morno" | "frio";
  // ... other fields
}
```

Existing UI components:
```typescript
// @dashboard-leads-profills/ui/components/button — Button with variants
// @dashboard-leads-profills/ui/components/card — Card, CardHeader, CardTitle, CardContent
// @dashboard-leads-profills/ui/components/input — Input
// @dashboard-leads-profills/ui/components/label — Label
// @dashboard-leads-profills/ui/components/sonner — toast()
// @dashboard-leads-profills/ui/components/collapsible — Collapsible, CollapsibleTrigger, CollapsibleContent (installed in Plan 01)
// @dashboard-leads-profills/ui/components/textarea — Textarea (installed in Plan 01)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TagSelector component and LeadForm with /leads/new page</name>
  <files>
    apps/web/src/components/tag-selector.tsx,
    apps/web/src/components/lead-form.tsx,
    apps/web/src/app/leads/new/page.tsx
  </files>
  <read_first>
    apps/web/src/lib/lead/validation.ts,
    apps/web/src/lib/lead/save-lead.ts,
    apps/web/src/lib/supabase/client.ts,
    apps/web/src/components/login-card.tsx,
    .planning/phases/03-lead-capture/03-UI-SPEC.md
  </read_first>
  <action>
    1. Create `apps/web/src/components/tag-selector.tsx` (per D-14, D-15, D-16):
       - Props: value: "quente" | "morno" | "frio", onChange: (tag) => void
       - 3 Button components side-by-side with semantic colors from UI-SPEC:
         - Quente: oklch(0.936 0.032 17) bg / oklch(0.45 0.18 17) text (light), oklch(0.3 0.06 17) bg / oklch(0.85 0.12 17) text (dark)
         - Morno: oklch(0.945 0.04 85) bg / oklch(0.5 0.13 85) text (light), oklch(0.3 0.06 85) bg / oklch(0.85 0.1 85) text (dark)
         - Frio: oklch(0.94 0.03 240) bg / oklch(0.45 0.15 240) text (light), oklch(0.3 0.05 240) bg / oklch(0.85 0.1 240) text (dark)
       - Use `role="radiogroup"` on container, `role="radio"` + `aria-checked` on each button
       - Selected button gets full opacity, unselected get outline/ghost variant
       - Use inline styles with oklch for color values, or data-attributes with Tailwind

    2. Create `apps/web/src/components/lead-form.tsx` (per D-01 through D-05, CAPT-01, CAPT-02):
       - "use client" directive
       - State: form fields via useState (name, phone, email, interestTag defaulting to "morno", company, position, segment, notes), errors object, isSubmitting boolean, showDetails boolean (for collapsible)
       - Get userId from Supabase client: `const supabase = createClient(); const { data } = await supabase.auth.getUser()` — wrap in useEffect or use getSession
       - Header: ArrowLeft button (router.back()) + "Novo Lead" title (per UI-SPEC layout)
       - Card container max-w-[480px] w-full mx-auto
       - Visible fields: Nome (Input, required), Telefone (Input + QR button placeholder disabled for now), Email (Input)
       - TagSelector component for interestTag
       - Photo section: placeholder for now (will be wired in Plan 03)
       - Collapsible "Mais detalhes" section (per D-04): empresa, cargo, segmento (Input), notas (Textarea)
       - "Salvar Lead" button: full-width, variant default, size lg
       - On submit: validate with leadFormSchema.parse(), show inline errors per UI-SPEC (border destructive + message), focus first error field
       - On valid: call saveLead(data, userId, photo), show toast "Lead salvo!", router.back() (or router.push("/dashboard") if no history)
       - Loading state: button shows Loader spinner, fields readonly, aria-busy="true"
       - All copy from UI-SPEC copywriting contract
       - Accessibility: Labels with htmlFor, aria-describedby for errors, role="alert" on error messages

    3. Create `apps/web/src/app/leads/new/page.tsx`:
       - Server component wrapper
       - Import and render LeadForm
       - Minimal: just the component in a container with padding

    4. Run `bun run check` for Biome compliance.
    5. Run `bun run check-types` for TypeScript validation.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "tag-selector" apps/web/src/components/lead-form.tsx || grep -q "TagSelector" apps/web/src/components/lead-form.tsx
    - grep -q "saveLead" apps/web/src/components/lead-form.tsx
    - grep -q "leadFormSchema" apps/web/src/components/lead-form.tsx
    - grep -q "radiogroup" apps/web/src/components/tag-selector.tsx
    - grep -q "morno" apps/web/src/components/tag-selector.tsx
    - grep -q "quente" apps/web/src/components/tag-selector.tsx
    - grep -q "frio" apps/web/src/components/tag-selector.tsx
    - grep -q "Collapsible" apps/web/src/components/lead-form.tsx
    - grep -q "Mais detalhes" apps/web/src/components/lead-form.tsx
    - grep -q "Salvar Lead" apps/web/src/components/lead-form.tsx
    - grep -q "Lead salvo" apps/web/src/components/lead-form.tsx
    - grep -q "LeadForm" apps/web/src/app/leads/new/page.tsx
    - test -f apps/web/src/app/leads/new/page.tsx
  </acceptance_criteria>
  <done>
    /leads/new page renders LeadForm with validation, TagSelector with 3 semantic-colored buttons (default morno), collapsible optional fields, and offline-first save to Dexie. Toast feedback and navigation on success.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FAB component and wire to dashboard</name>
  <files>
    apps/web/src/components/fab.tsx,
    apps/web/src/app/dashboard/page.tsx
  </files>
  <read_first>
    apps/web/src/app/dashboard/page.tsx,
    .planning/phases/03-lead-capture/03-UI-SPEC.md
  </read_first>
  <action>
    1. Create `apps/web/src/components/fab.tsx` (per D-01, UI-SPEC FAB constraints):
       - Link component wrapping a Button
       - `fixed bottom-6 right-6 h-14 w-14 rounded-full shadow-lg z-50`
       - Variant: default (primary color)
       - Icon: Plus from lucide-react, 24px
       - `aria-label="Adicionar novo lead"` (per UI-SPEC accessibility)
       - `href="/leads/new"` via Next.js Link

    2. Add FAB to dashboard page:
       - Read existing dashboard/page.tsx
       - Import and add `<FAB />` at the end of the page component (it's fixed position, so placement in JSX doesn't affect layout)

    3. Run `bun run check` and `bun run check-types`.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fab" apps/web/src/app/dashboard/page.tsx || grep -q "FAB" apps/web/src/app/dashboard/page.tsx || grep -q "Fab" apps/web/src/app/dashboard/page.tsx
    - grep -q "/leads/new" apps/web/src/components/fab.tsx
    - grep -q "Plus" apps/web/src/components/fab.tsx
    - grep -q "Adicionar novo lead" apps/web/src/components/fab.tsx
    - grep -q "fixed" apps/web/src/components/fab.tsx
    - grep -q "rounded-full" apps/web/src/components/fab.tsx
  </acceptance_criteria>
  <done>
    FAB visible on dashboard with Plus icon, links to /leads/new. Fixed position bottom-right, primary color, accessible label.
  </done>
</task>

</tasks>

<verification>
TypeScript compiles without errors. Form renders at /leads/new. FAB visible on dashboard.

```bash
cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types && bun run check
```
</verification>

<success_criteria>
- /leads/new page renders lead capture form
- Form validates: name required, phone or email required, interestTag required
- TagSelector shows 3 colored buttons with morno pre-selected
- Optional fields hidden behind "Mais detalhes" collapsible
- Submit calls saveLead (Dexie offline-first)
- Toast "Lead salvo!" on success, redirect back
- FAB on dashboard links to /leads/new
- TypeScript and Biome clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-lead-capture/03-02-SUMMARY.md`
</output>
