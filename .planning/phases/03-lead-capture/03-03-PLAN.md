---
phase: 03-lead-capture
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/web/src/components/qr-scanner.tsx
  - apps/web/src/components/photo-capture.tsx
  - apps/web/src/components/lead-form.tsx
autonomous: true
requirements:
  - CAPT-03
  - CAPT-04
  - CAPT-05

must_haves:
  truths:
    - "Botao QR ao lado do campo telefone abre overlay com camera para scan"
    - "QR code wa.me detectado preenche automaticamente o campo telefone"
    - "Botao camera abre camera nativa e captura foto do cartao de visita"
    - "Foto comprimida (max 1280px, JPEG 0.7) antes de armazenar"
    - "Preview 80x80px da foto no form com botao para remover"
  artifacts:
    - path: "apps/web/src/components/qr-scanner.tsx"
      provides: "Full-screen QR scanner overlay with html5-qrcode"
      min_lines: 50
    - path: "apps/web/src/components/photo-capture.tsx"
      provides: "Photo capture via native camera + compression + preview"
      min_lines: 40
  key_links:
    - from: "apps/web/src/components/qr-scanner.tsx"
      to: "apps/web/src/lib/lead/wa-parser.ts"
      via: "parseWhatsAppUrl for QR result"
      pattern: "parseWhatsAppUrl"
    - from: "apps/web/src/components/photo-capture.tsx"
      to: "apps/web/src/lib/lead/compression.ts"
      via: "compressImage on captured file"
      pattern: "compressImage"
    - from: "apps/web/src/components/lead-form.tsx"
      to: "apps/web/src/components/qr-scanner.tsx"
      via: "QRScanner rendered inside form with onScan callback"
      pattern: "QRScanner|qr-scanner"
    - from: "apps/web/src/components/lead-form.tsx"
      to: "apps/web/src/components/photo-capture.tsx"
      via: "PhotoCapture rendered inside form with onCapture callback"
      pattern: "PhotoCapture|photo-capture"
---

<objective>
Build QR scanner overlay and photo capture components, then wire them into the existing LeadForm. QR scan auto-fills phone from WhatsApp URLs. Photo capture compresses and shows preview.

Purpose: Complete the 3 capture modes (manual, QR, photo) for the lead form. QR and photo are the differentiating features for congress use.
Output: QRScanner and PhotoCapture components integrated into LeadForm.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-lead-capture/03-CONTEXT.md
@.planning/phases/03-lead-capture/03-RESEARCH.md
@.planning/phases/03-lead-capture/03-UI-SPEC.md
@.planning/phases/03-lead-capture/03-02-SUMMARY.md

<interfaces>
<!-- Utility modules from Plan 01 -->

From apps/web/src/lib/lead/wa-parser.ts:
```typescript
export function parseWhatsAppUrl(text: string): string | null;
```

From apps/web/src/lib/lead/compression.ts:
```typescript
export function compressImage(file: File): Promise<Blob>;
```

<!-- html5-qrcode API (installed in Plan 01) -->
```typescript
import { Html5Qrcode } from "html5-qrcode";
// new Html5Qrcode(elementId)
// scanner.start(facingMode, config, onSuccess, onError)
// scanner.stop()
// scanner.clear()
// scanner.isScanning
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QRScanner overlay component</name>
  <files>
    apps/web/src/components/qr-scanner.tsx
  </files>
  <read_first>
    apps/web/src/lib/lead/wa-parser.ts,
    .planning/phases/03-lead-capture/03-RESEARCH.md,
    .planning/phases/03-lead-capture/03-UI-SPEC.md
  </read_first>
  <action>
    1. Create `apps/web/src/components/qr-scanner.tsx` (per D-06, D-07, D-08):
       - "use client" directive
       - Props: open: boolean, onClose: () => void, onScan: (phone: string) => void
       - Full-screen overlay: `fixed inset-0 z-50 bg-background` (per UI-SPEC QR Scanner Overlay layout)
       - Header: X close button (Lucide X icon, `absolute top-4 left-4`), title "Escanear QR" centered
       - Camera viewfinder: div with id for html5-qrcode, `aspect-square max-w-[320px] mx-auto rounded-lg overflow-hidden`
       - Instruction text: "Aponte para o QR Code do WhatsApp" (body 14px, muted-foreground, centered)

    2. html5-qrcode integration (per Research Pattern 5):
       - useRef for Html5Qrcode instance
       - When `open` becomes true: create Html5Qrcode(elementId), call scanner.start({ facingMode: "environment" }, { fps: 10, qrbox: { width: 250, height: 250 } }, onSuccess, onError)
       - Guard against React Strict Mode double-invoke: check scannerRef.current?.isScanning before start (Pitfall 1)
       - onSuccess callback: parse with parseWhatsAppUrl(decodedText)
         - If phone found: call onScan(phone), stop scanner, call onClose, toast "Telefone detectado"
         - If not wa.me URL: toast.error("QR Code nao reconhecido. Tente novamente.") — overlay stays open
       - onError callback: no-op (normal during scanning — frames without QR)
       - Cleanup: useEffect return calls scanner.stop() + scanner.clear() (Pitfall 1)
       - Camera permission denied handling: catch error from start(), show toast "Permita o acesso a camera nas configuracoes do navegador." (per UI-SPEC states)

    3. Accessibility (per UI-SPEC):
       - Close button: `aria-label="Fechar"`
       - Focus trap: when overlay opens, focus the close button

    4. Run `bun run check` for Biome compliance.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Html5Qrcode" apps/web/src/components/qr-scanner.tsx
    - grep -q "parseWhatsAppUrl" apps/web/src/components/qr-scanner.tsx
    - grep -q "Escanear QR" apps/web/src/components/qr-scanner.tsx
    - grep -q "facingMode" apps/web/src/components/qr-scanner.tsx
    - grep -q "isScanning" apps/web/src/components/qr-scanner.tsx
    - grep -q "onScan\|onClose" apps/web/src/components/qr-scanner.tsx
    - grep -q "Telefone detectado" apps/web/src/components/qr-scanner.tsx
  </acceptance_criteria>
  <done>
    QR scanner overlay opens with camera, scans wa.me QR codes, parses phone number, closes and fills phone field. Handles camera permission denied. Proper cleanup on unmount.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PhotoCapture component and wire QR + Photo into LeadForm</name>
  <files>
    apps/web/src/components/photo-capture.tsx,
    apps/web/src/components/lead-form.tsx
  </files>
  <read_first>
    apps/web/src/lib/lead/compression.ts,
    apps/web/src/components/lead-form.tsx,
    apps/web/src/components/qr-scanner.tsx,
    .planning/phases/03-lead-capture/03-UI-SPEC.md
  </read_first>
  <action>
    1. Create `apps/web/src/components/photo-capture.tsx` (per D-09, D-10, D-11, D-12):
       - "use client" directive
       - Props: photo: Blob | null, onCapture: (blob: Blob) => void, onRemove: () => void
       - Hidden `<input type="file" accept="image/*" capture="environment">` (per D-09, anti-pattern: do NOT use getUserMedia)
       - "Tirar foto" button (Camera icon from Lucide) triggers the hidden input via ref.click()
       - On file selected: call compressImage(file) from compression.ts, then onCapture(compressedBlob)
       - Preview: if photo exists, show 80x80px thumbnail (per UI-SPEC: `w-20 h-20 object-cover rounded`) using URL.createObjectURL(photo)
       - Remove button: X icon on corner of preview, calls onRemove, revokes object URL
       - Loading state while compressing: show Loader spinner on the preview area
       - Accessibility: `aria-label="Tirar foto do cartao de visita"` on capture button, `aria-label="Remover foto"` on remove, `alt="Preview da foto capturada"` on img

    2. Wire QRScanner into LeadForm (per D-07):
       - Add state: showQRScanner (boolean, default false)
       - Add QR icon button (QrCode from Lucide) next to the phone Input field
       - Button `aria-label="Escanear QR Code do WhatsApp"` (per UI-SPEC)
       - onClick: setShowQRScanner(true)
       - Render QRScanner overlay when showQRScanner is true
       - onScan callback: setPhone(phone), setShowQRScanner(false)
       - onClose: setShowQRScanner(false)

    3. Wire PhotoCapture into LeadForm (per D-09, D-10, D-12):
       - Add state: photo (Blob | null, default null)
       - Render PhotoCapture component between tag selector and "Mais detalhes" (per UI-SPEC layout)
       - onCapture: setPhoto(blob)
       - onRemove: setPhoto(null)
       - Pass photo to saveLead(data, userId, photo) on submit

    4. Ensure the form tab order matches UI-SPEC: Nome -> Telefone -> QR btn -> Email -> Tags -> Camera btn -> Mais detalhes -> optional fields -> Salvar

    5. Run `bun run check` and `bun run check-types`.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "compressImage" apps/web/src/components/photo-capture.tsx
    - grep -q 'capture="environment"' apps/web/src/components/photo-capture.tsx || grep -q "capture" apps/web/src/components/photo-capture.tsx
    - grep -q "accept=\"image" apps/web/src/components/photo-capture.tsx || grep -q "image/\*" apps/web/src/components/photo-capture.tsx
    - grep -q "Remover foto" apps/web/src/components/photo-capture.tsx
    - grep -q "QRScanner\|QrScanner\|qr-scanner" apps/web/src/components/lead-form.tsx
    - grep -q "PhotoCapture\|photo-capture" apps/web/src/components/lead-form.tsx
    - grep -q "showQRScanner\|qrOpen\|scannerOpen" apps/web/src/components/lead-form.tsx
  </acceptance_criteria>
  <done>
    QR scanner button next to phone field opens overlay, auto-fills phone from wa.me QR. Photo capture button opens native camera, compresses image, shows 80x80 preview with remove option. Both wired into LeadForm. Form passes photo to saveLead.
  </done>
</task>

</tasks>

<verification>
TypeScript compiles. QR and photo components render in form. No network calls on form path.

```bash
cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types && bun run check
```
</verification>

<success_criteria>
- QR button next to phone field opens full-screen scanner overlay
- Scanner uses html5-qrcode with environment camera
- wa.me URL parsed and phone auto-filled, overlay closes
- Non-wa.me QR shows error toast, overlay stays open
- Camera button opens native camera via input[type=file]
- Photo compressed to max 1280px JPEG 0.7
- Preview 80x80px shown with remove button
- Photo passed to saveLead on submit
- TypeScript and Biome clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-lead-capture/03-03-SUMMARY.md`
</output>
