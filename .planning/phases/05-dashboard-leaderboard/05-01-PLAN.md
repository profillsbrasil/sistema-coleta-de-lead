---
phase: 05-dashboard-leaderboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/db/types.ts
  - apps/web/src/lib/db/index.ts
  - apps/web/src/lib/lead/stats.ts
  - apps/web/src/lib/lead/stats.test.ts
  - packages/api/src/routers/leaderboard.ts
  - packages/api/src/routers/index.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "getPersonalStats returns total, quente, morno, frio counts, hoje count, and weighted score"
    - "Score calculation uses quente=3, morno=2, frio=1 formula"
    - "Leads de hoje uses local timezone comparison, not ISO string comparison"
    - "Leaderboard tRPC procedure returns ranking ordered by score desc, totalLeads desc"
    - "LeaderboardEntry type exists in Dexie types for offline cache"
    - "Dexie version 2 adds leaderboardCache table without losing existing data"
  artifacts:
    - path: "apps/web/src/lib/lead/stats.ts"
      provides: "PersonalStats interface and getPersonalStats function"
      exports: ["PersonalStats", "getPersonalStats"]
    - path: "apps/web/src/lib/lead/stats.test.ts"
      provides: "Unit tests for stats calculation"
      min_lines: 50
    - path: "packages/api/src/routers/leaderboard.ts"
      provides: "leaderboardRouter with getRanking procedure"
      exports: ["leaderboardRouter"]
    - path: "apps/web/src/lib/db/types.ts"
      provides: "LeaderboardEntry interface"
      contains: "LeaderboardEntry"
    - path: "apps/web/src/lib/db/index.ts"
      provides: "Dexie version 2 with leaderboardCache table"
      contains: "leaderboardCache"
  key_links:
    - from: "packages/api/src/routers/leaderboard.ts"
      to: "packages/api/src/routers/index.ts"
      via: "router registration"
      pattern: "leaderboard: leaderboardRouter"
    - from: "apps/web/src/lib/lead/stats.ts"
      to: "apps/web/src/lib/db/index.ts"
      via: "Dexie query"
      pattern: "db\\.leads"
---

<objective>
Build the complete data layer for Phase 5: Dexie schema update (leaderboardCache table), personal stats calculation functions with tests, and leaderboard tRPC procedure with SQL aggregation.

Purpose: All UI components in Plans 02-03 depend on these data functions and types. Building data layer first ensures UI plans have concrete contracts.
Output: Tested stats module, leaderboard tRPC procedure, updated Dexie schema with LeaderboardEntry type.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-leaderboard/05-CONTEXT.md
@.planning/phases/05-dashboard-leaderboard/05-RESEARCH.md

<interfaces>
<!-- Existing types and modules the executor needs -->

From apps/web/src/lib/db/types.ts:
```typescript
export interface Lead {
  company: string | null;
  createdAt: string;
  deletedAt: string | null;
  email: string | null;
  interestTag: "quente" | "morno" | "frio";
  localId: string;
  name: string;
  notes: string | null;
  phone: string | null;
  photo: Blob | null;
  position: string | null;
  segment: string | null;
  serverId: number | null;
  syncStatus: "pending" | "synced" | "conflict";
  updatedAt: string;
  userId: string;
}

export interface SyncQueueItem {
  id?: number;
  localId: string;
  operation: "create" | "update" | "delete";
  payload: string;
  retryCount: number;
  timestamp: string;
}
```

From apps/web/src/lib/db/index.ts:
```typescript
const db = new Dexie("dashboard-leads") as Dexie & {
  leads: EntityTable<Lead, "localId">;
  syncQueue: EntityTable<SyncQueueItem, "id">;
};
db.version(1).stores({
  leads: "localId, serverId, userId, interestTag, syncStatus, createdAt, updatedAt",
  syncQueue: "++id, localId, operation, timestamp",
});
export { db };
```

From packages/api/src/index.ts:
```typescript
export const protectedProcedure = t.procedure.use(({ ctx, next }) => { ... });
export const router = t.router;
```

From packages/api/src/routers/index.ts:
```typescript
export const appRouter = router({
  healthCheck: publicProcedure.query(...),
  privateData: protectedProcedure.query(...),
  sync: syncRouter,
  todo: todoRouter,
});
export type AppRouter = typeof appRouter;
```

From packages/db/src/schema/leads.ts:
```typescript
export const interestTagEnum = pgEnum("interest_tag", ["quente", "morno", "frio"]);
export const leads = pgTable("leads", {
  id: bigserial("id", { mode: "bigint" }).primaryKey(),
  localId: uuid("local_id").notNull().unique(),
  userId: uuid("user_id").notNull(),
  name: text("name").notNull(),
  interestTag: interestTagEnum("interest_tag").notNull(),
  deletedAt: timestamp("deleted_at", { withTimezone: true }),
  // ... other fields
});
```

From apps/web/vitest.config.ts:
```typescript
// environment: jsdom, setupFiles: fake-indexeddb/auto, alias: @/ -> src/
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dexie types, schema update, and personal stats with TDD</name>
  <files>
    apps/web/src/lib/db/types.ts,
    apps/web/src/lib/db/index.ts,
    apps/web/src/lib/lead/stats.ts,
    apps/web/src/lib/lead/stats.test.ts
  </files>
  <read_first>
    apps/web/src/lib/db/types.ts,
    apps/web/src/lib/db/index.ts,
    apps/web/src/lib/lead/queries.ts,
    apps/web/vitest.config.ts
  </read_first>
  <behavior>
    - Test 1: getPersonalStats returns total count of non-deleted leads for given userId
    - Test 2: getPersonalStats returns correct breakdown (quente, morno, frio counts)
    - Test 3: getPersonalStats returns weighted score (quente*3 + morno*2 + frio*1)
    - Test 4: getPersonalStats returns count of leads created today (local timezone)
    - Test 5: getPersonalStats excludes soft-deleted leads (deletedAt !== null)
    - Test 6: getPersonalStats returns all zeros for user with no leads
    - Test 7: getPersonalStats only counts leads belonging to the given userId
  </behavior>
  <action>
    **Step 1: Add LeaderboardEntry interface to types.ts** (per D-09)

    Add this interface after SyncQueueItem in `apps/web/src/lib/db/types.ts`:
    ```typescript
    export interface LeaderboardEntry {
      userId: string;
      name: string;
      totalLeads: number;
      score: number;
      rank: number;
      lastSyncAt: string;
    }
    ```

    **Step 2: Update Dexie schema to version 2** (per D-09)

    In `apps/web/src/lib/db/index.ts`:
    - Import `LeaderboardEntry` in the type import
    - Add `leaderboardCache: EntityTable<LeaderboardEntry, "userId">` to the Dexie type cast
    - Keep version(1) exactly as-is (CRITICAL: do not remove or modify)
    - Add `db.version(2).stores(...)` that repeats ALL version(1) stores plus adds `leaderboardCache: "userId"`
    - Export `LeaderboardEntry` type

    Final db type cast:
    ```typescript
    const db = new Dexie("dashboard-leads") as Dexie & {
      leads: EntityTable<Lead, "localId">;
      syncQueue: EntityTable<SyncQueueItem, "id">;
      leaderboardCache: EntityTable<LeaderboardEntry, "userId">;
    };
    ```

    Version 2 stores block:
    ```typescript
    db.version(2).stores({
      leads: "localId, serverId, userId, interestTag, syncStatus, createdAt, updatedAt",
      syncQueue: "++id, localId, operation, timestamp",
      leaderboardCache: "userId",
    });
    ```

    **Step 3: Write failing tests for getPersonalStats** (RED phase)

    Create `apps/web/src/lib/lead/stats.test.ts`. Import `db` from `@/lib/db/index` and `getPersonalStats` from `@/lib/lead/stats`. Use `beforeEach` to clear `db.leads` table. Create helper to insert test leads:

    ```typescript
    function makeLead(overrides: Partial<Lead> = {}): Lead {
      return {
        localId: crypto.randomUUID(),
        serverId: null,
        userId: "user-1",
        name: "Test Lead",
        phone: "+5511999999999",
        email: null,
        company: null,
        position: null,
        segment: null,
        notes: null,
        interestTag: "morno",
        photo: null,
        createdAt: new Date().toISOString(),
        updatedAt: new Date().toISOString(),
        deletedAt: null,
        syncStatus: "synced",
        ...overrides,
      };
    }
    ```

    Tests:
    1. "returns total count of non-deleted leads" -- insert 3 leads for user-1, assert total === 3
    2. "returns correct tag breakdown" -- insert 2 quente, 1 morno, 3 frio for user-1, assert quente===2, morno===1, frio===3
    3. "calculates weighted score" -- same data, assert score === 2*3 + 1*2 + 3*1 === 11
    4. "counts leads created today" -- insert 2 leads with today's date, 1 with yesterday, assert hoje === 2. For "yesterday", use `new Date(Date.now() - 86400000).toISOString()`. For "today", use `new Date().toISOString()`.
    5. "excludes soft-deleted leads" -- insert 2 leads, 1 with deletedAt set, assert total === 1
    6. "returns zeros for user with no leads" -- call with "empty-user", assert all fields === 0
    7. "only counts leads for given userId" -- insert 2 leads for user-1 and 3 for user-2, call with user-1, assert total === 2

    **Step 4: Implement getPersonalStats** (GREEN phase)

    Create `apps/web/src/lib/lead/stats.ts`:

    ```typescript
    import { db } from "../db/index";

    export interface PersonalStats {
      total: number;
      quente: number;
      morno: number;
      frio: number;
      hoje: number;
      score: number;
    }

    export async function getPersonalStats(userId: string): Promise<PersonalStats> {
      const leads = await db.leads
        .where("userId")
        .equals(userId)
        .filter((lead) => lead.deletedAt === null)
        .toArray();

      const startOfToday = new Date();
      startOfToday.setHours(0, 0, 0, 0);

      const quente = leads.filter((l) => l.interestTag === "quente").length;
      const morno = leads.filter((l) => l.interestTag === "morno").length;
      const frio = leads.filter((l) => l.interestTag === "frio").length;

      return {
        total: leads.length,
        quente,
        morno,
        frio,
        hoje: leads.filter((l) => new Date(l.createdAt) >= startOfToday).length,
        score: quente * 3 + morno * 2 + frio * 1,
      };
    }
    ```

    IMPORTANT per Pitfall 5 from RESEARCH.md: Compare `new Date(l.createdAt) >= startOfToday` using Date objects, NOT ISO string comparison. This ensures local timezone correctness.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun vitest run apps/web/src/lib/lead/stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/lib/db/types.ts contains `export interface LeaderboardEntry`
    - apps/web/src/lib/db/types.ts LeaderboardEntry has fields: userId, name, totalLeads, score, rank, lastSyncAt
    - apps/web/src/lib/db/index.ts contains `db.version(2).stores(`
    - apps/web/src/lib/db/index.ts contains `leaderboardCache: "userId"`
    - apps/web/src/lib/db/index.ts version(1) is preserved unchanged
    - apps/web/src/lib/lead/stats.ts contains `export interface PersonalStats`
    - apps/web/src/lib/lead/stats.ts contains `export async function getPersonalStats`
    - apps/web/src/lib/lead/stats.ts uses `new Date(l.createdAt) >= startOfToday` (Date comparison, not string)
    - apps/web/src/lib/lead/stats.ts contains `quente * 3 + morno * 2 + frio * 1`
    - apps/web/src/lib/lead/stats.test.ts has at least 6 test cases
    - `bun vitest run apps/web/src/lib/lead/stats.test.ts` exits 0
  </acceptance_criteria>
  <done>All 7 stats tests pass. LeaderboardEntry type exported. Dexie schema at version 2 with leaderboardCache table. PersonalStats correctly calculates total, breakdown, hoje (timezone-safe), and weighted score.</done>
</task>

<task type="auto">
  <name>Task 2: Leaderboard tRPC procedure with SQL aggregation</name>
  <files>
    packages/api/src/routers/leaderboard.ts,
    packages/api/src/routers/index.ts
  </files>
  <read_first>
    packages/api/src/routers/index.ts,
    packages/api/src/routers/sync.ts,
    packages/api/src/index.ts,
    packages/db/src/schema/leads.ts
  </read_first>
  <action>
    **Step 1: Create leaderboard router** (per D-04, D-05, D-07, D-08)

    Create `packages/api/src/routers/leaderboard.ts`:

    ```typescript
    import { sql } from "drizzle-orm";
    import { protectedProcedure, router } from "../index";

    export const leaderboardRouter = router({
      getRanking: protectedProcedure.query(async ({ ctx }) => {
        const result = await ctx.db.execute(sql`
          SELECT
            l.user_id AS "userId",
            COALESCE(u.raw_user_meta_data->>'full_name', u.email, 'Unknown') AS name,
            COUNT(*)::int AS "totalLeads",
            SUM(CASE
              WHEN l.interest_tag = 'quente' THEN 3
              WHEN l.interest_tag = 'morno' THEN 2
              ELSE 1
            END)::int AS score
          FROM leads l
          LEFT JOIN auth.users u ON u.id = l.user_id
          WHERE l.deleted_at IS NULL
          GROUP BY l.user_id, u.raw_user_meta_data, u.email
          ORDER BY score DESC, "totalLeads" DESC
        `);

        return {
          ranking: result.rows as Array<{
            userId: string;
            name: string;
            totalLeads: number;
            score: number;
          }>,
          serverTimestamp: new Date().toISOString(),
        };
      }),
    });
    ```

    Notes:
    - Uses raw SQL via Drizzle `sql` template tag (NOT string interpolation) per security rules
    - JOINs `auth.users` for vendor name (per Pitfall 2 from RESEARCH.md, pragmatic approach for ~10 users)
    - Uses `COALESCE` to fallback: full_name -> email -> "Unknown"
    - Ordering: score DESC then totalLeads DESC (per D-07)
    - Uses `protectedProcedure` -- only authenticated users can query
    - Accesses `ctx.db` -- check actual db import pattern in this package

    IMPORTANT: Verify how `db` is accessed in this package. The sync router imports `db` from `@dashboard-leads-profills/db`. Follow the same pattern. If `ctx` does not have `db`, import directly:
    ```typescript
    import { db } from "@dashboard-leads-profills/db";
    ```
    And use `db.execute(sql`...`)` instead of `ctx.db.execute(...)`.

    **Step 2: Register leaderboard router in appRouter**

    In `packages/api/src/routers/index.ts`:
    - Add import: `import { leaderboardRouter } from "./leaderboard";`
    - Add to appRouter: `leaderboard: leaderboardRouter,`

    Final appRouter should be:
    ```typescript
    export const appRouter = router({
      healthCheck: publicProcedure.query(...),
      privateData: protectedProcedure.query(...),
      sync: syncRouter,
      todo: todoRouter,
      leaderboard: leaderboardRouter,
    });
    ```
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types</automated>
  </verify>
  <acceptance_criteria>
    - packages/api/src/routers/leaderboard.ts exists
    - packages/api/src/routers/leaderboard.ts contains `export const leaderboardRouter`
    - packages/api/src/routers/leaderboard.ts contains `getRanking: protectedProcedure.query`
    - packages/api/src/routers/leaderboard.ts contains `auth.users` (cross-schema JOIN)
    - packages/api/src/routers/leaderboard.ts contains `raw_user_meta_data->>'full_name'`
    - packages/api/src/routers/leaderboard.ts contains `ORDER BY score DESC`
    - packages/api/src/routers/leaderboard.ts does NOT contain string interpolation in SQL
    - packages/api/src/routers/index.ts contains `leaderboard: leaderboardRouter`
    - packages/api/src/routers/index.ts contains `import { leaderboardRouter } from "./leaderboard"`
    - `bun run check-types` exits 0
  </acceptance_criteria>
  <done>Leaderboard tRPC procedure registered and type-checks. Returns ranking with userId, name, totalLeads, score ordered by score desc then totalLeads desc. Uses raw SQL with auth.users JOIN for vendor names.</done>
</task>

</tasks>

<verification>
- `bun vitest run apps/web/src/lib/lead/stats.test.ts` -- all stats tests pass
- `bun run check-types` -- no type errors across monorepo
- `bun run test` -- full test suite green
</verification>

<success_criteria>
- PersonalStats function tested and working with 7 test cases
- LeaderboardEntry type and Dexie version 2 schema ready
- Leaderboard tRPC procedure registered and type-safe
- Score formula: quente=3, morno=2, frio=1 in both client stats and server SQL
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-leaderboard/05-01-SUMMARY.md`
</output>
