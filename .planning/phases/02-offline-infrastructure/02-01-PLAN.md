---
phase: 02-offline-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/leads.ts
  - packages/db/src/schema/index.ts
  - apps/web/src/lib/db/index.ts
  - apps/web/src/lib/db/types.ts
  - apps/web/vitest.config.ts
  - vitest.workspace.ts
  - apps/web/package.json
autonomous: true
requirements: [OFFL-01, OFFL-02]
must_haves:
  truths:
    - "Drizzle leads table exists with all required fields (name, phone, email, company, position, segment, notes, interestTag, photoUrl, soft-delete, timestamps, local_id UUID, bigserial PK)"
    - "Dexie database has leads and syncQueue tables with correct schema and TypeScript types"
    - "Vitest can run tests in apps/web with fake-indexeddb"
  artifacts:
    - path: "packages/db/src/schema/leads.ts"
      provides: "Drizzle leads table + interestTag enum"
      exports: ["leads", "interestTagEnum"]
    - path: "apps/web/src/lib/db/types.ts"
      provides: "Shared Lead and SyncQueueItem TypeScript interfaces"
      exports: ["Lead", "SyncQueueItem"]
    - path: "apps/web/src/lib/db/index.ts"
      provides: "Dexie database instance with leads and syncQueue tables"
      exports: ["db", "Lead", "SyncQueueItem"]
    - path: "apps/web/vitest.config.ts"
      provides: "Vitest config for apps/web with jsdom + fake-indexeddb"
  key_links:
    - from: "packages/db/src/schema/index.ts"
      to: "packages/db/src/schema/leads.ts"
      via: "re-export"
      pattern: "export.*from.*leads"
    - from: "apps/web/src/lib/db/index.ts"
      to: "apps/web/src/lib/db/types.ts"
      via: "import types"
      pattern: "import.*from.*types"
---

<objective>
Create the data foundation for offline-first: Drizzle leads schema (PostgreSQL) and Dexie database (IndexedDB), plus test infrastructure for apps/web.

Purpose: All subsequent plans (sync engine, tRPC procedures) depend on these schemas and types being defined first.
Output: Drizzle leads table, Dexie DB with leads + syncQueue, shared TypeScript types, Vitest config for apps/web.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-offline-infrastructure/02-CONTEXT.md
@.planning/phases/02-offline-infrastructure/02-RESEARCH.md

<interfaces>
<!-- Existing Drizzle schema pattern from packages/db/src/schema/auth.ts -->
From packages/db/src/schema/auth.ts:
```typescript
import { bigserial, pgEnum, pgTable, unique, uuid } from "drizzle-orm/pg-core";

export const appRoleEnum = pgEnum("app_role", ["admin", "vendedor"]);

export const userRoles = pgTable(
	"user_roles",
	{
		id: bigserial("id", { mode: "bigint" }).primaryKey(),
		userId: uuid("user_id").notNull(),
		role: appRoleEnum("role").notNull(),
	},
	(table) => [
		unique("user_roles_user_id_role_unique").on(table.userId, table.role),
	]
);
```

From packages/db/src/schema/index.ts:
```typescript
export * from "./auth";
export * from "./todo";
```

From packages/db/src/index.ts:
```typescript
import { env } from "@dashboard-leads-profills/env/server";
import { drizzle } from "drizzle-orm/node-postgres";
import * as schema from "./schema";
export const db = drizzle(env.DATABASE_URL, { schema });
```

From vitest.workspace.ts:
```typescript
import { defineWorkspace } from "vitest/config";
export default defineWorkspace(["packages/api", "packages/env"]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drizzle leads schema + Dexie DB + shared types</name>
  <files>packages/db/src/schema/leads.ts, packages/db/src/schema/index.ts, apps/web/src/lib/db/types.ts, apps/web/src/lib/db/index.ts</files>
  <read_first>packages/db/src/schema/auth.ts, packages/db/src/schema/index.ts, packages/db/src/index.ts, apps/web/package.json</read_first>
  <behavior>
    - Drizzle: leads table has bigserial PK, uuid local_id (unique, notNull), uuid user_id (notNull), text name (notNull), text phone, text email, text company, text position, text segment, text notes, interestTag enum (notNull), text photo_url, timestamp created_at (default now), timestamp updated_at (default now, $onUpdate), timestamp deleted_at (nullable). Indices on user_id, interest_tag, updated_at.
    - Dexie types: Lead interface has localId (string), serverId (number | null), name, phone, email, company, position, segment, notes (all string | null except name), interestTag ("quente" | "morno" | "frio"), photo (Blob | null), createdAt/updatedAt (string ISO), deletedAt (string | null), syncStatus ("pending" | "synced" | "conflict"), userId (string).
    - Dexie types: SyncQueueItem has id (number, auto), localId (string), operation ("create" | "update" | "delete"), timestamp (string), payload (string JSON), retryCount (number).
    - Dexie DB: version(1) with leads table indexed on "localId, serverId, userId, interestTag, syncStatus, createdAt, updatedAt" and syncQueue on "++id, localId, operation, timestamp".
  </behavior>
  <action>
    1. Create `packages/db/src/schema/leads.ts` per D-01, D-03, D-04:
       - `interestTagEnum = pgEnum("interest_tag", ["quente", "morno", "frio"])` (per D-01)
       - `leads = pgTable("leads", { ... })` with:
         - `id: bigserial("id", { mode: "bigint" }).primaryKey()` (server_id, per D-03)
         - `localId: uuid("local_id").notNull().unique()` (client UUID, per D-03)
         - `userId: uuid("user_id").notNull()` (Supabase user UUID)
         - `name: text("name").notNull()` (per D-01)
         - `phone: text("phone")` (nullable, per D-02: at least one of phone/email)
         - `email: text("email")` (nullable, per D-02)
         - `company: text("company")`, `position: text("position")`, `segment: text("segment")`, `notes: text("notes")` (all nullable, per D-01)
         - `interestTag: interestTagEnum("interest_tag").notNull()` (per D-01)
         - `photoUrl: text("photo_url")` (nullable; photo sync in Phase 3)
         - `createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()` (per D-04)
         - `updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow().$onUpdate(() => new Date())` (per D-04)
         - `deletedAt: timestamp("deleted_at", { withTimezone: true })` (soft-delete, per D-04)
       - Indices: `index("leads_user_id_idx").on(table.userId)`, `index("leads_interest_tag_idx").on(table.interestTag)`, `index("leads_updated_at_idx").on(table.updatedAt)`
    2. Update `packages/db/src/schema/index.ts` to add `export * from "./leads";`
    3. Create `apps/web/src/lib/db/types.ts` with Lead and SyncQueueItem interfaces matching Dexie schema. These types are the shared contract between Dexie and sync engine.
    4. Create `apps/web/src/lib/db/index.ts`:
       - `import Dexie, { type EntityTable } from "dexie";`
       - `import type { Lead, SyncQueueItem } from "./types";`
       - Create database named "dashboard-leads" with version(1)
       - leads: primary key on localId, indices: serverId, userId, interestTag, syncStatus, createdAt, updatedAt
       - syncQueue: auto-increment id, indices: localId, operation, timestamp
       - Export `db`, re-export types from types.ts
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types</automated>
  </verify>
  <acceptance_criteria>
    - grep "interestTagEnum" packages/db/src/schema/leads.ts returns pgEnum definition
    - grep "leads.*pgTable" packages/db/src/schema/leads.ts returns table definition
    - grep "localId.*uuid" packages/db/src/schema/leads.ts returns local_id column
    - grep "deletedAt.*timestamp" packages/db/src/schema/leads.ts returns soft-delete column
    - grep "leads" packages/db/src/schema/index.ts confirms re-export
    - grep "EntityTable" apps/web/src/lib/db/index.ts confirms Dexie typed tables
    - grep "syncQueue" apps/web/src/lib/db/index.ts confirms sync queue table
    - grep "syncStatus" apps/web/src/lib/db/types.ts confirms sync tracking field
  </acceptance_criteria>
  <done>Drizzle leads table defined with all fields per D-01/D-02/D-03/D-04. Dexie DB configured with leads and syncQueue tables. Shared TypeScript types exported. Type check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Test infrastructure for apps/web (Vitest + fake-indexeddb)</name>
  <files>apps/web/vitest.config.ts, apps/web/package.json, vitest.workspace.ts</files>
  <read_first>vitest.workspace.ts, apps/web/package.json, packages/api/vitest.config.ts</read_first>
  <action>
    1. Install fake-indexeddb as devDependency in apps/web: `bun add -D fake-indexeddb --filter web`
    2. Create `apps/web/vitest.config.ts`:
       ```typescript
       import { defineConfig } from "vitest/config";
       import path from "node:path";

       export default defineConfig({
         test: {
           environment: "jsdom",
           setupFiles: ["fake-indexeddb/auto"],
           include: ["src/**/*.test.ts"],
           alias: {
             "@": path.resolve(__dirname, "src"),
           },
         },
       });
       ```
    3. Update `vitest.workspace.ts` to include `"apps/web"`:
       ```typescript
       import { defineWorkspace } from "vitest/config";
       export default defineWorkspace(["packages/api", "packages/env", "apps/web"]);
       ```
    4. Verify setup by running `bun run test` and confirming no errors from the new workspace entry.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - grep "fake-indexeddb" apps/web/package.json confirms devDependency installed
    - grep "jsdom" apps/web/vitest.config.ts confirms test environment
    - grep "fake-indexeddb/auto" apps/web/vitest.config.ts confirms IndexedDB setup
    - grep "apps/web" vitest.workspace.ts confirms workspace inclusion
  </acceptance_criteria>
  <done>Vitest configured for apps/web with jsdom environment and fake-indexeddb. Workspace updated. `bun run test` passes with no errors.</done>
</task>

</tasks>

<verification>
- `bun run check-types` passes (Drizzle schema + Dexie types compile)
- `bun run test` passes (test infrastructure working)
- Drizzle leads schema matches all fields from D-01, D-03, D-04
- Dexie DB has leads + syncQueue tables with correct indices
</verification>

<success_criteria>
- Drizzle leads table with bigserial PK, UUID local_id, all fields per D-01, soft-delete per D-04
- Dexie database with leads and syncQueue EntityTables
- Shared Lead and SyncQueueItem TypeScript interfaces
- Vitest runs in apps/web with fake-indexeddb support
- All type checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-infrastructure/02-01-SUMMARY.md`
</output>
