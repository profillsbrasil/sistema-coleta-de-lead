---
phase: 09-sidebar-content-mobile-ux
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: []
autonomous: false
requirements:
  - MOBILE-01
  - MOBILE-02
  - MOBILE-04
  - TOUCH-01
  - TOUCH-04
  - LAYOUT-04
  - LAYOUT-08
  - POLISH-02

must_haves:
  truths:
    - "Vendedor logado ve grupo Vendedor na sidebar e NAO ve grupo Admin"
    - "Admin logado ve ambos os grupos Vendedor e Admin"
    - "No mobile, drawer fecha automaticamente apos clicar em nav link"
    - "Footer exibe avatar + nome + role + ModeToggle + botao Sair funcional"
    - "Logout redireciona para /login"
    - "Nav item ativo esta highlighted na rota atual"
    - "Touch targets dos nav items sao visivelmente maiores que o padrao"
    - "ModeToggle alterna entre light e dark mode corretamente"
  artifacts:
    - path: "apps/web/src/lib/gravatar.ts"
      provides: "Gravatar URL generator using SHA-256 hash"
      created_by: "09-01"
    - path: "apps/web/src/components/sidebar-user-menu.tsx"
      provides: "User menu component with ModeToggle for SidebarFooter"
      created_by: "09-01"
    - path: "apps/web/src/components/app-sidebar.tsx"
      provides: "AppSidebar with user menu, ModeToggle, auto-close, touch targets, active state"
      created_by: "09-01"
    - path: "apps/web/src/app/(app)/layout.tsx"
      provides: "App layout passing user data props to AppSidebar"
      created_by: "09-01"
  key_links:
    - from: "apps/web/src/app/(app)/layout.tsx"
      to: "apps/web/src/components/app-sidebar.tsx"
      via: "userName, userEmail, userRole, gravatarUrl props"
      pattern: "userName=|userEmail=|userRole=|gravatarUrl="
    - from: "apps/web/src/components/app-sidebar.tsx"
      to: "apps/web/src/components/sidebar-user-menu.tsx"
      via: "SidebarUserMenu rendered inside SidebarFooter"
      pattern: "SidebarUserMenu"
    - from: "apps/web/src/components/sidebar-user-menu.tsx"
      to: "next-themes useTheme()"
      via: "ModeToggle icon button toggles theme"
      pattern: "useTheme|setTheme"
    - from: "apps/web/src/components/app-sidebar.tsx"
      to: "useSidebar().setOpenMobile(false)"
      via: "useEffect on pathname change"
      pattern: "setOpenMobile\\(false\\)"
---

<objective>
Verificacao visual e funcional da sidebar completa: navegacao por role, drawer mobile auto-close, user menu no footer (com ModeToggle), touch targets, e active state.

Purpose: Confirmar que todas as funcionalidades implementadas no Plan 01 funcionam corretamente no browser, especialmente o comportamento mobile e ModeToggle que nao podem ser verificados apenas por type-check.
Output: Aprovacao humana ou lista de issues para correcao.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-sidebar-content-mobile-ux/09-01-SUMMARY.md
@.planning/phases/09-sidebar-content-mobile-ux/09-CONTEXT.md
@.planning/phases/09-sidebar-content-mobile-ux/09-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build e verificacao automatizada pre-checkpoint</name>
  <files></files>
  <read_first>
    - apps/web/src/components/app-sidebar.tsx
    - apps/web/src/components/sidebar-user-menu.tsx
    - apps/web/src/app/(app)/layout.tsx
    - apps/web/src/lib/gravatar.ts
  </read_first>
  <action>
Rodar suite completa de verificacao automatizada antes do checkpoint humano:

1. `bun run check-types` — zero type errors
2. `bun run check` — Biome linting/formatting OK
3. `bun run build` — Next.js build completa sem erro
4. `bun run test` — testes existentes passam

Verificacoes grep (estruturais):
- `grep -r "SidebarProvider" apps/web/src --include="*.tsx" -l` retorna APENAS `apps/web/src/app/(app)/layout.tsx` (single provider, per LAYOUT-03)
- `grep -c "min-h-11" apps/web/src/components/app-sidebar.tsx` retorna >= 2 (touch targets em ambos loops: VENDEDOR e ADMIN)
- `grep "setOpenMobile(false)" apps/web/src/components/app-sidebar.tsx` retorna match (auto-close mobile)
- `grep "getGravatarUrl" apps/web/src/app/\(app\)/layout.tsx` retorna match (server-side Gravatar computation)
- `grep "DropdownMenu" apps/web/src/components/sidebar-user-menu.tsx` retorna ZERO matches (per D-03)
- `grep "useTheme" apps/web/src/components/sidebar-user-menu.tsx` retorna match (ModeToggle per LAYOUT-08)
- `grep 'collapsible="offcanvas"' apps/web/src/components/app-sidebar.tsx` retorna match (per D-02)

Se qualquer verificacao falhar, corrigir antes do checkpoint.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types && bun run check && bun run build 2>&1 | tail -5 && bun run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `bun run check-types` exits 0
    - `bun run check` exits 0
    - `bun run build` exits 0
    - `bun run test` exits 0
    - grep for SidebarProvider in .tsx files returns exactly 1 file (layout.tsx)
    - grep for "min-h-11" in app-sidebar.tsx returns >= 2 occurrences
    - grep for "setOpenMobile(false)" in app-sidebar.tsx returns >= 1 match
    - grep for "DropdownMenu" in sidebar-user-menu.tsx returns 0 matches
    - grep for "useTheme" in sidebar-user-menu.tsx returns >= 1 match
  </acceptance_criteria>
  <done>Build, type-check, linting e testes passam. Verificacoes grep confirmam estrutura correta: single SidebarProvider, touch targets, auto-close, ModeToggle via useTheme, sem DropdownMenu no user menu.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao visual e funcional no browser</name>
  <files></files>
  <action>
Checkpoint de verificacao visual. Executar dev server com `bun run dev:web` e seguir o checklist abaixo.
  </action>
  <read_first>
    - apps/web/src/components/app-sidebar.tsx
    - apps/web/src/components/sidebar-user-menu.tsx
  </read_first>
  <what-built>
Sidebar completa com:
- Navegacao por role (Vendedor vs Admin groups)
- User menu no footer (avatar, nome, role badge, ModeToggle, botao Sair)
- ModeToggle (Sun/Moon icon) no SidebarFooter alternando light/dark theme
- Drawer mobile com auto-close apos navegacao
- Touch targets 44px nos nav items
- Active state refinado para rotas aninhadas
  </what-built>
  <how-to-verify>
Iniciar dev server se nao estiver rodando: `bun run dev:web`

**Desktop (largura > 768px):**

1. Acessar http://localhost:3001/dashboard como vendedor
   - [ ] Sidebar lateral visivel com grupo "Vendedor" (Dashboard, Leads, Novo Lead)
   - [ ] Grupo "Admin" NAO aparece
   - [ ] Footer da sidebar exibe avatar (ou iniciais), nome, role "Vendedor", icone Sun/Moon, e botao com icone de logout
   - [ ] Item "Dashboard" esta highlighted (active state)

2. Clicar em "Leads" na sidebar
   - [ ] Navega para /leads
   - [ ] Item "Leads" fica highlighted, "Dashboard" perde highlight
   - [ ] Sidebar permanece expandida (sem collapse)

3. Acessar http://localhost:3001/dashboard como admin
   - [ ] Grupo "Vendedor" visivel
   - [ ] Grupo "Admin" visivel (collapsible, aberto por default) com items: Leads, Usuarios, Stats Globais
   - [ ] Role badge mostra "Admin"

4. Clicar no botao de logout (icone LogOut no footer)
   - [ ] Redireciona para /login
   - [ ] Sessao encerrada

5. **ModeToggle (LAYOUT-08):**
   - [ ] Clicar no icone Sun/Moon no footer da sidebar
   - [ ] Theme alterna entre light e dark
   - [ ] Icone muda de Sun para Moon (ou vice-versa) conforme o theme atual
   - [ ] Clicar novamente reverte o theme

**Mobile (Chrome DevTools, largura < 768px, ex: iPhone 14):**

6. Acessar http://localhost:3001/dashboard
   - [ ] Sidebar NAO esta visivel (drawer fechado)
   - [ ] Botao hamburguer visivel no header

7. Clicar no botao hamburguer
   - [ ] Drawer abre a partir da esquerda
   - [ ] Sidebar content completo visivel (nav items + footer com user menu + ModeToggle)

8. Clicar em "Leads" no drawer
   - [ ] Drawer fecha AUTOMATICAMENTE
   - [ ] Pagina navega para /leads
   - [ ] NAO precisa fechar o drawer manualmente

9. Abrir drawer novamente, clicar em "Novo Lead"
   - [ ] Drawer fecha automaticamente
   - [ ] Navega para /leads/new

**Active state edge cases:**

10. Navegar para /leads/new
    - [ ] Item "Novo Lead" esta highlighted
    - [ ] Item "Leads" NAO esta highlighted simultaneamente (exact match fix)

11. Navegar para /admin/leads/123 (ou qualquer lead ID)
    - [ ] Item "Admin > Leads" esta highlighted

**Touch targets:**

12. No mobile, observar altura dos nav items
    - [ ] Items visivelmente maiores que botoes padrao (~44px de altura)
    - [ ] Facil de tocar sem errar o alvo
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: requires human visual verification"</automated>
  </verify>
  <acceptance_criteria>
    - Human approves all 12 verification steps in the checklist above
    - No visual regressions reported
  </acceptance_criteria>
  <done>Usuario aprova verificacao visual e funcional completa: sidebar navigation, user menu, ModeToggle (light/dark), mobile drawer, touch targets, active state.</done>
  <resume-signal>Type "approved" se tudo funciona, ou descreva os issues encontrados</resume-signal>
</task>

</tasks>

<verification>
1. Build + type-check + linting + testes automatizados passam
2. Verificacao visual humana confirma todos os criterios
3. Mobile drawer auto-close funciona consistentemente
4. Active state correto em todas as rotas testadas
5. ModeToggle alterna theme corretamente no SidebarFooter
</verification>

<success_criteria>
- Todas as verificacoes automatizadas passam (build, types, lint, test)
- Usuario aprova verificacao visual desktop (nav groups, user menu, ModeToggle, active state)
- Usuario aprova verificacao mobile (drawer open/close, auto-close apos nav, touch targets)
- Logout funciona e redireciona para /login
- ModeToggle funciona e alterna light/dark theme
</success_criteria>

<output>
After completion, create `.planning/phases/09-sidebar-content-mobile-ux/09-02-SUMMARY.md`
</output>
