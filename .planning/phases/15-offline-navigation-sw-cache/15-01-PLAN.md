---
phase: 15-offline-navigation-sw-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/public/sw.js
  - apps/web/src/components/service-worker-registrar.tsx
  - apps/web/src/components/providers.tsx
  - apps/web/src/app/(public)/offline/page.tsx
  - apps/web/src/lib/sw/rsc-url-normalizer.ts
  - apps/web/src/lib/sw/__tests__/rsc-url-normalizer.test.ts
  - apps/web/src/components/__tests__/service-worker-registrar.test.ts
  - apps/web/src/app/(public)/offline/__tests__/page.test.ts
autonomous: true
requirements:
  - SW-01
  - SW-02

must_haves:
  truths:
    - "Service Worker e registrado sem erro no console ao carregar o app"
    - "sw.js recebe o header Service-Worker-Allowed e nao produz DOMException de scope"
    - "RSC payloads sao cacheados com URL normalizada (sem ?_rsc=) como cache key"
    - "Assets estaticos em _next/static/** sao servidos do cache apos primeiro carregamento"
    - "Rota /offline existe e renderiza mensagem de fallback com link para /dashboard"
    - "Nenhum manifest.json referenciado, nenhum install prompt ativado"
  artifacts:
    - path: "apps/web/public/sw.js"
      provides: "Service Worker com Workbox CDN, NetworkFirst para RSC, CacheFirst para assets"
      contains: "importScripts.*workbox-cdn"
    - path: "apps/web/src/components/service-worker-registrar.tsx"
      provides: "Client Component que registra o SW via workbox-window"
      exports: ["ServiceWorkerRegistrar"]
    - path: "apps/web/src/lib/sw/rsc-url-normalizer.ts"
      provides: "Funcao pura que remove ?_rsc= da URL para uso como cache key"
      exports: ["normalizeRscUrl"]
    - path: "apps/web/src/app/(public)/offline/page.tsx"
      provides: "Pagina de fallback offline pre-cacheada no install do SW"
    - path: "apps/web/src/lib/sw/__tests__/rsc-url-normalizer.test.ts"
      provides: "Testes unitarios da funcao normalizeRscUrl"
  key_links:
    - from: "apps/web/src/components/providers.tsx"
      to: "apps/web/src/components/service-worker-registrar.tsx"
      via: "import e inclusao como filho dentro de Providers"
      pattern: "ServiceWorkerRegistrar"
    - from: "apps/web/public/sw.js"
      to: "rscUrlNormalizerPlugin"
      via: "cacheKeyWillBeUsed plugin no NetworkFirst strategy"
      pattern: "cacheKeyWillBeUsed"
    - from: "apps/web/next.config.ts"
      to: "apps/web/public/sw.js"
      via: "headers() com Service-Worker-Allowed: /"
      pattern: "Service-Worker-Allowed"
---

<objective>
Implementar o Service Worker minimo que resolve o problema de "Failed to fetch RSC payload" durante navegacao offline no Next.js App Router. O SW usa Workbox v7.4.0 via CDN, sem manifest, sem install prompt, sem next-pwa.

Purpose: Garantir que vendedores que carregaram o app online possam navegar entre rotas autenticadas offline durante eventos sem internet.
Output: sw.js funcional em /public, ServiceWorkerRegistrar integrado em Providers, rota /offline de fallback, e suite de testes unitarios para logica pura extraida.
</objective>

<execution_context>
@/home/othavio/Work/profills/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/profills/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-offline-navigation-sw-cache/15-CONTEXT.md
@.planning/phases/15-offline-navigation-sw-cache/15-RESEARCH.md
@.planning/phases/15-offline-navigation-sw-cache/15-VALIDATION.md

<interfaces>
<!-- Contratos e padroes que o executor precisa. Extraidos do codebase. -->

De apps/web/src/components/sync-status-provider.tsx (padrao useEffect a seguir):
```typescript
"use client";
// useEffect para operacoes client-only, sem acesso ao navigator no escopo do modulo
useEffect(() => {
  // acesso a APIs do browser aqui
}, []);
```

De apps/web/src/components/providers.tsx (ponto de integracao):
```typescript
export default function Providers({ children }: { children: React.ReactNode }) {
  return (
    <ThemeProvider ...>
      <QueryClientProvider client={queryClient}>
        <SyncStatusProvider>{children}</SyncStatusProvider>
        <ReactQueryDevtools />
      </QueryClientProvider>
      <Toaster richColors />
    </ThemeProvider>
  );
}
```

De apps/web/vitest.config.ts (importante — inclui apenas *.test.ts, nao *.test.tsx):
```typescript
include: ["src/**/*.test.ts"],
```
Todos os arquivos de teste DEVEM ter extensao .test.ts (nao .test.tsx).

De apps/web/src/components/sync-status-provider.test.ts (padrao de teste):
```typescript
// Testes sem @testing-library/react — apenas verificacao de exports via dynamic import
it("exports X function", async () => {
  const mod = await import("./module");
  expect(typeof mod.X).toBe("function");
});
```

Rotas autenticadas existentes (de apps/web/src/app/(app)/):
- /dashboard
- /leads
- /leads/new   ← rota de captura (NAO /leads/capture)
- /leads/[id]
- /admin/leads
- /admin/users
- /admin/stats

Rota de fallback a criar:
- /offline     ← em apps/web/src/app/(public)/offline/page.tsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extrair logica de normalizacao de URL e criar testes (Wave 0)</name>
  <files>
    apps/web/src/lib/sw/rsc-url-normalizer.ts
    apps/web/src/lib/sw/__tests__/rsc-url-normalizer.test.ts
  </files>
  <behavior>
    - normalizeRscUrl("https://example.com/dashboard?_rsc=abc123") → "https://example.com/dashboard"
    - normalizeRscUrl("https://example.com/leads?_rsc=xyz&other=1") → "https://example.com/leads?other=1" (remove apenas _rsc)
    - normalizeRscUrl("https://example.com/dashboard") → "https://example.com/dashboard" (sem efeito se nao tem _rsc)
    - normalizeRscUrl("https://example.com/leads/new?_rsc=abc") → "https://example.com/leads/new"
    - Funcao e pura — mesma entrada, mesma saida, sem side effects
  </behavior>
  <action>
    PRIMEIRO crie o arquivo de teste com todos os casos acima falhando (RED).

    Crie apps/web/src/lib/sw/__tests__/rsc-url-normalizer.test.ts:
    - Import: `import { normalizeRscUrl } from "../rsc-url-normalizer"`
    - 5 casos de teste cobrindo os behaviors acima
    - Execute `bun run test --filter web` — DEVE falhar (modulo nao existe ainda)

    Entao crie apps/web/src/lib/sw/rsc-url-normalizer.ts (GREEN):
    - Exporta `normalizeRscUrl(urlString: string): string`
    - Usa `new URL(urlString)`, chama `url.searchParams.delete("_rsc")`, retorna `url.toString()`
    - Nenhum import externo — funcao pura de string → string
    - TypeScript strict — parametro e retorno tipados explicitamente

    Execute `bun run test --filter web` — DEVE passar.

    Esta funcao sera usada conceitualmente no sw.js (que nao pode importar modulos TypeScript —
    o sw.js usa CDN e e plain JS). O beneficio e: a logica testavel e extraida; o sw.js replica
    a mesma logica inline via plugin cacheKeyWillBeUsed.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run test --filter web</automated>
  </verify>
  <done>
    normalizeRscUrl exportada e testada. Todos os 5 casos passam. bun run test --filter web retorna verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar ServiceWorkerRegistrar e testes de exportacao</name>
  <files>
    apps/web/src/components/service-worker-registrar.tsx
    apps/web/src/components/__tests__/service-worker-registrar.test.ts
  </files>
  <behavior>
    - ServiceWorkerRegistrar e uma named export (nao default)
    - ServiceWorkerRegistrar e uma funcao (componente React)
    - O modulo e importavel sem erro em ambiente jsdom (sem navigator.serviceWorker)
    - O componente retorna null (nao renderiza nada visivel)
  </behavior>
  <action>
    PRIMEIRO crie o arquivo de teste (RED):
    apps/web/src/components/__tests__/service-worker-registrar.test.ts:
    - Import dinamico: `const mod = await import("../service-worker-registrar")`
    - Verifica que `mod.ServiceWorkerRegistrar` e uma funcao
    - Verifica que o modulo nao tem export default

    Execute `bun run test --filter web` — DEVE falhar (arquivo nao existe).

    Entao crie apps/web/src/components/service-worker-registrar.tsx (GREEN):

    ```tsx
    "use client";

    import { useEffect } from "react";

    export function ServiceWorkerRegistrar() {
      useEffect(() => {
        if (!("serviceWorker" in navigator)) return;

        import("workbox-window").then(({ Workbox }) => {
          const wb = new Workbox("/sw.js");

          // D-09: quando nova versao do SW esta waiting, ativar imediatamente
          wb.addEventListener("waiting", () => {
            wb.messageSkipWaiting();
          });

          wb.register();
        });
      }, []);

      return null;
    }
    ```

    Convencoes obrigatorias:
    - "use client" no topo
    - Named export (nao default) — alinhado com o padrao do projeto
    - Sem console.log — sem logging em producao (per CLAUDE.md)
    - Sem `any` — TypeScript strict
    - Sem scope explicito no register() — o header Service-Worker-Allowed no next.config.ts
      garante que o scope "/" seja aceito sem precisar passar options

    Execute `bun run test --filter web` — DEVE passar.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run test --filter web</automated>
  </verify>
  <done>
    ServiceWorkerRegistrar exportado como named export. Testes passam. bun run test --filter web verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar pagina offline de fallback e testes</name>
  <files>
    apps/web/src/app/(public)/offline/page.tsx
    apps/web/src/app/(public)/offline/__tests__/page.test.ts
  </files>
  <behavior>
    - O modulo e importavel sem erro em jsdom
    - O modulo exporta um componente como default export
    - O componente e uma funcao
    - O modulo nao referencia manifest.json nem funcionalidade PWA
  </behavior>
  <action>
    PRIMEIRO crie o arquivo de teste (RED):
    apps/web/src/app/(public)/offline/__tests__/page.test.ts:
    - Import dinamico do modulo da pagina
    - Verifica que o default export e uma funcao
    - Verifica que o modulo nao tem propriedade "manifest"

    Execute `bun run test --filter web` — DEVE falhar.

    Entao crie apps/web/src/app/(public)/offline/page.tsx (GREEN):

    ```tsx
    import Link from "next/link";

    export default function OfflinePage() {
      return (
        <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8 text-center">
          <h1 className="text-2xl font-semibold">Sem conexao</h1>
          <p className="text-muted-foreground max-w-sm text-sm">
            Voce esta offline. As rotas que voce visitou anteriormente continuam disponiveis.
          </p>
          <Link
            className="text-primary text-sm underline underline-offset-4"
            href="/dashboard"
          >
            Ir para o dashboard
          </Link>
        </main>
      );
    }
    ```

    Convencoes obrigatorias:
    - Server Component (sem "use client") — pagina estatica, sem interatividade
    - Sem import de cn() — classes simples sem condicional
    - Sem manifest.json, sem install prompt, sem qualquer referencia a PWA
    - Link com href="/dashboard" (D-07: link para rotas disponiveis)
    - CSS: Tailwind classes apenas — nao criar arquivo CSS separado

    Execute `bun run test --filter web` — DEVE passar.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run test --filter web</automated>
  </verify>
  <done>
    Pagina /offline existe e exporta componente como default. Testes passam. bun run test --filter web verde.
  </done>
</task>

<task type="auto">
  <name>Task 4: Instalar workbox-window, criar sw.js e configurar next.config.ts</name>
  <files>
    apps/web/package.json
    apps/web/next.config.ts
    apps/web/public/sw.js
    apps/web/src/components/providers.tsx
  </files>
  <action>
    Esta task conecta tudo. Execute em ordem:

    PASSO 1 — Instalar workbox-window:
    ```bash
    cd /home/othavio/Work/profills/sistema-coleta-de-lead
    bun add workbox-window@7.4.0 --filter web
    ```
    Verificar que workbox-window aparece em apps/web/package.json > dependencies.

    PASSO 2 — Atualizar apps/web/next.config.ts para adicionar headers():
    O arquivo atual:
    ```typescript
    import "@dashboard-leads-profills/env/web";
    import type { NextConfig } from "next";

    const nextConfig: NextConfig = {
      typedRoutes: true,
      reactCompiler: true,
    };

    export default nextConfig;
    ```
    Adicionar async headers():
    ```typescript
    import "@dashboard-leads-profills/env/web";
    import type { NextConfig } from "next";

    const nextConfig: NextConfig = {
      typedRoutes: true,
      reactCompiler: true,
      async headers() {
        return [
          {
            source: "/sw.js",
            headers: [
              {
                key: "Service-Worker-Allowed",
                value: "/",
              },
              {
                key: "Cache-Control",
                value: "no-cache, no-store, must-revalidate",
              },
            ],
          },
        ];
      },
    };

    export default nextConfig;
    ```
    O header Service-Worker-Allowed: / e OBRIGATORIO — sem ele o registro falha com DOMException
    quando o scope "/" e solicitado (Pitfall 3 do RESEARCH.md).
    O Cache-Control no-store para sw.js garante que o browser sempre valida o arquivo antes de usar.

    PASSO 3 — Criar apps/web/public/sw.js:
    Criar o arquivo service worker completo. Usar apenas plain JavaScript (sem TypeScript,
    sem imports ES modules — Next.js NAO processa arquivos em /public):

    ```javascript
    // sw.js — Service Worker para cache de navegacao offline
    // Workbox v7.4.0 via CDN (importScripts — unico mecanismo valido em /public)
    // NAO e PWA: sem manifest, sem install prompt, sem background sync

    importScripts(
      "https://storage.googleapis.com/workbox-cdn/releases/7.4.0/workbox-sw.js"
    );

    const { registerRoute, setCatchHandler } = workbox.routing;
    const { NetworkFirst, CacheFirst } = workbox.strategies;
    const { precacheAndRoute } = workbox.precaching;
    const { ExpirationPlugin } = workbox.expiration;
    const { clientsClaim } = workbox.core;
    const { CacheableResponsePlugin } = workbox.cacheableResponse;

    // D-09: ativar imediatamente sem aguardar reload manual
    // NOTA: workbox.core.skipWaiting() foi REMOVIDO no v7 — usar self.skipWaiting() diretamente
    self.skipWaiting();
    clientsClaim();

    // D-01: pre-cache de todas as rotas autenticadas no install
    // revision: "v1" — atualizar ao mudar o app shell para invalidar o cache
    precacheAndRoute([
      { url: "/dashboard", revision: "v1" },
      { url: "/leads", revision: "v1" },
      { url: "/leads/new", revision: "v1" },
      { url: "/admin/leads", revision: "v1" },
      { url: "/admin/users", revision: "v1" },
      { url: "/admin/stats", revision: "v1" },
      { url: "/offline", revision: "v1" },  // D-08: pre-cache da pagina de fallback
    ]);

    // Plugin para normalizar URL antes de usar como cache key
    // Remove ?_rsc=<random> gerado pelo Next.js App Router em cada navegacao client-side
    // Sem isso: cada navegacao cria uma entrada diferente no cache (Pitfall 1 do RESEARCH.md)
    const rscUrlNormalizerPlugin = {
      cacheKeyWillBeUsed: async ({ request }) => {
        const url = new URL(request.url);
        url.searchParams.delete("_rsc");
        return url.toString();
      },
    };

    const RSC_CACHE = "rsc-payloads-v1";

    // D-03: RSC payloads — NetworkFirst (network first, fallback ao cache offline)
    // Detectar pelo header RSC: 1 que o App Router envia em toda navegacao client-side
    registerRoute(
      ({ request }) =>
        request.headers.get("RSC") === "1" ||
        request.headers.get("Rsc") === "1",
      new NetworkFirst({
        cacheName: RSC_CACHE,
        networkTimeoutSeconds: 3,
        plugins: [
          rscUrlNormalizerPlugin,
          // CacheableResponsePlugin: garante que respostas 200 sejam armazenadas
          // mesmo que Next.js envie Cache-Control: private (nao e no-store para RSC)
          new CacheableResponsePlugin({ statuses: [0, 200] }),
          new ExpirationPlugin({
            maxEntries: 50,
            maxAgeSeconds: 60 * 60, // 1 hora
          }),
        ],
      })
    );

    const STATIC_CACHE = "static-assets-v1";

    // D-04: assets estaticos Next.js — CacheFirst (imutaveis por hash no nome do arquivo)
    registerRoute(
      ({ url }) => url.pathname.startsWith("/_next/static/"),
      new CacheFirst({
        cacheName: STATIC_CACHE,
        plugins: [
          new ExpirationPlugin({
            maxEntries: 200,
            maxAgeSeconds: 365 * 24 * 60 * 60, // 1 ano
          }),
        ],
      })
    );

    // D-04: fonts Google (carregadas por next/font/google)
    registerRoute(
      ({ url }) =>
        url.hostname === "fonts.googleapis.com" ||
        url.hostname === "fonts.gstatic.com",
      new CacheFirst({
        cacheName: "google-fonts-v1",
        plugins: [
          new ExpirationPlugin({
            maxEntries: 20,
            maxAgeSeconds: 365 * 24 * 60 * 60,
          }),
        ],
      })
    );

    // D-07: fallback para rotas offline nao cacheadas
    setCatchHandler(({ event }) => {
      // Navegacao HTML: servir pagina offline
      if (event.request.destination === "document") {
        return caches.match("/offline");
      }
      // RSC payload falhou e nao esta em cache:
      // retornar 503 com Content-Type correto — React nao quebra, mantém estado atual
      if (
        event.request.headers.get("RSC") === "1" ||
        event.request.headers.get("Rsc") === "1"
      ) {
        return new Response("", {
          status: 503,
          headers: { "Content-Type": "text/x-component" },
        });
      }
      return Response.error();
    });
    ```

    PASSO 4 — Integrar ServiceWorkerRegistrar em providers.tsx:
    Adicionar import e incluir `<ServiceWorkerRegistrar />` como sibling de `<Toaster />`,
    fora do QueryClientProvider (nao precisa de query context):

    ```tsx
    "use client";

    import { Toaster } from "@dashboard-leads-profills/ui/components/sonner";
    import { QueryClientProvider } from "@tanstack/react-query";
    import { ReactQueryDevtools } from "@tanstack/react-query-devtools";

    import { queryClient } from "@/utils/trpc";

    import { ServiceWorkerRegistrar } from "./service-worker-registrar";
    import { SyncStatusProvider } from "./sync-status-provider";
    import { ThemeProvider } from "./theme-provider";

    export default function Providers({ children }: { children: React.ReactNode }) {
      return (
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          disableTransitionOnChange
          enableSystem
        >
          <QueryClientProvider client={queryClient}>
            <SyncStatusProvider>{children}</SyncStatusProvider>
            <ReactQueryDevtools />
          </QueryClientProvider>
          <Toaster richColors />
          <ServiceWorkerRegistrar />
        </ThemeProvider>
      );
    }
    ```

    VERIFICACOES POS-EDICAO:
    1. `bun run check-types` — sem erros de TypeScript
    2. `bun run check` — Biome sem erros
    3. `bun run test --filter web` — suite verde
  </action>
  <verify>
    <automated>cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run check-types && bun run test --filter web</automated>
  </verify>
  <done>
    - workbox-window@7.4.0 em apps/web/package.json
    - next.config.ts com headers() para Service-Worker-Allowed: /
    - apps/web/public/sw.js criado com todas as strategies (NetworkFirst RSC, CacheFirst static, precache, catchHandler)
    - ServiceWorkerRegistrar integrado em providers.tsx
    - check-types verde, test verde
    - Nenhum manifest.json referenciado no sw.js
  </done>
</task>

</tasks>

<verification>
Verificacao geral apos completar todos os tasks:

```bash
# Tipos
cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run check-types

# Linting
cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run check

# Testes
cd /home/othavio/Work/profills/sistema-coleta-de-lead && bun run test --filter web

# Confirmar ausencia de manifest no sw.js (SW-02)
grep -i "manifest" /home/othavio/Work/profills/sistema-coleta-de-lead/apps/web/public/sw.js && echo "ERRO: manifest encontrado" || echo "OK: sem manifest"

# Confirmar header obrigatorio no next.config.ts
grep "Service-Worker-Allowed" /home/othavio/Work/profills/sistema-coleta-de-lead/apps/web/next.config.ts && echo "OK: header presente" || echo "ERRO: header ausente"

# Confirmar que skipWaiting correto e usado (sem workbox.core.skipWaiting)
grep "workbox.core.skipWaiting" /home/othavio/Work/profills/sistema-coleta-de-lead/apps/web/public/sw.js && echo "ERRO: API depreciada" || echo "OK: self.skipWaiting() correto"
```
</verification>

<success_criteria>
1. `bun run test --filter web` verde com os 3 novos arquivos de teste (rsc-url-normalizer, service-worker-registrar, offline/page)
2. `bun run check-types` sem erros — TypeScript strict satisfeito
3. `bun run check` sem erros — Biome 2.4 satisfeito
4. `apps/web/public/sw.js` existe e contém: importScripts do Workbox CDN, self.skipWaiting(), cacheKeyWillBeUsed plugin, NetworkFirst para RSC, CacheFirst para _next/static, precacheAndRoute com 7 rotas, setCatchHandler
5. `apps/web/next.config.ts` contém headers() com Service-Worker-Allowed: /
6. `apps/web/src/components/providers.tsx` importa e renderiza ServiceWorkerRegistrar
7. `apps/web/src/app/(public)/offline/page.tsx` existe sem referencia a manifest ou PWA
8. Nenhum arquivo modificado referencia manifest.json nem install prompt (SW-02)
</success_criteria>

<output>
Apos completar todos os tasks e verificacoes, criar:
`.planning/phases/15-offline-navigation-sw-cache/15-01-SUMMARY.md`

O SUMMARY deve incluir:
- Arquivos criados/modificados com uma linha descrevendo o que cada um faz
- Decisoes tomadas (cache bucket names, networkTimeoutSeconds=3, maxEntries)
- Qualquer desvio dos patterns do RESEARCH.md e por que
- Estado dos testes (quantos passam)
- Proximos passos: executar 15-02-PLAN.md para validacao manual cross-browser
</output>
