---
phase: 01-auth-migration
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - apps/web/package.json
  - packages/api/src/routers/todo.ts
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
must_haves:
  truths:
    - "No file in the entire project references better-auth"
    - "bun run check-types passes with zero errors"
    - "bun run check (Biome) passes"
    - "bun run build completes successfully"
    - "OAuth login flow works end-to-end (requires manual verification)"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Web app dependencies without better-auth"
  key_links:
    - from: "apps/web/src/components/login-card.tsx"
      to: "/auth/callback"
      via: "OAuth redirect flow"
      pattern: "/auth/callback"
    - from: "apps/web/src/app/auth/callback/route.ts"
      to: "/dashboard"
      via: "post-auth redirect"
      pattern: "/dashboard"
user_setup:
  - service: supabase-dashboard
    why: "OAuth providers must be configured in Supabase Dashboard before login works"
    dashboard_config:
      - task: "Enable Google provider with Client ID and Secret from Google Cloud Console"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"
      - task: "Enable Facebook provider with App ID and Secret from Facebook Developers"
        location: "Supabase Dashboard -> Authentication -> Providers -> Facebook"
      - task: "Enable LinkedIn (OIDC) provider with Client ID and Secret from LinkedIn Developers"
        location: "Supabase Dashboard -> Authentication -> Providers -> LinkedIn (OIDC)"
      - task: "Add redirect URL: http://localhost:3001/auth/callback"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs"
      - task: "Run Custom Access Token Hook SQL in SQL Editor"
        location: "Supabase Dashboard -> SQL Editor"
      - task: "Enable Custom Access Token Hook"
        location: "Supabase Dashboard -> Authentication -> Hooks -> Custom Access Token"
  - service: supabase-schema
    why: "user_roles table and app_role enum must be pushed to database"
    env_vars: []
    dashboard_config:
      - task: "Run bun run db:push from project root to push user_roles table"
        location: "Terminal"
---

<objective>
Final cleanup, dependency removal, build verification, and manual OAuth flow checkpoint. Ensures the entire auth migration is complete and working.

Purpose: Guarantee no Better-Auth remnants remain, all automated checks pass, and the human verifies the actual OAuth flow works with configured providers.
Output: Clean build with no Better-Auth references. Human-verified OAuth login.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-migration/01-CONTEXT.md
@.planning/phases/01-auth-migration/01-01-SUMMARY.md
@.planning/phases/01-auth-migration/01-02-SUMMARY.md
@.planning/phases/01-auth-migration/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Better-Auth remnants and verify clean build</name>
  <files>
    apps/web/package.json,
    packages/api/src/routers/todo.ts
  </files>
  <read_first>
    apps/web/package.json,
    packages/api/src/routers/todo.ts,
    packages/api/src/routers/index.ts,
    packages/auth/package.json,
    packages/api/package.json
  </read_first>
  <action>
    **Step 1: Remove better-auth from apps/web/package.json**

    The apps/web/package.json still has `"better-auth": "catalog:"` in dependencies. Remove it:
    ```bash
    cd apps/web && bun remove better-auth
    ```

    Also remove `@tanstack/react-form` if it's only used by the deleted sign-in-form.tsx and sign-up-form.tsx. Check if any other component uses it first. If no other file imports from `@tanstack/react-form`, remove it:
    ```bash
    cd apps/web && bun remove @tanstack/react-form
    ```

    **Step 2: Fix any remaining router references to old context shape**

    Read `packages/api/src/routers/todo.ts` and check if it references `ctx.session`. If it does, update to use `ctx.user`. The user ID in Supabase JWT is in `ctx.user.sub` (the JWT `sub` claim is the Supabase user UUID).

    For example, if todo.ts has:
    ```typescript
    ctx.session.user.id
    ```
    Change to:
    ```typescript
    ctx.user.sub
    ```

    **Step 3: Search entire codebase for Better-Auth references**

    Run a grep to find any remaining references:
    ```bash
    grep -r "better-auth\|betterAuth\|authClient\|auth-client\|BETTER_AUTH" --include="*.ts" --include="*.tsx" --include="*.json" apps/ packages/
    ```

    Fix any remaining references found.

    **Step 4: Run bun install to update lockfile**

    ```bash
    bun install
    ```

    **Step 5: Run full verification suite**

    ```bash
    bun run check-types
    bun run check
    bun run build
    ```

    Fix any errors. The build requires valid env vars — ensure `.env` has at minimum placeholder values for `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` and `DATABASE_URL`.

    If build fails due to env validation, set placeholder values in `.env`:
    ```
    NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-key
    DATABASE_URL=postgresql://placeholder:placeholder@localhost:5432/placeholder
    ```
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types && bun run check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json does NOT contain `better-auth`
    - No .ts or .tsx file in apps/ or packages/ contains `better-auth` or `betterAuth` or `authClient` (from better-auth)
    - No .ts or .tsx file imports from `@/lib/auth-client`
    - `bun run check-types` exits 0
    - `bun run check` exits 0 (or only warnings)
  </acceptance_criteria>
  <done>
    All Better-Auth references removed from the entire project. Type check and lint pass. Dependencies cleaned.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of OAuth login flow</name>
  <files>n/a</files>
  <action>
    Present the user with setup instructions and verification steps for the complete OAuth flow.

    **What was built across Plans 01-04:**
    1. Supabase client utilities (browser + server) in apps/web/src/lib/supabase/
    2. proxy.ts for session refresh via getClaims()
    3. OAuth callback route at /auth/callback
    4. LoginCard with Google (primary), LinkedIn (outline), Facebook (outline) buttons
    5. User menu with Supabase session and "Sair" logout
    6. tRPC context reading Supabase JWT claims
    7. protectedProcedure (auth required) and adminProcedure (admin role required)
    8. user_roles Drizzle table for RBAC
    9. Custom Access Token Hook SQL for injecting role into JWT

    **Pre-requisites the user must complete (one-time setup):**
    1. Add env vars to `apps/web/.env`:
       - `NEXT_PUBLIC_SUPABASE_URL` (from Supabase Dashboard -> Settings -> API)
       - `NEXT_PUBLIC_SUPABASE_ANON_KEY` (from Supabase Dashboard -> Settings -> API)
       - `DATABASE_URL` (should already exist)
    2. Push schema: `bun run db:push` (creates user_roles table + app_role enum)
    3. Run Custom Access Token Hook SQL in Supabase SQL Editor (file at `packages/db/src/migrations/custom-access-token-hook.sql`)
    4. Enable the hook: Supabase Dashboard -> Authentication -> Hooks -> Custom Access Token -> select `custom_access_token_hook`
    5. Configure at least Google OAuth provider: Supabase Dashboard -> Authentication -> Providers -> Google
    6. Add `http://localhost:3001/auth/callback` to Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs

    **Test steps for the user:**
    1. Start dev server: `bun run dev:web`
    2. Visit http://localhost:3001/login
    3. Verify: Card centered on page with title "Dashboard Leads", 3 buttons (Google primary, LinkedIn outline, Facebook outline)
    4. Click "Entrar com Google"
    5. Verify: Button shows spinner, other buttons disabled, redirect to Google OAuth
    6. Complete Google login
    7. Verify: Redirected to /dashboard, user menu shows your name
    8. Click user menu -> verify email shown -> click "Sair"
    9. Verify: Redirected to /login
    10. Visit http://localhost:3001/dashboard without logging in
    11. Verify: Redirected to /login by proxy.ts
    12. Close browser, reopen, visit http://localhost:3001/dashboard
    13. Verify: Session persists (not redirected to /login) — AUTH-05
  </action>
  <verify>Human confirms OAuth flow works end-to-end</verify>
  <done>User types "approved" after verifying login, session persistence, logout, and route protection work correctly.</done>
</task>

</tasks>

<verification>
- Zero references to `better-auth`, `betterAuth`, `authClient`, `BETTER_AUTH_*` in codebase
- `bun run check-types` passes
- `bun run check` passes
- `bun run build` passes (with valid env vars)
- Human verified: OAuth login, session persistence, logout, route protection
</verification>

<success_criteria>
Phase 1 Auth Migration is complete: Supabase Auth replaces Better-Auth across the entire stack. OAuth login works with Google/LinkedIn/Facebook. Sessions persist. Routes are protected. Admin role check exists in tRPC. All automated checks pass. Human has verified the flow.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-migration/01-04-SUMMARY.md`
</output>
