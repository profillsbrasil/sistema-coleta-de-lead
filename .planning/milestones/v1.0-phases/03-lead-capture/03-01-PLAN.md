---
phase: 03-lead-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/lead/validation.ts
  - apps/web/src/lib/lead/validation.test.ts
  - apps/web/src/lib/lead/wa-parser.ts
  - apps/web/src/lib/lead/wa-parser.test.ts
  - apps/web/src/lib/lead/compression.ts
  - apps/web/src/lib/lead/compression.test.ts
  - apps/web/src/lib/lead/save-lead.ts
  - apps/web/src/lib/lead/save-lead.test.ts
autonomous: true
requirements:
  - CAPT-01
  - CAPT-05
  - CAPT-07
  - CAPT-08

must_haves:
  truths:
    - "Zod schema valida nome obrigatorio, pelo menos um de phone/email, e interestTag enum"
    - "parseWhatsAppUrl extrai numero de telefone de URLs wa.me com e sem + e com DDI"
    - "compressImage reduz imagem para max 1280px e retorna JPEG Blob"
    - "saveLead persiste lead no Dexie e adiciona operacao na syncQueue"
  artifacts:
    - path: "apps/web/src/lib/lead/validation.ts"
      provides: "Zod schema leadFormSchema + LeadFormData type"
      exports: ["leadFormSchema", "LeadFormData"]
    - path: "apps/web/src/lib/lead/wa-parser.ts"
      provides: "parseWhatsAppUrl function"
      exports: ["parseWhatsAppUrl"]
    - path: "apps/web/src/lib/lead/compression.ts"
      provides: "compressImage function"
      exports: ["compressImage"]
    - path: "apps/web/src/lib/lead/save-lead.ts"
      provides: "saveLead function (Dexie write + syncQueue)"
      exports: ["saveLead"]
  key_links:
    - from: "apps/web/src/lib/lead/save-lead.ts"
      to: "apps/web/src/lib/db/index.ts"
      via: "db.leads.add + db.syncQueue.add"
      pattern: "db\\.leads\\.add|db\\.syncQueue\\.add"
    - from: "apps/web/src/lib/lead/validation.ts"
      to: "apps/web/src/lib/db/types.ts"
      via: "LeadFormData maps to Lead interface fields"
      pattern: "interestTag.*quente.*morno.*frio"
---

<objective>
Install dependencies (html5-qrcode, shadcn textarea/collapsible) and create the utility modules for lead capture with TDD: Zod validation schema, WhatsApp URL parser, image compression, and saveLead function.

Purpose: Foundation layer that all UI components will consume. TDD ensures correctness of business logic before building UI.
Output: 4 tested utility modules in `apps/web/src/lib/lead/`, plus new dependencies installed.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lead-capture/03-CONTEXT.md
@.planning/phases/03-lead-capture/03-RESEARCH.md
@.planning/phases/03-lead-capture/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/web/src/lib/db/types.ts:
```typescript
export interface Lead {
  company: string | null;
  createdAt: string;
  deletedAt: string | null;
  email: string | null;
  interestTag: "quente" | "morno" | "frio";
  localId: string;
  name: string;
  notes: string | null;
  phone: string | null;
  photo: Blob | null;
  position: string | null;
  segment: string | null;
  serverId: number | null;
  syncStatus: "pending" | "synced" | "conflict";
  updatedAt: string;
  userId: string;
}

export interface SyncQueueItem {
  id?: number;
  localId: string;
  operation: "create" | "update" | "delete";
  payload: string;
  retryCount: number;
  timestamp: string;
}
```

From apps/web/src/lib/db/index.ts:
```typescript
export { db }; // Dexie instance with db.leads and db.syncQueue
export type { Lead, SyncQueueItem };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and create utility modules with TDD</name>
  <files>
    apps/web/src/lib/lead/validation.ts,
    apps/web/src/lib/lead/validation.test.ts,
    apps/web/src/lib/lead/wa-parser.ts,
    apps/web/src/lib/lead/wa-parser.test.ts,
    apps/web/src/lib/lead/compression.ts,
    apps/web/src/lib/lead/compression.test.ts
  </files>
  <read_first>
    apps/web/src/lib/db/types.ts,
    apps/web/src/lib/db/index.ts,
    packages/db/src/schema/leads.ts,
    .planning/phases/03-lead-capture/03-RESEARCH.md
  </read_first>
  <behavior>
    validation.ts:
    - Test: leadFormSchema.parse({name:"João", phone:"11999", interestTag:"morno"}) succeeds
    - Test: leadFormSchema.parse({name:"", phone:"11999"}) fails with "Nome e obrigatorio"
    - Test: leadFormSchema.parse({name:"João"}) fails with "Informe telefone ou email" (no phone, no email)
    - Test: leadFormSchema.parse({name:"João", email:"a@b.com"}) succeeds (email only, no phone)
    - Test: leadFormSchema.parse({name:"João", phone:"11999", interestTag:"invalido"}) fails (invalid tag)
    - Test: default interestTag is "morno" when omitted
    - Test: optional fields (company, position, segment, notes) default to empty string

    wa-parser.ts:
    - Test: parseWhatsAppUrl("https://wa.me/5511999999999") returns "+5511999999999"
    - Test: parseWhatsAppUrl("https://wa.me/+5511999999999") returns "+5511999999999"
    - Test: parseWhatsAppUrl("wa.me/5511999999999") returns "+5511999999999" (no protocol)
    - Test: parseWhatsAppUrl("https://google.com") returns null
    - Test: parseWhatsAppUrl("random text") returns null
    - Test: parseWhatsAppUrl("https://wa.me/") returns null (no number)

    compression.ts:
    - Test: compressImage returns a Blob with type "image/jpeg"
    - Test: compressImage output is smaller than or equal to input for large images
    - Note: canvas mocking needed in jsdom — test the dimension calculation logic separately
  </behavior>
  <action>
    1. Install dependencies:
       - `cd /home/othavio/Work/sistema-coleta-de-lead && bun add html5-qrcode`
       - `cd /home/othavio/Work/sistema-coleta-de-lead && bunx shadcn@latest add textarea collapsible --yes`

    2. Create `apps/web/src/lib/lead/validation.ts` (per D-03, D-16, CAPT-01, CAPT-07):
       - Zod schema with fields: name (z.string().min(1)), phone (z.string().optional().default("")), email (z.string().email().optional().or(z.literal(""))), interestTag (z.enum(["quente","morno","frio"]).default("morno")), company, position, segment, notes (all z.string().optional().default(""))
       - .refine() to ensure phone or email is provided: `(data) => data.phone || data.email` with message "Informe telefone ou email" on path ["phone"]
       - Export LeadFormData type via z.infer

    3. Create `apps/web/src/lib/lead/wa-parser.ts` (per D-08, CAPT-03):
       - Regex: `/(?:https?:\/\/)?wa\.me\/\+?(\d{10,15})/i`
       - Function parseWhatsAppUrl(text: string): string | null
       - Return phone with "+" prefix normalized

    4. Create `apps/web/src/lib/lead/compression.ts` (per D-11, CAPT-05):
       - Function compressImage(file: File): Promise<Blob>
       - MAX_DIMENSION = 1280, JPEG_QUALITY = 0.7
       - Use createImageBitmap + canvas.toBlob
       - Calculate ratio based on largest dimension
       - Close bitmap after drawing

    5. TDD: Write tests FIRST (RED), then implement (GREEN), then clean up (REFACTOR).
       - validation.test.ts: test all validation cases listed in behavior
       - wa-parser.test.ts: test URL parsing edge cases
       - compression.test.ts: test with mocked canvas/createImageBitmap if possible in jsdom, otherwise test dimension calculation logic as pure function

    6. Run `bun run check` to ensure Biome compliance.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun vitest run apps/web/src/lib/lead/validation.test.ts apps/web/src/lib/lead/wa-parser.test.ts apps/web/src/lib/lead/compression.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "leadFormSchema" apps/web/src/lib/lead/validation.ts
    - grep -q "LeadFormData" apps/web/src/lib/lead/validation.ts
    - grep -q "parseWhatsAppUrl" apps/web/src/lib/lead/wa-parser.ts
    - grep -q "compressImage" apps/web/src/lib/lead/compression.ts
    - grep -q "wa\.me" apps/web/src/lib/lead/wa-parser.ts
    - grep -q "1280" apps/web/src/lib/lead/compression.ts
    - grep -q "0.7" apps/web/src/lib/lead/compression.ts
    - grep -rq "describe\|it\|test" apps/web/src/lib/lead/validation.test.ts
    - grep -rq "describe\|it\|test" apps/web/src/lib/lead/wa-parser.test.ts
  </acceptance_criteria>
  <done>
    All 3 utility modules exported, all tests passing, html5-qrcode and shadcn textarea/collapsible installed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create saveLead function with TDD (Dexie offline-first write)</name>
  <files>
    apps/web/src/lib/lead/save-lead.ts,
    apps/web/src/lib/lead/save-lead.test.ts
  </files>
  <read_first>
    apps/web/src/lib/db/types.ts,
    apps/web/src/lib/db/index.ts,
    apps/web/src/lib/lead/validation.ts
  </read_first>
  <behavior>
    - Test: saveLead creates a Lead in db.leads with localId (UUID), syncStatus "pending", serverId null
    - Test: saveLead adds SyncQueueItem to db.syncQueue with operation "create" and JSON payload (no photo in payload)
    - Test: saveLead with photo stores Blob in lead.photo
    - Test: saveLead without photo stores null in lead.photo
    - Test: saveLead sets createdAt and updatedAt to ISO timestamp
    - Test: saveLead sets userId from parameter
    - Test: optional fields null when empty string
  </behavior>
  <action>
    1. Create `apps/web/src/lib/lead/save-lead.ts` (per D-05, CAPT-08):
       - Function saveLead(data: LeadFormData, userId: string, photo: Blob | null): Promise<string> (returns localId)
       - Use crypto.randomUUID() for localId
       - Create Lead object mapping LeadFormData fields, setting syncStatus to "pending", serverId to null
       - Convert empty strings to null for optional fields (phone, email, company, position, segment, notes)
       - db.leads.add(lead) then db.syncQueue.add({localId, operation:"create", payload: JSON.stringify(text fields only — no photo), retryCount:0, timestamp:now})
       - Return localId

    2. TDD with fake-indexeddb (already configured from Phase 2):
       - Import from "fake-indexeddb/auto" in test setup
       - Test all behaviors listed above
       - Verify db.leads.get(localId) returns correct data
       - Verify db.syncQueue has exactly one item after saveLead

    3. Run `bun run check` for Biome compliance.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun vitest run apps/web/src/lib/lead/save-lead.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "saveLead" apps/web/src/lib/lead/save-lead.ts
    - grep -q "db.leads.add" apps/web/src/lib/lead/save-lead.ts
    - grep -q "db.syncQueue.add" apps/web/src/lib/lead/save-lead.ts
    - grep -q "crypto.randomUUID" apps/web/src/lib/lead/save-lead.ts
    - grep -q "pending" apps/web/src/lib/lead/save-lead.ts
    - grep -rq "describe\|it\|test" apps/web/src/lib/lead/save-lead.test.ts
  </acceptance_criteria>
  <done>
    saveLead persists lead in Dexie with syncStatus "pending" and enqueues sync operation. All tests green. No network calls in the function.
  </done>
</task>

</tasks>

<verification>
All utility modules have passing unit tests. No network calls in any module (offline-first). Biome check passes.

```bash
cd /home/othavio/Work/sistema-coleta-de-lead && bun vitest run apps/web/src/lib/lead/ --reporter=verbose
```
</verification>

<success_criteria>
- 4 utility modules exist with exported functions
- All unit tests passing (validation, wa-parser, compression, save-lead)
- html5-qrcode installed in package.json
- shadcn textarea and collapsible components available
- No network calls in any utility function
- Biome check clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-lead-capture/03-01-SUMMARY.md`
</output>
