---
phase: 07-auth-admin-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/proxy.ts
  - apps/web/src/middleware.ts
  - apps/web/src/app/dashboard/page.tsx
  - apps/web/src/app/leads/new/page.tsx
  - apps/web/src/app/dashboard/personal-dashboard.tsx
  - apps/web/src/app/dashboard/dashboard.tsx
autonomous: true
requirements:
  - AUTH-05
  - ADMN-07

must_haves:
  truths:
    - "Next.js carrega middleware.ts e o middleware manifest nao e vazio (session refresh dispara em cada request)"
    - "Admin que seleciona outro vendedor no dashboard ve os stats reais desse vendedor (nao zeros)"
    - "dashboard/page.tsx detecta role admin via getClaims() — consistente com admin/layout.tsx e tRPC context"
    - "/leads/new redireciona para /login quando usuario nao autenticado (consistente com /leads e /leads/[id])"
    - "proxy.ts (raiz) e deletado — nao ha mais arquivo de middleware mal posicionado"
  artifacts:
    - path: "apps/web/src/middleware.ts"
      provides: "Next.js middleware com default export nomeado middleware + config.matcher"
      exports: ["default (middleware function)", "config"]
    - path: "apps/web/src/app/dashboard/page.tsx"
      provides: "Auth guard + isAdmin via getClaims()"
      contains: "supabase.auth.getClaims()"
    - path: "apps/web/src/app/leads/new/page.tsx"
      provides: "Auth guard server-side para /leads/new"
      contains: "redirect(\"/login\")"
    - path: "apps/web/src/app/dashboard/personal-dashboard.tsx"
      provides: "PersonalDashboard com overrideStats prop opcional"
      contains: "overrideStats"
    - path: "apps/web/src/app/dashboard/dashboard.tsx"
      provides: "Dashboard com useQuery condicional para admin stats via tRPC"
      contains: "trpc.admin.stats.getGlobalStats"
  key_links:
    - from: "apps/web/src/middleware.ts"
      to: "apps/web/src/lib/supabase/proxy.ts"
      via: "import { updateSession } from \"@/lib/supabase/proxy\""
      pattern: "updateSession"
    - from: "apps/web/src/app/dashboard/dashboard.tsx"
      to: "trpc.admin.stats.getGlobalStats"
      via: "useQuery com trpc.admin.stats.getGlobalStats.queryOptions"
      pattern: "getGlobalStats"
    - from: "apps/web/src/app/dashboard/dashboard.tsx"
      to: "apps/web/src/app/dashboard/personal-dashboard.tsx"
      via: "overrideStats prop"
      pattern: "overrideStats"
---

<objective>
Fechar os 4 gaps identificados pelo audit v1.0: 2 blockers (AUTH-05, ADMN-07) e 2 inconsistencias de media severidade.

Purpose: AUTH-05 impede que o session refresh via cookies funcione — Next.js ignora completamente proxy.ts. ADMN-07 faz o admin ver zeros ao selecionar outro vendedor porque Dexie so tem dados do usuario autenticado. Os dois non-blockers (isAdmin via getClaims, /leads/new auth guard) fecham inconsistencias de auth pattern no codebase.

Output: middleware.ts criado em src/, proxy.ts deletado, dashboard/page.tsx usando getClaims(), /leads/new com auth guard, PersonalDashboard aceitando overrideStats, Dashboard passando stats do servidor quando admin visualiza outro vendedor.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-auth-admin-fixes/07-CONTEXT.md

<interfaces>
<!-- Tipos e contratos que o executor precisa. Extraidos do codebase. -->

De apps/web/src/lib/lead/stats.ts:





```typescript
export interface PersonalStats {
  frio: number;
  hoje: number;
  morno: number;
  quente: number;
  score: number;
  total: number;

}

```



De packages/api/src/routers/admin/stats.ts — retorno de getGlobalStats:

```typescript
// Input: { userId?: string, tag?, segment?, startDate?, endDate? }
// Return:
{
  total: number;
  quente: number;
  morno: number;

  frio: number;
  score: number;

  today: number;        // <- mapear para PersonalStats.hoje
  activeVendors: number;

}

```



De apps/web/src/app/dashboard/dashboard.tsx — estado atual relevante:


```typescript

const [selectedVendor, setSelectedVendor] = useState<string | undefined>(undefined);

const effectiveUserId = selectedVendor ?? userId;
// PersonalDashboard recebe: <PersonalDashboard userId={effectiveUserId} />

```


De apps/web/src/app/admin/layout.tsx — padrao correto de getClaims():

```typescript

const { data: claimsData } = await supabase.auth.getClaims();
const userRole = (claimsData?.claims as Record<string, unknown>)?.user_role;
const isAdmin = userRole === "admin";
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar middleware.ts e deletar proxy.ts (AUTH-05)</name>
  <files>
    apps/web/src/middleware.ts (criar)
    apps/web/proxy.ts (deletar)
  </files>
  <read_first>
    - apps/web/proxy.ts — arquivo atual a deletar (ver o config.matcher a copiar)
    - apps/web/src/lib/supabase/proxy.ts — confirmar que updateSession esta exportada (nao alterar)
1   - apps/web/src/app/admin/layout.tsx — referencia de arquivo em src/ que ja funciona (confirmar path @/ funciona)
  </read_first>
  <action>
1. Criar `apps/web/src/middleware.ts` com o conteudo exato abaixo (Next.js exige default export nomeado `middleware`):

1 `typescript
 mport type { NextRequest } from "next/server";
import { updateSession } from "@/lib/supabase/proxy";

 xport default async function middleware(request: NextRequest) {
1 eturn await updateSession(request);


export const config = {
 matcher: [
1 "/((?!_next/static|_next/image|icon.png|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)",
 ],
};
```

1. Deletar `apps/web/proxy.ts` (arquivo na raiz do app — nao e o src/lib/supabase/proxy.ts).

Atencao: `apps/web/src/lib/supabase/proxy.ts` NAO deve ser alterado — ele contem `updateSession` que o novo middleware importa. Apenas o `apps/web/proxy.ts` (raiz, sem `src/`) deve ser deletado.

O `@/` alias em `apps/web/src/middleware.ts` mapeia para `apps/web/src/` (configurado em tsconfig), entao `@/lib/supabase/proxy` resolve para `apps/web/src/lib/supabase/proxy.ts` corretamente.
  </action>
  <verify>
    <automated>ls /home/othavio/Work/sistema-coleta-de-lead/apps/web/src/middleware.ts && echo "middleware.ts existe" && ! test -f /home/othavio/Work/sistema-coleta-de-lead/apps/web/proxy.ts && echo "proxy.ts deletado"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/middleware.ts` existe
    - `apps/web/proxy.ts` NAO existe (arquivo deletado)
    - `apps/web/src/lib/supabase/proxy.ts` ainda existe (nao alterado)
    - `grep -n "export default async function middleware" apps/web/src/middleware.ts` retorna linha com a funcao
    - `grep -n "updateSession" apps/web/src/middleware.ts` retorna a linha de import
    - `grep -n "matcher" apps/web/src/middleware.ts` retorna o config.matcher
    - `grep -rn "proxy.ts" apps/web/src/` NAO retorna nenhuma linha (ninguem importa o arquivo deletado)
  </acceptance_criteria>
  <done>Next.js carrega middleware.ts corretamente: session refresh via updateSession dispara em cada request, e o middleware manifest nao esta vazio.</done>

</task>

<task type="auto">
  <name>Task 2: Corrigir isAdmin para getClaims() e adicionar auth guard em /leads/new</name>
  <files>

    apps/web/src/app/dashboard/page.tsx
    apps/web/src/app/leads/new/page.tsx

  </files>
  <read_first>
    - apps/web/src/app/dashboard/page.tsx — arquivo atual com app_metadata/user_metadata a substituir

    - apps/web/src/app/admin/layout.tsx — padrao canonical de getClaims() a copiar (linhas 23-24)
    - apps/web/src/app/leads/page.tsx — padrao canonical de auth guard server-side a copiar

    - apps/web/src/app/leads/new/page.tsx — arquivo atual sem auth guard
  </read_first>
  <action>

**Fix A — apps/web/src/app/dashboard/page.tsx:**


Substituir o bloco de deteccao de role (linhas 18-21) pelo padrao de getClaims() de admin/layout.tsx.

Estado atual:

 ``typescript
  nst userRole =

  (user.app_metadata as Record<string, unknown>)?.user_role ??
  (user.user_metadata as Record<string, unknown>)?.user_role;
  nst isAdmin = userRole === "admin";
 ``

  bstituir por (exatamente como em admin/layout.tsx linhas 23-24):

 ``typescript
 onst { data: claimsData } = await supabase.auth.getClaims();
  nst userRole = (claimsData?.claims as Record<string, unknown>)?.user_role;
  nst isAdmin = userRole === "admin";


  variavel `supabase` ja existe na linha 9 (`const supabase = await createClient()`). O bloco de auth guard (`if (!user) { redirect("/login"); }`) deve ser mantido antes do getClaims(). O resultado final do arquivo deve ser:

 ``typescript
  port { redirect } from "next/navigation";

   ort FAB from "@/components/fab";
   ort { createClient } from "@/lib/supabase/server";

 mport Dashboard from "./dashboard";

  port default async function DashboardPage() {
  onst supabase = await createClient();
   nst {
   ata: { user },
   = await supabase.auth.getUser();

 if (!user) {
  redirect("/login");


   nst { data: claimsData } = await supabase.auth.getClaims();
  onst userRole = (claimsData?.claims as Record<string, unknown>)?.user_role;
  onst isAdmin = userRole === "admin";

 return (
  <div className="mx-auto w-full max-w-[480px] px-4 pt-8">
   <Dashboard isAdmin={isAdmin} userId={user.id} />
   <FAB />

  </div>
  ;

 ``

 *Fix B — apps/web/src/app/leads/new/page.tsx:**


 onverter para async server component com auth guard. Copiar o padrao exato de `apps/web/src/app/leads/page.tsx`:

 ``typescript
 mport { redirect } from "next/navigation";

 mport { createClient } from "@/lib/supabase/server";
import LeadForm from "@/components/lead-form";


  port default async function NewLeadPage() {
 const supabase = await createClient();
 const {
  data: { user },
 } = await supabase.auth.getUser();


 if (!user) {
  redirect("/login");
 }

 return <LeadForm />;
}
```

  </action>
  <verify>

    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getClaims" apps/web/src/app/dashboard/page.tsx` retorna uma linha com `supabase.auth.getClaims()`
    - `grep -n "app_metadata\|user_metadata" apps/web/src/app/dashboard/page.tsx` NAO retorna nenhuma linha
    - `grep -n "async function DashboardPage" apps/web/src/app/dashboard/page.tsx` — a funcao e async
    - `grep -n "async function NewLeadPage" apps/web/src/app/leads/new/page.tsx` — a funcao e async
    - `grep -n 'redirect("/login")' apps/web/src/app/leads/new/page.tsx` retorna uma linha

    - `grep -n "createClient" apps/web/src/app/leads/new/page.tsx` retorna uma linha
    - `bun run check-types` passa sem erros relacionados a esses arquivos
  </acceptance_criteria>
  <done>dashboard/page.tsx usa getClaims() identico ao admin/layout.tsx. /leads/new redireciona para /login quando nao autenticado. Ambos os arquivos sao async server components com auth guard.</done>
</task>

 task type="auto">
  <name>Task 3: ADMN-07 — overrideStats em PersonalDashboard e vendor stats via tRPC em Dashboard</name>

  <files>
    apps/web/src/app/dashboard/personal-dashboard.tsx
    apps/web/src/app/dashboard/dashboard.tsx
  </files>
  <read_first>
    - apps/web/src/app/dashboard/personal-dashboard.tsx — componente atual com useLiveQuery
    - apps/web/src/app/dashboard/dashboard.tsx — componente atual com selectedVendor state
    - apps/web/src/lib/lead/stats.ts — interface PersonalStats (tipos exatos dos campos)

    - packages/api/src/routers/admin/stats.ts — assinatura de getGlobalStats e campos do retorno (today, nao hoje)
  </read_first>
  <action>
**Fix A — apps/web/src/app/dashboard/personal-dashboard.tsx:**

 dicionar prop opcional `overrideStats: PersonalStats | null` ao componente. Quando fornecida e nao nula, usar diretamente (skip useLiveQuery). Importar `PersonalStats` de `@/lib/lead/stats`.

  componente deve:

 . Aceitar `{ userId, overrideStats }` como props (overrideStats e opcional, default null)
 . Chamar `useLiveQuery` SEMPRE (hooks nao podem ser condicionais) — mas usar `overrideStats ?? stats` para renderizar
 . Mostrar skeleton quando `overrideStats === null && !stats` (estado de loading sem override)

```typescript
 use client";

   ort {
    d,
     Content,
     Header,
    dTitle,
    om "@dashboard-leads-profills/ui/components/card";
     t {
      ChartConfig,
     tContainer,
    rtTooltip,
    rtTooltipContent,
     m "@dashboard-leads-profills/ui/components/chart";
     t { Skeleton } from "@dashboard-leads-profills/ui/components/skeleton";
    rt { useLiveQuery } from "dexie-react-hooks";
    rt { Bar, BarChart, XAxis, YAxis } from "recharts";
     t { StatCard } from "@/components/stat-card";
     t { type PersonalStats, getPersonalStats } from "@/lib/lead/stats";

    t chartConfig: ChartConfig = {
    nte: { label: "Quente", color: "oklch(0.45 0.18 17)" },
     o: { label: "Morno", color: "oklch(0.5 0.13 85)" },
     : { label: "Frio", color: "oklch(0.45 0.15 240)" },


     face PersonalDashboardProps {
     Id: string;
     rideStats?: PersonalStats | null;


     t default function PersonalDashboard({ userId, overrideStats = null }: PersonalDashboardProps) {
     t localStats = useLiveQuery(() => getPersonalStats(userId), [userId]);

     t stats = overrideStats ?? localStats;

     !stats) {
     urn (
     iv aria-busy="true" className="space-y-4">
     div className="grid grid-cols-2 gap-2">
     <Skeleton className="h-20" />
     <Skeleton className="h-20" />
     /div>
     div className="grid grid-cols-3 gap-2">
     <Skeleton className="h-20" />
     <Skeleton className="h-20" />
     <Skeleton className="h-20" />
     /div>
     Skeleton className="h-20" />
     Skeleton className="h-[160px]" />
    /div>



     t chartData = [
    tag: "Quente", count: stats.quente, fill: "oklch(0.45 0.18 17)" },
     ag: "Morno", count: stats.morno, fill: "oklch(0.5 0.13 85)" },
      g: "Frio", count: stats.frio, fill: "oklch(0.45 0.15 240)" },


        (
       className="space-y-4">
      v className="grid grid-cols-2 gap-2">
     StatCard label="Total de Leads" value={stats.total} />
    <StatCard label="Leads Hoje" value={stats.hoje} />
     div>

        className="grid grid-cols-3 gap-2">
       atCard
       assName="[&_span:first-child]:text-[oklch(0.45_0.18_17)] dark:[&_span:first-child]:text-[oklch(0.85_0.12_17)]"
       bel="Quentes"
      alue={stats.quente}
     >
    <StatCard
     className="[&_span:first-child]:text-[oklch(0.5_0.13_85)] dark:[&_span:first-child]:text-[oklch(0.85_0.1_85)]"
      abel="Mornos"

       lue={stats.morno}

       atCard
       assName="[&_span:first-child]:text-[oklch(0.45_0.15_240)] dark:[&_span:first-child]:text-[oklch(0.85_0.1_240)]"
      abel="Frios"
     value={stats.frio}
    />
     div>


       tCard label="Seu Score" value={`${stats.score} pts`} />

       d>
       rdHeader>
      CardTitle>Leads por Tag</CardTitle>
     /CardHeader>
    <CardContent>
     <ChartContainer className="h-[120px] w-full" config={chartConfig}>
      <BarChart data={chartData} layout="vertical">

       <XAxis hide type="number" />
       <YAxis dataKey="tag" type="category" width={60} />
       <ChartTooltip content={<ChartTooltipContent />} />
       <Bar dataKey="count" radius={4} />

      </BarChart>
     </ChartContainer>
    </CardContent>

   </Card>

  </div>

  ;




    x B — apps/web/src/app/dashboard/dashboard.tsx:**

    ionar query condicional para stats do admin quando um vendedor esta selecionado. Mapear o retorno de `getGlobalStats` (campo `today`) para `PersonalStats` (campo `hoje`).



  icionar apos o `vendorsQuery`:

  `typescript
    t adminVendorStatsQuery = useQuery(
    c.admin.stats.getGlobalStats.queryOptions(
    userId: selectedVendor },

    enabled: !!isAdmin && !!selectedVendor }





  nst adminVendorStats: PersonalStats | null =
 isAdmin && selectedVendor && adminVendorStatsQuery.data
  ? {
    total: adminVendorStatsQuery.data.total,
    hoje: adminVendorStatsQuery.data.today,
    quente: adminVendorStatsQuery.data.quente,

    morno: adminVendorStatsQuery.data.morno,
    frio: adminVendorStatsQuery.data.frio,
    score: adminVendorStatsQuery.data.score,

   }

  : null;
  `

  portar `type PersonalStats` de `@/lib/lead/stats`.

 lterar a linha de render do PersonalDashboard de:

  `typescript
   rsonalDashboard userId={effectiveUserId} />


 ara:

 ``typescript
  ersonalDashboard userId={effectiveUserId} overrideStats={adminVendorStats} />


     uivo completo resultante de dashboard.tsx deve ser:

     pescript
     client";

    rt {
   lect,
 SelectContent,
 SelectItem,
 SelectTrigger,
  electValue,
   rom "@dashboard-leads-profills/ui/components/select";
     t {
     ,
     Content,
     List,
     Trigger,
     m "@dashboard-leads-profills/ui/components/tabs";
     t { useQuery } from "@tanstack/react-query";
       { useState } from "react";
       { type PersonalStats } from "@/lib/lead/stats";
       { trpc } from "@/utils/trpc";
       LeaderboardTab from "./leaderboard-tab";
     t PersonalDashboard from "./personal-dashboard";

       ce DashboardProps {
      in?: boolean;
      d: string;


        efault function Dashboard({ userId, isAdmin }: DashboardProps) {
         electedVendor, setSelectedVendor] = useState<string | undefined>(
        ned


       vendorsQuery = useQuery(
     c.admin.leads.listVendors.queryOptions(undefined, {
      bled: !!isAdmin,



       adminVendorStatsQuery = useQuery(
       admin.stats.getGlobalStats.queryOptions(
        rId: selectedVendor },
         led: !!isAdmin && !!selectedVendor }




       adminVendorStats: PersonalStats | null =
     dmin && selectedVendor && adminVendorStatsQuery.data

       tal: adminVendorStatsQuery.data.total,
      oje: adminVendorStatsQuery.data.today,
      uente: adminVendorStatsQuery.data.quente,
       rno: adminVendorStatsQuery.data.morno,
       io: adminVendorStatsQuery.data.frio,
        re: adminVendorStatsQuery.data.score,

        l;

       effectiveUserId = selectedVendor ?? userId;


     rn (
      s defaultValue="dashboard">
        className="flex items-center justify-between gap-4">
      absList>
      TabsTrigger value="dashboard">Meu Dashboard</TabsTrigger>
       absTrigger value="leaderboard">Leaderboard</TabsTrigger>
       absList>
        dmin && vendorsQuery.data ? (
         ect
        ValueChange={(v) =>
       setSelectedVendor(v === "self" || !v ? undefined : v)
      }

      value={selectedVendor ?? "self"}
     >
      <SelectTrigger className="w-[200px]">
       <SelectValue placeholder="Selecionar vendedor" />
      </SelectTrigger>
      <SelectContent>

       <SelectItem value="self">Meu Dashboard</SelectItem>
       {vendorsQuery.data.map((v) => (
        <SelectItem key={v.userId} value={v.userId}>
         {v.name}
        </SelectItem>
       ))}
      </SelectContent>

     </Select>
    ) : null}
   </div>
   <TabsContent className="mt-4" value="dashboard">
    <PersonalDashboard userId={effectiveUserId} overrideStats={adminVendorStats} />
   </TabsContent>

   <TabsContent className="mt-4" value="leaderboard">
    <LeaderboardTab userId={userId} />
   </TabsContent>
  </Tabs>
 );
}
```

  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "overrideStats" apps/web/src/app/dashboard/personal-dashboard.tsx` retorna pelo menos 3 linhas (na interface, no parametro, e no uso)

    - `grep -n "overrideStats ?? localStats" apps/web/src/app/dashboard/personal-dashboard.tsx` retorna uma linha
    - `grep -n "PersonalStats" apps/web/src/app/dashboard/personal-dashboard.tsx` retorna a importacao do tipo
    - `grep -n "getGlobalStats" apps/web/src/app/dashboard/dashboard.tsx` retorna uma linha com a query
    - `grep -n "adminVendorStats" apps/web/src/app/dashboard/dashboard.tsx` retorna pelo menos 2 linhas
    - `grep -n "overrideStats={adminVendorStats}" apps/web/src/app/dashboard/dashboard.tsx` retorna uma linha
    - `grep -n "hoje: adminVendorStatsQuery.data.today" apps/web/src/app/dashboard/dashboard.tsx` retorna uma linha (mapeamento today->hoje)
    - `bun run check-types` passa sem erros de tipo
  </acceptance_criteria>
  <done>Admin que seleciona outro vendedor no dropdown ve os stats reais desse vendedor carregados via tRPC (nao zeros do Dexie local). Vendedor normal continua usando Dexie via useLiveQuery sem alteracao de comportamento.</done>
</task>

</tasks>

<verification>

Apos execucao das 3 tasks, verificar:

1. `ls apps/web/src/middleware.ts` — arquivo existe
2. `! test -f apps/web/proxy.ts` — arquivo deletado
3. `grep "export default async function middleware" apps/web/src/middleware.ts` — default export correto
4. `grep "getClaims" apps/web/src/app/dashboard/page.tsx` — padrao consistente com admin layout
5. `grep 'redirect("/login")' apps/web/src/app/leads/new/page.tsx` — auth guard presente
6. `grep "overrideStats" apps/web/src/app/dashboard/personal-dashboard.tsx` — prop adicionada
7. `grep "getGlobalStats" apps/web/src/app/dashboard/dashboard.tsx` — query condicional presente
8. `bun run check-types` — sem erros de tipo
9. `bun run check` — sem erros de lint/format
</verification>

<success_criteria>

- AUTH-05 FECHADO: `apps/web/src/middleware.ts` existe com `export default async function middleware`, `apps/web/proxy.ts` foi deletado
- ADMN-07 FECHADO: Admin seleciona vendedor, Dashboard chama `trpc.admin.stats.getGlobalStats({ userId: selectedVendor })`, resultado mapeado para `PersonalStats` (today -> hoje) e passado como `overrideStats` para PersonalDashboard
- Consistencia isAdmin: `dashboard/page.tsx` usa `getClaims()` identico a `admin/layout.tsx` — sem mais `app_metadata`/`user_metadata`
- Auth guard /leads/new: `/leads/new` e async server component com `getUser()` + `redirect("/login")` identico a `/leads/page.tsx`
- `bun run check-types` e `bun run check` passam sem erros
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/07-auth-admin-fixes/07-01-SUMMARY.md` seguindo o template em `.claude/get-shit-done/templates/summary.md`.
</output>
