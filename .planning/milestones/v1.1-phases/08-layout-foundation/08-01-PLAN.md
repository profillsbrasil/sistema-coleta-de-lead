---
phase: 08-layout-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(public)/login/page.tsx
  - apps/web/src/app/(public)/layout.tsx
  - apps/web/src/app/(public)/page.tsx
  - apps/web/src/app/(app)/dashboard/page.tsx
  - apps/web/src/app/(app)/dashboard/dashboard.tsx
  - apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx
  - apps/web/src/app/(app)/dashboard/personal-dashboard.tsx
  - apps/web/src/app/(app)/leads/page.tsx
  - apps/web/src/app/(app)/leads/lead-list.tsx
  - apps/web/src/app/(app)/leads/new/page.tsx
  - apps/web/src/app/(app)/leads/[id]/page.tsx
  - apps/web/src/app/(app)/leads/[id]/lead-detail.tsx
  - apps/web/src/app/(app)/admin/page.tsx
  - apps/web/src/app/(app)/admin/leads/page.tsx
  - apps/web/src/app/(app)/admin/leads/leads-panel.tsx
  - apps/web/src/app/(app)/admin/leads/[id]/page.tsx
  - apps/web/src/app/(app)/admin/users/page.tsx
  - apps/web/src/app/(app)/admin/stats/page.tsx
autonomous: true
requirements:
  - LAYOUT-02
  - MOBILE-03

must_haves:
  truths:
    - "Login page at /login renders without sidebar (inside (public) route group)"
    - "Home page / redirects to /dashboard if logged in, /login if not"
    - "All authenticated pages (/dashboard, /leads, /admin/*) are inside (app) route group"
    - "/todos page no longer exists"
    - "/auth/callback stays at root, outside route groups"
  artifacts:
    - path: "apps/web/src/app/(public)/layout.tsx"
      provides: "Public layout without sidebar"
      contains: "children"
    - path: "apps/web/src/app/(public)/login/page.tsx"
      provides: "Login page in public group"
      contains: "LoginCard"
    - path: "apps/web/src/app/(public)/page.tsx"
      provides: "Home redirect"
      contains: "redirect"
    - path: "apps/web/src/app/(app)/dashboard/page.tsx"
      provides: "Dashboard in app group"
      contains: "Dashboard"
    - path: "apps/web/src/app/(app)/leads/page.tsx"
      provides: "Leads in app group"
      contains: "LeadList"
    - path: "apps/web/src/app/(app)/admin/page.tsx"
      provides: "Admin redirect in app group"
      contains: "redirect"
  key_links:
    - from: "apps/web/src/app/(public)/page.tsx"
      to: "/dashboard and /login"
      via: "server-side redirect based on auth check"
      pattern: "redirect.*dashboard|redirect.*login"
---

<objective>
Create route groups (public) and (app), move all pages into their correct groups, rewrite home page as redirect, and delete /todos.

Purpose: Separate public pages (login) from authenticated pages (dashboard, leads, admin) at the file system level so each group can have its own layout — no conditional rendering in root layout (per D-01 through D-05, LAYOUT-02, MOBILE-03).

Output: All pages relocated into route groups. Home page becomes a redirect. /todos deleted.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-layout-foundation/08-CONTEXT.md
@.planning/phases/08-layout-foundation/08-RESEARCH.md
@.planning/phases/08-layout-foundation/08-UI-SPEC.md

<interfaces>
<!-- Key files the executor needs to understand before moving -->

From apps/web/src/app/layout.tsx (root — NOT modified in this plan):
```typescript
// Root layout stays untouched in Plan 01 — cleanup happens in Plan 03
import Header from "@/components/header";
import Providers from "@/components/providers";
// grid wrapper + Header will be removed in Plan 03
```

From apps/web/src/app/login/page.tsx (to be moved as-is):
```typescript
import { Suspense } from "react";
import LoginCard from "@/components/login-card";
// Uses @/components/login-card — import path stays the same after move
```

From apps/web/src/app/dashboard/page.tsx (to be moved, auth guard stays for now):
```typescript
import { redirect } from "next/navigation";
import FAB from "@/components/fab";
import { createClient } from "@/lib/supabase/server";
import Dashboard from "./dashboard"; // relative import — works after move
```

From apps/web/src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create route groups and move all pages</name>
  <files>
    apps/web/src/app/(public)/layout.tsx
    apps/web/src/app/(public)/login/page.tsx
    apps/web/src/app/(public)/page.tsx
    apps/web/src/app/(app)/dashboard/page.tsx
    apps/web/src/app/(app)/dashboard/dashboard.tsx
    apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx
    apps/web/src/app/(app)/dashboard/personal-dashboard.tsx
    apps/web/src/app/(app)/leads/page.tsx
    apps/web/src/app/(app)/leads/lead-list.tsx
    apps/web/src/app/(app)/leads/new/page.tsx
    apps/web/src/app/(app)/leads/[id]/page.tsx
    apps/web/src/app/(app)/leads/[id]/lead-detail.tsx
    apps/web/src/app/(app)/admin/page.tsx
    apps/web/src/app/(app)/admin/leads/page.tsx
    apps/web/src/app/(app)/admin/leads/leads-panel.tsx
    apps/web/src/app/(app)/admin/leads/[id]/page.tsx
    apps/web/src/app/(app)/admin/users/page.tsx
    apps/web/src/app/(app)/admin/users/users-panel.tsx
    apps/web/src/app/(app)/admin/stats/page.tsx
    apps/web/src/app/(app)/admin/stats/stats-panel.tsx
  </files>
  <read_first>
    apps/web/src/app/layout.tsx
    apps/web/src/app/page.tsx
    apps/web/src/app/login/page.tsx
    apps/web/src/app/dashboard/page.tsx
    apps/web/src/app/dashboard/dashboard.tsx
    apps/web/src/app/leads/page.tsx
    apps/web/src/app/leads/lead-list.tsx
    apps/web/src/app/leads/new/page.tsx
    apps/web/src/app/leads/[id]/page.tsx
    apps/web/src/app/leads/[id]/lead-detail.tsx
    apps/web/src/app/admin/page.tsx
    apps/web/src/app/admin/layout.tsx
    apps/web/src/app/admin/leads/page.tsx
    apps/web/src/app/admin/leads/leads-panel.tsx
    apps/web/src/app/admin/users/page.tsx
    apps/web/src/app/admin/stats/page.tsx
    apps/web/src/app/todos/page.tsx
    apps/web/src/lib/supabase/server.ts
  </read_first>
  <action>
    **Step 1: Create (public) route group directory and files**

    1a. Create `apps/web/src/app/(public)/layout.tsx` — pass-through layout (per D-01, Research Pattern 4). Login page already has its own centering (`flex min-h-svh items-center justify-center p-4`), so public layout is a pure pass-through:
    ```typescript
    export default function PublicLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return <>{children}</>;
    }
    ```

    1b. Move `apps/web/src/app/login/page.tsx` to `apps/web/src/app/(public)/login/page.tsx` — EXACT same content, no changes. Uses `@/components/login-card` which is an alias import that works regardless of route group nesting.

    1c. Create `apps/web/src/app/(public)/page.tsx` — home page rewritten as redirect (per D-02, Research Pattern 5):
    ```typescript
    import { redirect } from "next/navigation";
    import { createClient } from "@/lib/supabase/server";

    export default async function HomePage() {
      const supabase = await createClient();
      const {
        data: { user },
      } = await supabase.auth.getUser();

      if (user) {
        redirect("/dashboard");
      }

      redirect("/login");
    }
    ```

    **Step 2: Create (app) route group and move authenticated pages**

    2a. Create directory structure:
    - `apps/web/src/app/(app)/dashboard/`
    - `apps/web/src/app/(app)/leads/`
    - `apps/web/src/app/(app)/leads/new/`
    - `apps/web/src/app/(app)/leads/[id]/`
    - `apps/web/src/app/(app)/admin/`
    - `apps/web/src/app/(app)/admin/leads/`
    - `apps/web/src/app/(app)/admin/leads/[id]/`
    - `apps/web/src/app/(app)/admin/users/`
    - `apps/web/src/app/(app)/admin/stats/`

    2b. Move dashboard files (ALL files in the directory, not just page.tsx):
    - `app/dashboard/page.tsx` -> `app/(app)/dashboard/page.tsx` (keep auth guard for now — Plan 02 centralizes it)
    - `app/dashboard/dashboard.tsx` -> `app/(app)/dashboard/dashboard.tsx`
    - `app/dashboard/leaderboard-tab.tsx` -> `app/(app)/dashboard/leaderboard-tab.tsx`
    - `app/dashboard/personal-dashboard.tsx` -> `app/(app)/dashboard/personal-dashboard.tsx`
    All use relative imports (`./dashboard`, `./leaderboard-tab`) that work after the move.

    2c. Move leads files:
    - `app/leads/page.tsx` -> `app/(app)/leads/page.tsx`
    - `app/leads/lead-list.tsx` -> `app/(app)/leads/lead-list.tsx`
    - `app/leads/new/page.tsx` -> `app/(app)/leads/new/page.tsx`
    - `app/leads/[id]/page.tsx` -> `app/(app)/leads/[id]/page.tsx`
    - `app/leads/[id]/lead-detail.tsx` -> `app/(app)/leads/[id]/lead-detail.tsx`
    All unchanged — relative imports and `@/` alias imports both work.

    2d. Move admin files (keeping admin/layout.tsx for now — deleted in Plan 03):
    - `app/admin/layout.tsx` -> `app/(app)/admin/layout.tsx` (TEMPORARY — Plan 03 replaces this)
    - `app/admin/page.tsx` -> `app/(app)/admin/page.tsx`
    - `app/admin/leads/page.tsx` -> `app/(app)/admin/leads/page.tsx`
    - `app/admin/leads/leads-panel.tsx` -> `app/(app)/admin/leads/leads-panel.tsx`
    - `app/admin/leads/[id]/page.tsx` -> `app/(app)/admin/leads/[id]/page.tsx`
    - `app/admin/leads/[id]/` -> check for all files in this dir and move ALL of them
    - `app/admin/users/page.tsx` -> `app/(app)/admin/users/page.tsx`
    - `app/admin/users/` -> check for all files in this dir and move ALL of them (e.g., users-panel.tsx)
    - `app/admin/stats/page.tsx` -> `app/(app)/admin/stats/page.tsx`
    - `app/admin/stats/` -> check for all files in this dir and move ALL of them (e.g., stats-panel.tsx)
    All unchanged.

    **Step 3: Delete old pages and /todos**

    3a. Delete `apps/web/src/app/page.tsx` (old home page — per D-03)
    3b. Delete `apps/web/src/app/todos/page.tsx` (per D-03 — not useful in product)
    3c. Delete `apps/web/src/app/todos/` directory entirely
    3d. Delete `apps/web/src/app/login/` directory (moved to (public))
    3e. Delete `apps/web/src/app/dashboard/` directory (moved to (app))
    3f. Delete `apps/web/src/app/leads/` directory (moved to (app))
    3g. Delete `apps/web/src/app/admin/` directory (moved to (app))

    **Step 4: Verify /auth/callback stays at root**

    Confirm `apps/web/src/app/auth/callback/route.ts` remains untouched at the root level (per D-04). Do NOT move it into any route group.

    Confirm `apps/web/src/app/api/trpc/[trpc]/route.ts` remains untouched at the root level. Do NOT move API routes.

    **Step 5: Delete .next cache and verify**

    Delete `.next` directory to clear stale route type cache (per Research Pitfall 5). Then run type check.

    **IMPORTANT:** Use `git mv` for file moves to preserve git history. For new files (public layout, home redirect), create with Write tool.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && rm -rf apps/web/.next && bun run check-types && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/(public)/layout.tsx` exists and contains `children`
    - `apps/web/src/app/(public)/login/page.tsx` exists and contains `LoginCard`
    - `apps/web/src/app/(public)/page.tsx` exists and contains `redirect("/dashboard")` and `redirect("/login")`
    - `apps/web/src/app/(app)/dashboard/page.tsx` exists and contains `Dashboard`
    - `apps/web/src/app/(app)/leads/page.tsx` exists and contains `LeadList`
    - `apps/web/src/app/(app)/leads/new/page.tsx` exists and contains `LeadForm`
    - `apps/web/src/app/(app)/admin/page.tsx` exists and contains `redirect("/admin/leads")`
    - `apps/web/src/app/(app)/admin/layout.tsx` exists (temporarily, until Plan 03)
    - `apps/web/src/app/auth/callback/route.ts` exists (not moved)
    - `apps/web/src/app/api/trpc/[trpc]/route.ts` exists (not moved)
    - `apps/web/src/app/todos/` does NOT exist
    - `apps/web/src/app/page.tsx` does NOT exist (moved to (public)/page.tsx)
    - `apps/web/src/app/login/` does NOT exist (moved to (public)/login/)
    - `apps/web/src/app/dashboard/` does NOT exist (moved to (app)/dashboard/)
    - `apps/web/src/app/leads/` does NOT exist (moved to (app)/leads/)
    - `apps/web/src/app/admin/` does NOT exist (moved to (app)/admin/)
    - `bun run check-types` exits 0
    - `bun run check` exits 0
  </acceptance_criteria>
  <done>Route groups (public) and (app) exist. Login in (public), all authenticated pages in (app). Home redirects based on auth. /todos deleted. /auth/callback and /api/ stay at root. Type check passes.</done>
</task>

</tasks>

<verification>
- `ls apps/web/src/app/(public)/login/page.tsx` succeeds
- `ls apps/web/src/app/(app)/dashboard/page.tsx` succeeds
- `ls apps/web/src/app/(app)/admin/page.tsx` succeeds
- `test ! -d apps/web/src/app/todos` succeeds
- `test ! -f apps/web/src/app/page.tsx` succeeds
- `grep redirect apps/web/src/app/(public)/page.tsx` returns matches for both /dashboard and /login
- `bun run check-types` exits 0
- `bun run check` exits 0
</verification>

<success_criteria>
All pages live in correct route groups. No pages remain at the old locations. Type check and lint pass. /auth/callback and /api/ untouched at root.
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-foundation/08-01-SUMMARY.md`
</output>
