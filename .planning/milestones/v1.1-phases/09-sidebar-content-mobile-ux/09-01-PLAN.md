---
phase: 09-sidebar-content-mobile-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/gravatar.ts
  - apps/web/src/components/sidebar-user-menu.tsx
  - apps/web/src/app/(app)/layout.tsx
  - apps/web/src/components/app-sidebar.tsx
autonomous: true
requirements:
  - LAYOUT-04
  - LAYOUT-08
  - MOBILE-01
  - MOBILE-02
  - MOBILE-04
  - MOBILE-05
  - TOUCH-01
  - TOUCH-04
  - POLISH-02

must_haves:
  truths:
    - "Vendedor logado ve grupo Vendedor na sidebar (Dashboard, Leads, Novo Lead) e NAO ve grupo Admin"
    - "Admin logado ve ambos os grupos: Vendedor e Admin (collapsible) com itens corretos"
    - "No mobile, clicar em qualquer nav link fecha o drawer automaticamente"
    - "Footer da sidebar exibe avatar + nome + role do usuario logado com botao Sair e ModeToggle"
    - "Nav item da rota atual aparece highlighted — incluindo rotas aninhadas como /admin/leads/123"
    - "Touch targets dos nav items tem minimo 44px de altura"
    - "Sidebar desktop permanece sempre expandida (sem collapse, sem icon-only)"
    - "ModeToggle (sun/moon) no SidebarFooter alterna entre light/dark/system theme"
  artifacts:
    - path: "apps/web/src/lib/gravatar.ts"
      provides: "Gravatar URL generator using SHA-256 hash"
      exports: ["getGravatarUrl"]
    - path: "apps/web/src/components/sidebar-user-menu.tsx"
      provides: "User menu component for SidebarFooter with ModeToggle"
      exports: ["default"]
      min_lines: 30
    - path: "apps/web/src/components/app-sidebar.tsx"
      provides: "AppSidebar with user menu, ModeToggle, auto-close, touch targets, active state"
      exports: ["default"]
    - path: "apps/web/src/app/(app)/layout.tsx"
      provides: "App layout passing user data props to AppSidebar"
  key_links:
    - from: "apps/web/src/app/(app)/layout.tsx"
      to: "apps/web/src/components/app-sidebar.tsx"
      via: "userName, userEmail, userRole, gravatarUrl props"
      pattern: "userName=|userEmail=|userRole=|gravatarUrl="
    - from: "apps/web/src/components/app-sidebar.tsx"
      to: "apps/web/src/components/sidebar-user-menu.tsx"
      via: "SidebarUserMenu rendered inside SidebarFooter"
      pattern: "SidebarUserMenu"
    - from: "apps/web/src/app/(app)/layout.tsx"
      to: "apps/web/src/lib/gravatar.ts"
      via: "getGravatarUrl called server-side"
      pattern: "getGravatarUrl"
    - from: "apps/web/src/components/app-sidebar.tsx"
      to: "useSidebar().setOpenMobile(false)"
      via: "useEffect on pathname change"
      pattern: "setOpenMobile\\(false\\)"
    - from: "apps/web/src/components/sidebar-user-menu.tsx"
      to: "next-themes useTheme()"
      via: "ModeToggle icon button toggles theme"
      pattern: "useTheme|setTheme"
---

<objective>
Implementar sidebar completa com navegacao por role, user menu no footer (com ModeToggle), auto-close do drawer mobile, touch targets 44px, e active state refinado para rotas aninhadas.

Purpose: Sidebar fica totalmente funcional para vendedores e admins, com UX mobile polida e identidade do usuario visivel no footer. ModeToggle migrado para SidebarFooter completa LAYOUT-08.
Output: gravatar.ts utility, sidebar-user-menu.tsx component, app-sidebar.tsx e layout.tsx atualizados.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-sidebar-content-mobile-ux/09-CONTEXT.md
@.planning/phases/09-sidebar-content-mobile-ux/09-RESEARCH.md
@.planning/phases/09-sidebar-content-mobile-ux/09-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/ui/src/components/sidebar.tsx (exports):
```typescript
export {
  Sidebar, SidebarContent, SidebarFooter, SidebarGroup, SidebarGroupLabel,
  SidebarHeader, SidebarMenu, SidebarMenuButton, SidebarMenuItem,
  SidebarProvider, SidebarInset, SidebarTrigger, useSidebar
};
// useSidebar() returns: { setOpenMobile: (open: boolean) => void, isMobile: boolean, ... }
```

From packages/ui/src/components/avatar.tsx:
```typescript
export { Avatar, AvatarBadge, AvatarFallback, AvatarGroup, AvatarGroupCount, AvatarImage };
// Avatar accepts size?: "default" | "sm" | "lg" (default = size-8 = 32px)
// AvatarFallback renders bg-muted text-muted-foreground by default
```

From packages/ui/src/components/button.tsx:
```typescript
export { Button, buttonVariants };
// size variants: "default" | "xs" | "sm" | "lg" | "icon" (size-8) | "icon-xs" (size-6) | "icon-sm" (size-7) | "icon-lg" (size-9)
// variant: "default" | "outline" | "secondary" | "ghost" | "destructive" | "link"
```

From apps/web/src/components/app-sidebar.tsx (current):
```typescript
interface AppSidebarProps { isAdmin: boolean; }
export default function AppSidebar({ isAdmin }: AppSidebarProps)
// Uses: usePathname(), Sidebar collapsible="offcanvas", VENDEDOR_ITEMS, ADMIN_ITEMS
// SidebarFooter is empty: <SidebarFooter />
```

From apps/web/src/app/(app)/layout.tsx (current):
```typescript
export default async function AppLayout({ children }: { children: React.ReactNode })
// Calls: supabase.auth.getUser(), supabase.auth.getClaims()
// Passes: <AppSidebar isAdmin={isAdmin} />
```

From apps/web/src/lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient
// Browser-side Supabase client for signOut
```

From apps/web/src/components/mode-toggle.tsx (existing, to be adapted):
```typescript
export function ModeToggle()
// Uses: useTheme() from "next-themes", Sun/Moon from "lucide-react"
// Current: DropdownMenu with Light/Dark/System options
// Phase 9: Simplify to icon button that cycles light -> dark -> system
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar gravatar.ts utility e sidebar-user-menu.tsx component (com ModeToggle)</name>
  <files>apps/web/src/lib/gravatar.ts, apps/web/src/components/sidebar-user-menu.tsx</files>
  <read_first>
    - packages/ui/src/components/avatar.tsx
    - packages/ui/src/components/button.tsx
    - packages/ui/src/components/sidebar.tsx (lines 697-722, exports)
    - apps/web/src/lib/supabase/client.ts
    - apps/web/src/components/user-menu.tsx (reference for signOut pattern)
    - apps/web/src/components/mode-toggle.tsx (reference for theme toggle pattern)
  </read_first>
  <action>
**Arquivo 1: `apps/web/src/lib/gravatar.ts`**

Criar utility async que gera Gravatar URL usando SHA-256 hash do email (per D-04 updated: SHA-256). Sera chamado server-side no layout.

```typescript
export async function getGravatarUrl(email: string, size = 80): Promise<string> {
  const trimmed = email.trim().toLowerCase();
  const encoder = new TextEncoder();
  const data = encoder.encode(trimmed);
  const hashBuffer = await crypto.subtle.digest("SHA-256", data);
  const hashArray = Array.from(new Uint8Array(hashBuffer));
  const hashHex = hashArray.map((b) => b.toString(16).padStart(2, "0")).join("");
  return `https://gravatar.com/avatar/${hashHex}?s=${size}&d=404`;
}
```

Key details:
- SHA-256 via Web Crypto API (zero dependencies) -- per D-04 (updated from MD5 to SHA-256)
- `d=404` makes Gravatar return 404 when no avatar exists, triggering AvatarFallback
- Function is async because `crypto.subtle.digest` returns Promise
- Exported as named export `getGravatarUrl`

**Arquivo 2: `apps/web/src/components/sidebar-user-menu.tsx`**

Criar client component que exibe avatar + nome + role badge + ModeToggle + botao logout no SidebarFooter (per D-03, D-04, D-05, D-06, LAYOUT-08).

Props interface:
```typescript
interface SidebarUserMenuProps {
  userName: string;
  userEmail: string;
  userRole: string;
  gravatarUrl: string;
}
```

Implementation details:
- `"use client"` directive no topo
- Helper `getInitials(name: string): string` -- split por espaco, pegar primeira letra do primeiro e ultimo nome, uppercase, max 2 chars. Se nome unico, pegar 2 primeiras letras
- Avatar: `<Avatar>` default size (size-8 = 32px), `<AvatarImage src={gravatarUrl} alt={userName} />`, `<AvatarFallback>{getInitials(userName)}</AvatarFallback>`
- Layout: flex horizontal `flex items-center gap-3 px-2 py-2`
  - Avatar (32x32)
  - Div com nome + role empilhados (`flex min-w-0 flex-1 flex-col`)
    - Nome: `<span className="truncate text-sm font-semibold">{userName}</span>` (per UI-SPEC: text-sm font-semibold)
    - Role: `<span className="truncate text-xs text-muted-foreground">{userRole === "admin" ? "Admin" : "Vendedor"}</span>` (per D-06)
  - **ModeToggle icon button (per LAYOUT-08):** `<Button variant="ghost" size="icon-sm" onClick={handleToggleTheme} aria-label="Alternar tema">` com Sun/Moon icons alternando via `useTheme()` de `next-themes`. Implementar como cycling toggle: light -> dark -> system -> light. Usar `resolvedTheme` para decidir qual icone exibir (Sun para dark, Moon para light). NAO usar DropdownMenu -- botao simples que cicla.
    ```typescript
    const { setTheme, resolvedTheme } = useTheme();
    function handleToggleTheme() {
      setTheme(resolvedTheme === "dark" ? "light" : "dark");
    }
    ```
    Icon rendering: `{resolvedTheme === "dark" ? <Sun /> : <Moon />}` (mostra Sun no dark mode = "clique para ir para light", mostra Moon no light mode = "clique para ir para dark"). Usar `mounted` state pattern para evitar hydration mismatch:
    ```typescript
    const [mounted, setMounted] = useState(false);
    useEffect(() => setMounted(true), []);
    // Render placeholder icon or null when !mounted
    ```
  - Botao logout: `<Button variant="ghost" size="icon-sm" onClick={handleSignOut} aria-label="Sair"><LogOut /></Button>` (per D-03: inline, 1 clique)
- Logout handler: `const router = useRouter()` + `async function handleSignOut()` que chama `createClient().auth.signOut()` e depois `router.push("/login")` (per RESEARCH Pitfall 3)
- Wrapped em `<SidebarMenu><SidebarMenuItem>...</SidebarMenuItem></SidebarMenu>` (shadcn sidebar footer pattern)
- Importar: Avatar/AvatarImage/AvatarFallback de `@dashboard-leads-profills/ui/components/avatar`, Button de `@dashboard-leads-profills/ui/components/button`, SidebarMenu/SidebarMenuItem de `@dashboard-leads-profills/ui/components/sidebar`, LogOut/Sun/Moon de `lucide-react`, useRouter de `next/navigation`, createClient de `@/lib/supabase/client`, useTheme de `next-themes`, useState/useEffect de `react`
- NO sizing classes on LogOut/Sun/Moon icons inside Button (shadcn skill rule)
- NO DropdownMenu for either logout or theme toggle (per D-03 for logout, simplicity for theme)
- Default export: `export default function SidebarUserMenu(...)`

Footer row layout summary: `[Avatar] [Name+Role] [ModeToggle btn] [Logout btn]`
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/gravatar.ts` exists and contains `export async function getGravatarUrl(`
    - `apps/web/src/lib/gravatar.ts` contains `crypto.subtle.digest("SHA-256"`
    - `apps/web/src/lib/gravatar.ts` contains `d=404`
    - `apps/web/src/components/sidebar-user-menu.tsx` exists and contains `"use client"`
    - `sidebar-user-menu.tsx` contains `export default function SidebarUserMenu(`
    - `sidebar-user-menu.tsx` contains `interface SidebarUserMenuProps`
    - `sidebar-user-menu.tsx` contains `gravatarUrl: string`
    - `sidebar-user-menu.tsx` contains `<AvatarFallback>` (shadcn rule: Avatar always needs AvatarFallback)
    - `sidebar-user-menu.tsx` contains `<AvatarImage src={gravatarUrl}`
    - `sidebar-user-menu.tsx` contains `aria-label="Sair"` (accessibility)
    - `sidebar-user-menu.tsx` contains `aria-label="Alternar tema"` (accessibility for ModeToggle)
    - `sidebar-user-menu.tsx` contains `router.push("/login")` (redirect after signOut)
    - `sidebar-user-menu.tsx` does NOT contain `DropdownMenu` (per D-03)
    - `sidebar-user-menu.tsx` contains `variant="ghost"` on logout Button
    - `sidebar-user-menu.tsx` contains `size="icon-sm"` on both ModeToggle and logout Buttons
    - `sidebar-user-menu.tsx` contains `function getInitials(`
    - `sidebar-user-menu.tsx` contains `useTheme` import from `next-themes`
    - `sidebar-user-menu.tsx` contains `setTheme` call (theme toggle)
    - `sidebar-user-menu.tsx` contains `<Sun` and `<Moon` (Lucide icons for theme toggle)
    - `bun run check-types` exits 0
  </acceptance_criteria>
  <done>gravatar.ts utility exporta getGravatarUrl com SHA-256 hash e d=404. sidebar-user-menu.tsx renderiza avatar (Gravatar + fallback iniciais), nome (truncate, semibold), role (Admin/Vendedor), ModeToggle (Sun/Moon icon button que alterna light/dark), e botao logout inline. Type check passa.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar layout.tsx e app-sidebar.tsx — user props, auto-close, touch targets, active state</name>
  <files>apps/web/src/app/(app)/layout.tsx, apps/web/src/components/app-sidebar.tsx</files>
  <read_first>
    - apps/web/src/app/(app)/layout.tsx
    - apps/web/src/components/app-sidebar.tsx
    - apps/web/src/components/sidebar-user-menu.tsx (created in Task 1)
    - apps/web/src/lib/gravatar.ts (created in Task 1)
    - packages/ui/src/components/sidebar.tsx (lines 36-55, useSidebar hook definition)
  </read_first>
  <action>
**Arquivo 1: `apps/web/src/app/(app)/layout.tsx`**

Modificar o Server Component layout para extrair user data e passar como props ao AppSidebar (per D-05: zero client-side fetch).

Mudancas especificas:

1. Adicionar import: `import { getGravatarUrl } from "@/lib/gravatar";`

2. Apos o bloco existente de `getClaims()` e `isAdmin`, adicionar extracao de user data:
```typescript
const claims = claimsData?.claims as Record<string, unknown>;
const userRole = (claims?.user_role as string) ?? "vendedor";
const userName = (user?.user_metadata?.full_name as string) ?? user?.email?.split("@")[0] ?? "Usuario";
const userEmail = user?.email ?? "";
const gravatarUrl = await getGravatarUrl(userEmail);
```

Note: A variavel `userRole` ja existe no layout atual como `const userRole = (claimsData?.claims as Record<string, unknown>)?.user_role;`. Refatorar para extrair `claims` primeiro e depois derivar `userRole` como string com fallback "vendedor". Manter `isAdmin` derivado de `userRole`.

3. Atualizar o JSX do AppSidebar para passar as novas props:
```tsx
<AppSidebar
  isAdmin={isAdmin}
  userName={userName}
  userEmail={userEmail}
  userRole={userRole}
  gravatarUrl={gravatarUrl}
/>
```

O layout final deve ter esta estrutura de dados:
- `user` vem de `getUser()` (ja existe)
- `claims` vem de `getClaims()` (ja existe parcialmente)
- `userRole` = string com fallback "vendedor"
- `isAdmin` = `userRole === "admin"` (ja existe)
- `userName` = full_name ?? email prefix ?? "Usuario"
- `userEmail` = email ?? ""
- `gravatarUrl` = await getGravatarUrl(userEmail) (async, OK pois e Server Component)

**Arquivo 2: `apps/web/src/components/app-sidebar.tsx`**

Modificar o Client Component AppSidebar para receber user props, adicionar auto-close mobile, touch targets 44px, e active state refinado.

Mudancas especificas:

1. **Expandir interface AppSidebarProps:**
```typescript
interface AppSidebarProps {
  isAdmin: boolean;
  userName: string;
  userEmail: string;
  userRole: string;
  gravatarUrl: string;
}
```

2. **Adicionar imports:**
- `import { useEffect } from "react";`
- `import { useSidebar } from "@dashboard-leads-profills/ui/components/sidebar";` (adicionar `useSidebar` ao import existente de sidebar)
- `import SidebarUserMenu from "@/components/sidebar-user-menu";`

3. **Adicionar mobile auto-close (per D-07):**
Dentro do componente AppSidebar, APOS a linha `const pathname = usePathname();`, adicionar:
```typescript
const { setOpenMobile } = useSidebar();

useEffect(() => {
  setOpenMobile(false);
}, [pathname, setOpenMobile]);
```
Isso fecha o drawer mobile automaticamente quando pathname muda. `setOpenMobile` e no-op no desktop (per RESEARCH Pattern 2).

4. **Touch targets 44px nos nav items (per D-09):**
Adicionar `className="min-h-11"` a TODOS os `SidebarMenuButton` dos VENDEDOR_ITEMS e ADMIN_ITEMS. `min-h-11` = 44px = 2.75rem (WCAG 2.5.5 minimum).

De:
```tsx
<SidebarMenuButton
  isActive={pathname.startsWith(href)}
  render={<Link href={href as unknown as "/"} />}
>
```

Para:
```tsx
<SidebarMenuButton
  className="min-h-11"
  isActive={/* active state logic */}
  render={<Link href={href as unknown as "/"} />}
>
```

Aplicar em AMBOS os loops: VENDEDOR_ITEMS e ADMIN_ITEMS.

5. **Active state refinado (per UI-SPEC Interaction Contract #3 e RESEARCH Pitfall 1):**

Para VENDEDOR_ITEMS, usar logica especifica para `/leads` (exact match) vs outros items (startsWith):
```typescript
const isActive = href === "/leads"
  ? pathname === "/leads"
  : pathname.startsWith(href);
```

Isso evita que `/leads` e `/leads/new` fiquem ambos highlighted simultaneamente. Os outros items (`/dashboard`, `/leads/new`) continuam usando `startsWith` normalmente.

Para ADMIN_ITEMS, manter `pathname.startsWith(href)` (nao tem conflito de prefix entre `/admin/leads`, `/admin/users`, `/admin/stats`).

Implementar como helper function dentro do componente ou inline. Exemplo:
```typescript
function isItemActive(href: string, pathname: string): boolean {
  if (href === "/leads") return pathname === "/leads";
  return pathname.startsWith(href);
}
```

6. **Render SidebarUserMenu no footer:**
Substituir `<SidebarFooter />` (self-closing, vazio) por:
```tsx
<SidebarFooter>
  <SidebarUserMenu
    userName={userName}
    userEmail={userEmail}
    userRole={userRole}
    gravatarUrl={gravatarUrl}
  />
</SidebarFooter>
```

7. **Destructure novas props:**
```typescript
export default function AppSidebar({ isAdmin, userName, userEmail, userRole, gravatarUrl }: AppSidebarProps) {
```

8. **NAO adicionar collapse desktop, Ctrl+B, ou cookie persistence** (per D-01 -- CANCELADO). Manter `collapsible="offcanvas"` (per D-02).

Note: ModeToggle JA esta incluido dentro de SidebarUserMenu (Task 1). NAO importar ModeToggle separadamente no app-sidebar.tsx -- ele e renderizado internamente pelo SidebarUserMenu component.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -20 && bun run check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/(app)/layout.tsx` contains `import { getGravatarUrl } from "@/lib/gravatar"`
    - `layout.tsx` contains `await getGravatarUrl(`
    - `layout.tsx` contains `userName={userName}`
    - `layout.tsx` contains `userEmail={userEmail}`
    - `layout.tsx` contains `userRole={userRole}`
    - `layout.tsx` contains `gravatarUrl={gravatarUrl}`
    - `layout.tsx` contains `?? "vendedor"` (role fallback)
    - `layout.tsx` contains `?? "Usuario"` (name fallback)
    - `apps/web/src/components/app-sidebar.tsx` contains `useSidebar` import
    - `app-sidebar.tsx` contains `setOpenMobile(false)` inside useEffect
    - `app-sidebar.tsx` contains `useEffect` import from "react"
    - `app-sidebar.tsx` contains `className="min-h-11"` (touch targets -- should appear in both VENDEDOR and ADMIN item loops)
    - `app-sidebar.tsx` contains `pathname === "/leads"` (exact match for /leads active state)
    - `app-sidebar.tsx` contains `<SidebarUserMenu` inside SidebarFooter
    - `app-sidebar.tsx` contains `import SidebarUserMenu from "@/components/sidebar-user-menu"`
    - `app-sidebar.tsx` does NOT contain `collapsible="icon"` or `collapsible="none"` (per D-01)
    - `app-sidebar.tsx` contains `collapsible="offcanvas"` (per D-02)
    - `bun run check-types` exits 0
    - `bun run check` exits 0 (Biome formatting/linting)
  </acceptance_criteria>
  <done>Layout passa userName, userEmail, userRole e gravatarUrl como server props ao AppSidebar. AppSidebar tem useEffect que chama setOpenMobile(false) em cada mudanca de pathname. Todos os SidebarMenuButton dos nav items tem className="min-h-11" (44px touch targets). Active state usa exact match para /leads e startsWith para os demais. SidebarFooter renderiza SidebarUserMenu com dados do usuario (inclui ModeToggle internamente). Type check e linting passam.</done>
</task>

</tasks>

<verification>
1. `bun run check-types` — zero type errors across all packages
2. `bun run check` — Biome linting/formatting passes
3. `bun run build` — Next.js build completes successfully
4. Grep verification:
   - `grep -r "SidebarProvider" apps/web/src --include="*.tsx" | wc -l` returns 1 (single provider)
   - `grep -r "setOpenMobile(false)" apps/web/src --include="*.tsx"` returns match in app-sidebar.tsx
   - `grep -r "min-h-11" apps/web/src --include="*.tsx"` returns matches for both VENDEDOR and ADMIN items
   - `grep -r "getGravatarUrl" apps/web/src` returns matches in gravatar.ts and layout.tsx
   - `grep -r "useTheme" apps/web/src/components/sidebar-user-menu.tsx` returns match (ModeToggle)
   - `grep -r "Sun\|Moon" apps/web/src/components/sidebar-user-menu.tsx` returns matches (ModeToggle icons)
</verification>

<success_criteria>
- gravatar.ts exporta getGravatarUrl com SHA-256 hash e d=404
- sidebar-user-menu.tsx renderiza avatar, nome, role, ModeToggle (Sun/Moon), e logout inline (sem DropdownMenu)
- ModeToggle no SidebarFooter alterna entre light e dark theme via useTheme() de next-themes (LAYOUT-08 completo)
- layout.tsx extrai user data do Supabase auth e passa como server props
- app-sidebar.tsx fecha drawer mobile automaticamente via useEffect + pathname
- Todos os nav items tem touch target minimo de 44px (min-h-11)
- Active state funciona corretamente para /leads (exact match) e rotas aninhadas (startsWith)
- SidebarFooter exibe SidebarUserMenu com avatar + nome + role + ModeToggle + logout
- Zero type errors, zero linting errors, build bem-sucedido
</success_criteria>

<output>
After completion, create `.planning/phases/09-sidebar-content-mobile-ux/09-01-SUMMARY.md`
</output>
