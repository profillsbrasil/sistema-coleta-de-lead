---
phase: 11-dashboard-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(app)/dashboard/page.tsx
  - apps/web/src/app/(app)/dashboard/personal-dashboard.tsx
autonomous: true
requirements:
  - RESP-04
  - RESP-05

must_haves:
  truths:
    - "Dashboard ocupa toda a largura do SidebarInset — sem max-w-[480px]"
    - "Stat cards exibem 1 coluna em 320px, 2 colunas em 640px, 4 colunas em 1024px"
    - "Charts redimensionam corretamente apos sidebar abrir/fechar — sem overflow"
    - "ChartContainer usa w-full sem width hardcoded"
    - "chartConfig usa theme: { light, dark } em vez de color com oklch inline"
    - "chartData referencias fill via var(--color-quente) etc. em vez de oklch inline"
  artifacts:
    - path: "apps/web/src/app/(app)/dashboard/page.tsx"
      provides: "Wrapper sem max-w-[480px]"
      contains: "w-full"
    - path: "apps/web/src/app/(app)/dashboard/personal-dashboard.tsx"
      provides: "Grid responsivo + ChartConfig theme-aware + chart key reset"
      contains: "grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4"
  key_links:
    - from: "apps/web/src/app/(app)/dashboard/personal-dashboard.tsx"
      to: "packages/ui/src/components/sidebar.tsx"
      via: "useSidebar() para key reset do ChartContainer"
      pattern: "useSidebar"
---

<objective>
Tornar o dashboard totalmente responsivo: remover a restricao `max-w-[480px]` do `dashboard/page.tsx`, converter os stat cards de `personal-dashboard.tsx` para grid responsivo (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`), corrigir o `chartConfig` para usar `theme: { light, dark }` (dark mode correto), e garantir que os charts redimensionam apos sidebar toggle via `key` reset ligado ao estado `useSidebar().open`.

Purpose: O dashboard atualmente e limitado a 480px, os charts nao usam cores tematicas, e os stat cards tem layout fixo de 2 e 3 colunas que nao e responsivo em mobile.

Output: `dashboard/page.tsx` sem max-width + `personal-dashboard.tsx` com grid responsivo e ChartConfig theme-aware.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-dashboard-visual-polish/11-CONTEXT.md
@.planning/phases/11-dashboard-visual-polish/11-RESEARCH.md
@.planning/phases/11-dashboard-visual-polish/11-UI-SPEC.md

<interfaces>
<!-- Interfaces e patterns criticos para o executor -->

De packages/ui/src/components/sidebar.tsx (useSidebar hook):
```tsx
// useSidebar() retorna (entre outros):
interface SidebarContext {
  open: boolean;        // estado desktop (true = expandida)
  openMobile: boolean;  // estado mobile drawer
  // ... outros campos
}
// Importar de:
import { useSidebar } from "@dashboard-leads-profills/ui/components/sidebar";
```

De packages/ui/src/components/chart.tsx (ChartConfig type):
```tsx
type ChartConfig = {
  [k in string]: {
    label?: React.ReactNode;
    icon?: React.ComponentType;
    color?: string;         // cor simples (NAO usar — nao e theme-aware)
    theme?: {               // USAR ESTE para dark mode correto
      light: string;
      dark: string;
    };
  };
};
```

Estado atual de personal-dashboard.tsx (linhas 21-25, 58-62, 65-87):
```tsx
// ATUAL (incorreto para dark mode):
const chartConfig: ChartConfig = {
  quente: { label: "Quente", color: "oklch(0.45 0.18 17)" },    // hardcoded
  morno: { label: "Morno", color: "oklch(0.5 0.13 85)" },       // hardcoded
  frio: { label: "Frio", color: "oklch(0.45 0.15 240)" },       // hardcoded
};

// ATUAL (fill hardcoded — bypassa CSS variables):
const chartData = [
  { tag: "Quente", count: stats.quente, fill: "oklch(0.45 0.18 17)" },
  { tag: "Morno", count: stats.morno, fill: "oklch(0.5 0.13 85)" },
  { tag: "Frio", count: stats.frio, fill: "oklch(0.45 0.15 240)" },
];

// ATUAL (layout fixo nao responsivo):
<div className="space-y-4">
  <div className="grid grid-cols-2 gap-2">        // 2 cards
  <div className="grid grid-cols-3 gap-2">        // 3 cards
  <StatCard ... />                                 // 1 card sozinho
```

Estado atual de dashboard/page.tsx (linha 17):
```tsx
<div className="mx-auto w-full max-w-[480px] px-4 pt-8">  // max-w a remover
```

Padrao de referencia de admin/stats/stats-panel.tsx (linhas 30-38, 141-149):
```tsx
// Grid responsivo correto (padrao a replicar):
<div className="mb-6 grid grid-cols-2 gap-4 lg:grid-cols-4">

// Skeleton correspondente:
function StatCardsSkeleton() {
  return (
    <div className="mb-6 grid grid-cols-2 gap-4 lg:grid-cols-4">
      <Skeleton className="h-20" />
      <Skeleton className="h-20" />
      <Skeleton className="h-20" />
      <Skeleton className="h-20" />
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remover max-w-[480px] e corrigir padding em dashboard/page.tsx</name>
  <files>apps/web/src/app/(app)/dashboard/page.tsx</files>
  <read_first>
    - apps/web/src/app/(app)/dashboard/page.tsx (arquivo completo — 22 linhas)
  </read_first>
  <action>
Modificar `apps/web/src/app/(app)/dashboard/page.tsx`:

Linha 17 — substituir o wrapper div:
```tsx
// ANTES:
<div className="mx-auto w-full max-w-[480px] px-4 pt-8">

// DEPOIS:
<div className="w-full">
```

O dashboard passa a ocupar toda a largura disponivel dentro do `SidebarInset`. O padding de pagina ja e aplicado pelo `(app)/layout.tsx` via `<div className="flex-1 p-4 md:p-6">`.

Apos a edicao, rodar `bun run check` para garantir Biome compliance.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && grep -n "max-w" apps/web/src/app/\(app\)/dashboard/page.tsx; bun run check-types 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/(app)/dashboard/page.tsx` NAO contem `max-w-[480px]`
    - `apps/web/src/app/(app)/dashboard/page.tsx` NAO contem `mx-auto` junto com o container principal
    - `apps/web/src/app/(app)/dashboard/page.tsx` contem `className="w-full"` no wrapper div
    - `bun run check-types` retorna exit code 0
  </acceptance_criteria>
  <done>Dashboard sem restricao de largura — ocupa todo o SidebarInset como todas as outras paginas.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Grid responsivo nos stat cards + ChartConfig theme-aware + chart key reset em personal-dashboard.tsx</name>
  <files>apps/web/src/app/(app)/dashboard/personal-dashboard.tsx</files>
  <read_first>
    - apps/web/src/app/(app)/dashboard/personal-dashboard.tsx (arquivo completo — 108 linhas)
    - apps/web/src/app/(app)/admin/stats/stats-panel.tsx (ver padrao de grid responsivo e skeleton — linhas 29-38, 141-149)
    - packages/ui/src/components/chart.tsx (verificar ChartConfig type e como theme funciona)
  </read_first>
  <action>
Modificar `apps/web/src/app/(app)/dashboard/personal-dashboard.tsx` em tres partes:

**Parte 1 — Adicionar import de useSidebar:**
```tsx
// Adicionar no bloco de imports (apos os imports existentes de chart):
import { useSidebar } from "@dashboard-leads-profills/ui/components/sidebar";
```

**Parte 2 — Corrigir chartConfig para theme-aware (dark mode):**
```tsx
// ANTES:
const chartConfig: ChartConfig = {
  quente: { label: "Quente", color: "oklch(0.45 0.18 17)" },
  morno: { label: "Morno", color: "oklch(0.5 0.13 85)" },
  frio: { label: "Frio", color: "oklch(0.45 0.15 240)" },
};

// DEPOIS:
const chartConfig: ChartConfig = {
  quente: {
    label: "Quente",
    theme: { light: "oklch(0.45 0.18 17)", dark: "oklch(0.85 0.12 17)" },
  },
  morno: {
    label: "Morno",
    theme: { light: "oklch(0.5 0.13 85)", dark: "oklch(0.85 0.1 85)" },
  },
  frio: {
    label: "Frio",
    theme: { light: "oklch(0.45 0.15 240)", dark: "oklch(0.85 0.1 240)" },
  },
};
```

**Parte 3 — Corrigir chartData para referenciar CSS variables:**
```tsx
// ANTES:
const chartData = [
  { tag: "Quente", count: stats.quente, fill: "oklch(0.45 0.18 17)" },
  { tag: "Morno", count: stats.morno, fill: "oklch(0.5 0.13 85)" },
  { tag: "Frio", count: stats.frio, fill: "oklch(0.45 0.15 240)" },
];

// DEPOIS:
const chartData = [
  { tag: "Quente", count: stats.quente, fill: "var(--color-quente)" },
  { tag: "Morno", count: stats.morno, fill: "var(--color-morno)" },
  { tag: "Frio", count: stats.frio, fill: "var(--color-frio)" },
];
```

**Parte 4 — Adicionar useSidebar no corpo do componente e key reset no ChartContainer:**
```tsx
// No corpo de PersonalDashboard, antes do return, apos a linha "const stats = overrideStats ?? localStats;":
const { open } = useSidebar();
```

E no ChartContainer (ja existente no return), adicionar key:
```tsx
// ANTES:
<ChartContainer className="h-[120px] w-full" config={chartConfig}>

// DEPOIS:
<ChartContainer
  className="h-[120px] w-full"
  config={chartConfig}
  key={`leads-chart-${String(open)}`}
>
```

**Parte 5 — Substituir layout de stat cards por grids responsivos:**
```tsx
// ANTES (no return do componente, quando stats existe):
<div className="space-y-4">
  <div className="grid grid-cols-2 gap-2">
    <StatCard label="Total de Leads" value={stats.total} />
    <StatCard label="Leads Hoje" value={stats.hoje} />
  </div>
  <div className="grid grid-cols-3 gap-2">
    <StatCard className="[&_span:first-child]:..." label="Quentes" value={stats.quente} />
    <StatCard className="[&_span:first-child]:..." label="Mornos" value={stats.morno} />
    <StatCard className="[&_span:first-child]:..." label="Frios" value={stats.frio} />
  </div>
  <StatCard label="Seu Score" value={`${stats.score} pts`} />
  <Card> ... </Card>
</div>

// DEPOIS:
<div className="flex flex-col gap-4">
  <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
    <StatCard label="Total de Leads" value={stats.total} />
    <StatCard label="Leads Hoje" value={stats.hoje} />
    <StatCard
      className="[&_span:first-child]:text-[oklch(0.45_0.18_17)] dark:[&_span:first-child]:text-[oklch(0.85_0.12_17)]"
      label="Quentes"
      value={stats.quente}
    />
    <StatCard
      className="[&_span:first-child]:text-[oklch(0.5_0.13_85)] dark:[&_span:first-child]:text-[oklch(0.85_0.1_85)]"
      label="Mornos"
      value={stats.morno}
    />
  </div>
  <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
    <StatCard
      className="[&_span:first-child]:text-[oklch(0.45_0.15_240)] dark:[&_span:first-child]:text-[oklch(0.85_0.1_240)]"
      label="Frios"
      value={stats.frio}
    />
    <StatCard label="Seu Score" value={`${stats.score} pts`} />
  </div>
  <Card> ... </Card>
</div>
```

**Parte 6 — Atualizar o skeleton de loading para refletir os novos grids:**
```tsx
// ANTES (estado de loading):
<div aria-busy="true" className="space-y-4">
  <div className="grid grid-cols-2 gap-2">
    <Skeleton className="h-20" />
    <Skeleton className="h-20" />
  </div>
  <div className="grid grid-cols-3 gap-2">
    <Skeleton className="h-20" />
    <Skeleton className="h-20" />
    <Skeleton className="h-20" />
  </div>
  <Skeleton className="h-20" />
  <Skeleton className="h-[160px]" />
</div>

// DEPOIS:
<div aria-busy="true" className="flex flex-col gap-4">
  <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
    <Skeleton className="h-20" />
    <Skeleton className="h-20" />
    <Skeleton className="h-20" />
    <Skeleton className="h-20" />
  </div>
  <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
    <Skeleton className="h-20" />
    <Skeleton className="h-20" />
  </div>
  <Skeleton className="h-[160px]" />
</div>
```

Apos todas as edicoes, rodar `bun run check` para corrigir quaisquer issues do Biome.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -5 && grep -n "grid-cols-1.*sm:grid-cols-2.*lg:grid-cols-4" apps/web/src/app/\(app\)/dashboard/personal-dashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `personal-dashboard.tsx` contem `grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4`
    - `personal-dashboard.tsx` NAO contem `space-y-4` (substituido por `flex flex-col gap-4`)
    - `personal-dashboard.tsx` NAO contem `grid-cols-2 gap-2` (substituido pelos novos grids)
    - `personal-dashboard.tsx` NAO contem `grid-cols-3 gap-2` (substituido pelos novos grids)
    - `personal-dashboard.tsx` contem `theme: { light:` dentro de `chartConfig` (confirma migracao de `color:` para `theme:`)
    - `personal-dashboard.tsx` contem `fill: "var(--color-quente)"` (confirma migracao de oklch inline)
    - `personal-dashboard.tsx` contem `useSidebar` importado e usado com destructuring `{ open }`
    - `personal-dashboard.tsx` contem `key={\`leads-chart-${String(open)}\`}` no ChartContainer
    - `bun run check-types` retorna exit code 0
    - `bun run check` retorna sem erros de Biome
  </acceptance_criteria>
  <done>Dashboard totalmente responsivo: stat cards em grid 1→2→4 colunas, charts com dark mode correto via ChartConfig.theme, e key reset garantindo resize apos sidebar toggle.</done>
</task>

</tasks>

<verification>
Verificacao automatizada: `bun run check-types` + `bun run check` sem erros.

Verificacao manual (browser):
1. Abrir http://localhost:3001/dashboard em 320px — stat cards em coluna unica sem overflow horizontal
2. Redimensionar para 640px — stat cards em 2 colunas
3. Redimensionar para 1024px — stat cards em 4 colunas (row 1) + 2 colunas (row 2)
4. Abrir/fechar sidebar em desktop — chart "Leads por Tag" redimensiona sem overflow
5. Alternar para dark mode — chart bars mudam de cor (nao ficam identicos ao light mode)
6. Confirmar sem `max-w-[480px]` — dashboard ocupa a largura do SidebarInset
</verification>

<success_criteria>
1. `dashboard/page.tsx` sem `max-w-[480px]`, com `className="w-full"`
2. `personal-dashboard.tsx` com `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` nos stat cards
3. `chartConfig` com `theme: { light, dark }` para quente/morno/frio
4. `chartData` com `fill: "var(--color-quente)"` etc.
5. `ChartContainer` com `key` vinculado ao `useSidebar().open`
6. `bun run check-types` e `bun run check` com exit code 0
</success_criteria>

<output>
Apos a conclusao, criar `.planning/phases/11-dashboard-visual-polish/11-dashboard-visual-polish-02-SUMMARY.md`
</output>
