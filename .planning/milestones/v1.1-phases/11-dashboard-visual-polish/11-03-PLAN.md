---
phase: 11-dashboard-visual-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified:
  - apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx
  - apps/web/src/app/(app)/admin/stats/stats-panel.tsx
  - apps/web/src/app/(app)/leads/lead-list.tsx
  - "apps/web/src/app/(app)/leads/[id]/lead-detail.tsx"
  - apps/web/src/app/(app)/admin/stats/stats-charts.tsx
autonomous: true
requirements:
  - POLISH-03
  - POLISH-05
  - RESP-04
  - RESP-05

must_haves:
  truths:
    - "Leaderboard exibe rank + nome + score em cards verticais sem overflow horizontal em 320px"
    - "Nenhum arquivo nas 7 rotas autenticadas usa space-y-* (substituido por flex flex-col gap-*)"
    - "stats-charts.tsx usa var(--color-*) no barData.fill em vez de oklch inline"
    - "lead-list.tsx e lead-detail.tsx sem max-w-[480px] fixo desnecessario"
    - "Todas as paginas usam padding consistente via layout.tsx (sem padding duplicado nas paginas)"
  artifacts:
    - path: "apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx"
      provides: "Leaderboard com flex flex-col gap-2 em vez de space-y-2"
      contains: "flex flex-col gap-2"
    - path: "apps/web/src/app/(app)/admin/stats/stats-panel.tsx"
      provides: "stats-panel com flex flex-col gap-2 em vez de space-y-2"
      contains: "flex flex-col gap-2"
    - path: "apps/web/src/app/(app)/admin/stats/stats-charts.tsx"
      provides: "barData com fill via var(--color-*) em vez de oklch inline"
      contains: "var(--color-quente)"
    - path: "apps/web/src/app/(app)/leads/lead-list.tsx"
      provides: "lead-list sem max-w-[480px] + padding consistente com layout"
      contains: "flex flex-col gap-6"
    - path: "apps/web/src/app/(app)/leads/[id]/lead-detail.tsx"
      provides: "lead-detail sem max-w-[480px] desnecessario no skeleton"
      contains: "flex flex-col gap-4"
  key_links:
    - from: "apps/web/src/app/(app)/admin/stats/stats-charts.tsx"
      to: "packages/ui/src/components/chart.tsx"
      via: "tagChartConfig com chaves quente/morno/frio para injetar CSS vars"
      pattern: "var(--color-quente)"
---

<objective>
Executar o pass final de Impeccable polish em todas as paginas autenticadas restantes: (1) validar leaderboard a 320px e converter space-y-* para flex/gap, (2) corrigir dark mode em stats-charts.tsx (barData fills hardcoded), (3) remover padding/max-width desnecessarios em leads e lead-detail que estao duplicando o padding do layout.

Purpose: Este e o plano de finalizacao — garante que todos os requirements POLISH-03 (leaderboard 320px), POLISH-05 (impeccable polish, consistencia visual) e a continuidade de RESP-04/RESP-05 (stats-charts dark mode) estao satisfeitos. Plans 11-01 e 11-02 devem estar completos antes deste.

Output: 5 arquivos modificados — leaderboard-tab, stats-panel, stats-charts, lead-list, lead-detail.
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-dashboard-visual-polish/11-CONTEXT.md
@.planning/phases/11-dashboard-visual-polish/11-RESEARCH.md
@.planning/phases/11-dashboard-visual-polish/11-UI-SPEC.md

<interfaces>
<!-- Tipos e contratos relevantes para o executor -->

De apps/web/src/components/leaderboard-entry.tsx (sem mudancas neste plano):
```tsx
// LeaderboardEntry ja tem layout correto para 320px:
// CardContent com "flex items-start gap-3"
// rank: min-w-6 text-xs, nome: flex-1 font-semibold text-sm, score: font-semibold text-sm
// Nenhuma mudanca necessaria aqui.
```

ChartConfig theme-aware (padrao do Plan 11-02 a replicar em stats-charts.tsx):
```tsx
// Em personal-dashboard.tsx (ja corrigido no Plan 11-02):
const chartConfig: ChartConfig = {
  quente: {
    label: "Quente",
    theme: { light: "oklch(0.45 0.18 17)", dark: "oklch(0.85 0.12 17)" },
  },
  morno: {
    label: "Morno",
    theme: { light: "oklch(0.5 0.13 85)", dark: "oklch(0.85 0.1 85)" },
  },
  frio: {
    label: "Frio",
    theme: { light: "oklch(0.45 0.15 240)", dark: "oklch(0.85 0.1 240)" },
  },
};
// chartData usa fill: "var(--color-quente)" etc.
```

Estado atual de stats-charts.tsx (problema a corrigir — linhas 24-26 e 51-55):
```tsx
// tagChartConfig atual (nao tem chaves com cores — incompleto):
const tagChartConfig: ChartConfig = {
  count: { label: "Leads" },   // sem tema de cor definido para quente/morno/frio
};

// barData atual (fill hardcoded — bypassa CSS vars):
const barData = [
  { tag: "Quente", count: tagData.quente, fill: "oklch(0.45 0.18 17)" },
  { tag: "Morno", count: tagData.morno, fill: "oklch(0.5 0.13 85)" },
  { tag: "Frio", count: tagData.frio, fill: "oklch(0.45 0.15 240)" },
];
```

Estado atual de lead-list.tsx (problema de padding duplicado — linhas 67-68):
```tsx
// Wrapper com padding proprio (px-4 py-8) E max-w-[480px]:
<div className="flex flex-col px-4 py-8">
  <div className="mx-auto w-full max-w-[480px]">
// O padding p-4 md:p-6 ja e aplicado pelo (app)/layout.tsx — px-4 aqui duplica
```

Estado atual de lead-detail.tsx (skeleton com max-w desnecessario — linha 58-60):
```tsx
<div className="flex flex-col px-4 py-8">
  <div className="mx-auto w-full max-w-[480px]">
    // skeleton items
  </div>
</div>
// E depois no return principal (linha 70+): LeadForm sem max-w constraint
// O max-w do skeleton cria inconsistencia: skeleton e estreito, form e full-width
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Leaderboard polish + stats-panel space-y fix + stats-charts dark mode</name>
  <files>
    apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx,
    apps/web/src/app/(app)/admin/stats/stats-panel.tsx,
    apps/web/src/app/(app)/admin/stats/stats-charts.tsx
  </files>
  <read_first>
    - apps/web/src/app/(app)/dashboard/leaderboard-tab.tsx (arquivo completo)
    - apps/web/src/app/(app)/admin/stats/stats-panel.tsx (arquivo completo — foco nas linhas 42-49 e 92-103)
    - apps/web/src/app/(app)/admin/stats/stats-charts.tsx (arquivo completo)
  </read_first>
  <action>
**Arquivo 1: leaderboard-tab.tsx — Substituir space-y-* por flex/gap**

Tres ocorrencias de `space-y-*` a substituir:

1. Skeleton loading state (linha 72):
   ```tsx
   // ANTES:
   <div aria-busy="true" className="space-y-2">
   // DEPOIS:
   <div aria-busy="true" className="flex flex-col gap-2">
   ```

2. Container raiz do leaderboard quando ha entradas (linha 92):
   ```tsx
   // ANTES:
   <div className="space-y-2">
   // DEPOIS:
   <div className="flex flex-col gap-2">
   ```

3. Elemento `<ol>` da lista (linha 94):
   ```tsx
   // ANTES:
   <ol className="space-y-2">
   // DEPOIS:
   <ol className="flex flex-col gap-2">
   ```

Nenhuma outra alteracao em leaderboard-tab.tsx. Layout de LeaderboardEntry permanece intocado (ja correto para 320px com flex items-start gap-3, min-w-6, flex-1).



**Arquivo 2: stats-panel.tsx — Substituir space-y-* por flex/gap**

Uma ocorrencia de `space-y-*` no `RankingTableSkeleton` (linha 44):
```tsx
// ANTES:
<div className="space-y-2">
  {SKELETON_ROWS.map((k) => (...))}
</div>
// DEPOIS:
<div className="flex flex-col gap-2">
  {SKELETON_ROWS.map((k) => (...))}
</div>
```

Nenhuma outra alteracao em stats-panel.tsx.



**Arquivo 3: stats-charts.tsx — Corrigir barData fills para dark mode**

Tres mudancas:

1. Substituir `tagChartConfig` para incluir chaves com tema por cor (quente/morno/frio):
   ```tsx
   // ANTES:
   const tagChartConfig: ChartConfig = {
     count: { label: "Leads" },
   };

   // DEPOIS:
   const tagChartConfig: ChartConfig = {
     count: { label: "Leads" },
     quente: {
       label: "Quente",
       theme: { light: "oklch(0.45 0.18 17)", dark: "oklch(0.85 0.12 17)" },
     },
     morno: {
       label: "Morno",
       theme: { light: "oklch(0.5 0.13 85)", dark: "oklch(0.85 0.1 85)" },
     },
     frio: {
       label: "Frio",
       theme: { light: "oklch(0.45 0.15 240)", dark: "oklch(0.85 0.1 240)" },
     },
   };
   ```

2. Substituir `barData` para usar `var(--color-*)` em vez de oklch inline (linhas 51-55):
   ```tsx
   // ANTES:
   const barData = [
     { tag: "Quente", count: tagData.quente, fill: "oklch(0.45 0.18 17)" },
     { tag: "Morno", count: tagData.morno, fill: "oklch(0.5 0.13 85)" },
     { tag: "Frio", count: tagData.frio, fill: "oklch(0.45 0.15 240)" },
   ];

   // DEPOIS:
   const barData = [
     { tag: "Quente", count: tagData.quente, fill: "var(--color-quente)" },
     { tag: "Morno", count: tagData.morno, fill: "var(--color-morno)" },
     { tag: "Frio", count: tagData.frio, fill: "var(--color-frio)" },
   ];
   ```

3. Verificar que o `ChartContainer` para barras usa `config={tagChartConfig}` (linha 69) — se sim, nenhuma mudanca adicional necessaria.

Apos todas as edicoes, rodar `bun run check` para corrigir quaisquer issues do Biome.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -5 && grep -rn "space-y-" apps/web/src/app/\(app\)/dashboard/leaderboard-tab.tsx apps/web/src/app/\(app\)/admin/stats/stats-panel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `leaderboard-tab.tsx` NAO contem `space-y-` (grep retorna vazio para este arquivo)
    - `leaderboard-tab.tsx` contem `flex flex-col gap-2` em pelo menos 2 ocorrencias
    - `stats-panel.tsx` NAO contem `space-y-2` no `RankingTableSkeleton`
    - `stats-charts.tsx` contem `var(--color-quente)` no barData
    - `stats-charts.tsx` contem `var(--color-morno)` no barData
    - `stats-charts.tsx` contem `var(--color-frio)` no barData
    - `stats-charts.tsx` contem a string `theme: {` dentro de `tagChartConfig`
    - `stats-charts.tsx` NAO contem `fill: "oklch(` no barData
    - `bun run check-types` retorna exit code 0
  </acceptance_criteria>
  <done>Leaderboard sem space-y-*, stats-panel skeleton sem space-y-*, e stats-charts com dark mode correto via CSS variables.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remover padding duplicado e max-w desnecessario em lead-list e lead-detail</name>
  <files>
    apps/web/src/app/(app)/leads/lead-list.tsx,
    apps/web/src/app/(app)/leads/[id]/lead-detail.tsx
  </files>
  <read_first>
    - apps/web/src/app/(app)/leads/lead-list.tsx (arquivo completo — foco nas linhas 66-79)
    - apps/web/src/app/(app)/leads/[id]/lead-detail.tsx (arquivo completo — foco nas linhas 56-68 e 70-91)
    - apps/web/src/app/(app)/layout.tsx (confirmar que p-4 md:p-6 e aplicado no div filho de SidebarInset)
  </read_first>
  <action>
**Arquivo 1: lead-list.tsx — Remover padding duplicado e max-w-[480px]**

O `(app)/layout.tsx` ja aplica `<div className="flex-1 p-4 md:p-6">{children}</div>` — entao `px-4` e `py-8` em `lead-list.tsx` duplicam o padding.

Linha 67-68 — substituir o wrapper duplo:
```tsx
// ANTES:
<div className="flex flex-col px-4 py-8">
  <div className="mx-auto w-full max-w-[480px]">
    {/* conteudo interior */}
  </div>
</div>

// DEPOIS: remover os dois wrappers, substituir por um unico:
<div className="flex flex-col gap-6">
  {/* conteudo interior direto aqui */}
</div>
```

Ou seja: o `<div className="flex items-center justify-between">` com h1 e o contador, o TagFilter, e os estados de carregamento/lista tornam-se filhos diretos do `<div className="flex flex-col gap-6">`.

Remover tambem:
- `mt-4` do `<div className="mt-4">` do TagFilter (o gap-6 cuida do espacamento)
- `mt-6` do `<div aria-busy="true" className="mt-6 flex flex-col gap-4">` nos skeletons

Estado esperado apos edicao (resumido):
```tsx
return (
  <div className="flex flex-col gap-6">
    <div className="flex items-center justify-between">
      <h1 className="font-semibold text-xl">Meus Leads</h1>
      {leads === undefined ? null : (
        <span aria-live="polite" className="text-muted-foreground text-sm">
          {leads.length} leads
        </span>
      )}
    </div>

    <TagFilter onChange={setActiveTag} value={activeTag} />

    {leads === undefined ? (
      <div aria-busy="true" className="flex flex-col gap-4">
        {/* skeletons */}
      </div>
    ) : leads.length === 0 ? (
      /* estado vazio inalterado */
    ) : (
      <div aria-label="Lista de leads" className="flex flex-col gap-4">
        {/* lead cards inalterados */}
      </div>
    )}

    {/* div sentinelRef e FAB permanecem como estao */}
  </div>
);
```



**Arquivo 2: lead-detail.tsx — Remover max-w-[480px] do skeleton e padding duplicado**

O skeleton de loading (linha 58-67) tem `px-4 py-8` e `max-w-[480px]` — inconsistente com o `LeadForm` que renderiza full-width.

Substituir o wrapper do skeleton:
```tsx
// ANTES (skeleton state, linhas 58-67):
<div className="flex flex-col px-4 py-8">
  <div className="mx-auto w-full max-w-[480px]">
    <Skeleton className="mb-6 h-10 w-48" />
    <Skeleton className="mb-4 h-12 w-full" />
    <Skeleton className="mb-4 h-12 w-full" />
    <Skeleton className="mb-4 h-12 w-full" />
    <Skeleton className="h-12 w-full" />
  </div>
</div>

// DEPOIS:
<div className="flex flex-col gap-4">
  <Skeleton className="h-10 w-48" />
  <Skeleton className="h-12 w-full" />
  <Skeleton className="h-12 w-full" />
  <Skeleton className="h-12 w-full" />
  <Skeleton className="h-12 w-full" />
</div>
```

O `LeadForm` no return principal (linha 72) ja e full-width — nenhuma mudanca necessaria ali.

Apos todas as edicoes, rodar `bun run check` para garantir Biome compliance e `bun run test` para confirmar suite verde.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -5 && bun run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `lead-list.tsx` NAO contem `max-w-[480px]` (grep retorna vazio)
    - `lead-list.tsx` NAO contem `px-4 py-8` no wrapper principal
    - `lead-list.tsx` contem `className="flex flex-col gap-6"` como wrapper do return
    - `lead-detail.tsx` NAO contem `max-w-[480px]` no bloco do skeleton (linhas 56-68)
    - `lead-detail.tsx` NAO contem `px-4 py-8` no wrapper do skeleton
    - `lead-detail.tsx` contem `className="flex flex-col gap-4"` como wrapper do skeleton
    - `bun run check-types` retorna exit code 0
    - `bun run check` retorna sem erros de Biome
    - `bun run test` retorna exit code 0 (suite existente permanece verde)
  </acceptance_criteria>
  <done>Leads pages com padding consistente via layout — sem max-w-[480px] nem padding duplicado. Visual uniforme com todas as outras paginas autenticadas.</done>
</task>

</tasks>

<verification>
Verificacao automatizada: `bun run check-types` + `bun run check` + `bun run test` sem erros.

Verificacao manual — audit final das 7 rotas autenticadas (arrange + adapt + polish + typeset):

**arrange (espacamento/layout):**
- Todas as paginas sem `space-y-*` — apenas `flex flex-col gap-*`
- Padding uniforme (vem do layout, nao duplicado nas paginas)

**adapt (responsividade 320px):**
- `/leads` em 320px: lista de leads sem overflow horizontal, h1 visivel
- `/leads/[id]` em 320px: formulario em coluna unica sem overflow
- `/dashboard` leaderboard tab em 320px: rank + nome + score visiveis nos cards

**polish (micro-detalhes):**
- `/admin/stats` em dark mode: chart bars de tags mudam de cor (oklch escuro vs claro)
- AppTopbar (do Plan 11-01) visivel em todas as 7 rotas

**typeset (tipografia):**
- Labels secundarios usam `text-muted-foreground` em todas as paginas
- Valores principais usam `font-semibold` sem hardcoded color
</verification>

<success_criteria>
1. `leaderboard-tab.tsx`, `stats-panel.tsx`: nenhum `space-y-*` restante nos componentes modificados
2. `stats-charts.tsx`: barData usa `var(--color-quente/morno/frio)`, tagChartConfig com theme
3. `lead-list.tsx`: sem `max-w-[480px]`, sem padding proprio, usa `flex flex-col gap-6` como wrapper
4. `lead-detail.tsx`: skeleton sem `max-w-[480px]` nem `px-4 py-8`, usa `flex flex-col gap-4`
5. `bun run check-types`, `bun run check`, `bun run test` todos com exit code 0
6. Visual audit: todas as 7 rotas respondem corretamente a 320px, 768px, 1280px
</success_criteria>

<output>
Apos a conclusao, criar `.planning/phases/11-dashboard-visual-polish/11-dashboard-visual-polish-03-SUMMARY.md`
</output>
