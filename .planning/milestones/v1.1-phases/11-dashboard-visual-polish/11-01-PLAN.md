---
phase: 11-dashboard-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/app-topbar.tsx
  - apps/web/src/app/(app)/layout.tsx
autonomous: true
requirements:
  - POLISH-01
  - POLISH-04

must_haves:
  truths:
    - "AppTopbar aparece em todas as paginas autenticadas (desktop e mobile)"
    - "SidebarTrigger e visivel apenas em mobile (< 768px) no topbar"
    - "Breadcrumb mostra localizacao correta para todas as 7 rotas autenticadas"
    - "Segmento dinamico (UUID) no breadcrumb renderiza como 'Detalhe', nao o UUID bruto"
    - "AppTopbar tem altura h-14 e border-b, identico ao header anterior"
  artifacts:
    - path: "apps/web/src/components/app-topbar.tsx"
      provides: "Componente cliente com SidebarTrigger + breadcrumb dinamico"
      exports: ["AppTopbar"]
    - path: "apps/web/src/app/(app)/layout.tsx"
      provides: "Layout com AppTopbar substituindo o header md:hidden"
      contains: "AppTopbar"
  key_links:
    - from: "apps/web/src/app/(app)/layout.tsx"
      to: "apps/web/src/components/app-topbar.tsx"
      via: "import e render de <AppTopbar />"
      pattern: "AppTopbar"
    - from: "apps/web/src/components/app-topbar.tsx"
      to: "packages/ui/src/components/breadcrumb.tsx"
      via: "import Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator"
      pattern: "from.*breadcrumb"
---

<objective>
Criar o componente `AppTopbar` com `SidebarTrigger` (mobile only) e breadcrumb dinamico derivado de `usePathname()`, e substituir o `<header className="md:hidden">` atual no `(app)/layout.tsx` pelo `<AppTopbar />` permanente.

Purpose: O topbar atual e visivel apenas em mobile e nao exibe contexto de navegacao. O AppTopbar permanente em todas as viewports serve como indicador de localizacao (breadcrumb) para desktop e gatilho de sidebar para mobile.

Output: `apps/web/src/components/app-topbar.tsx` (novo) + `apps/web/src/app/(app)/layout.tsx` (modificado).
</objective>

<execution_context>
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/sistema-coleta-de-lead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-dashboard-visual-polish/11-CONTEXT.md
@.planning/phases/11-dashboard-visual-polish/11-RESEARCH.md
@.planning/phases/11-dashboard-visual-polish/11-UI-SPEC.md

<interfaces>
<!-- Componentes ja instalados que o executor deve usar diretamente -->

De packages/ui/src/components/breadcrumb.tsx:
```tsx
// Exports disponiveis (todos named exports):
function Breadcrumb(props: React.ComponentProps<"nav">): JSX.Element
function BreadcrumbList(props: React.ComponentProps<"ol">): JSX.Element
// BreadcrumbList default classes: "wrap-break-word flex flex-wrap items-center gap-1.5 text-muted-foreground text-sm"
function BreadcrumbItem(props: React.ComponentProps<"li">): JSX.Element
// BreadcrumbItem default classes: "inline-flex items-center gap-1"
function BreadcrumbLink(props: useRender.ComponentProps<"a">): JSX.Element
// BreadcrumbLink default classes: "transition-colors hover:text-foreground"
function BreadcrumbPage(props: React.ComponentProps<"span">): JSX.Element
// BreadcrumbPage default classes: "font-normal text-foreground" (aria-current="page")
function BreadcrumbSeparator(props: React.ComponentProps<"li">): JSX.Element
// BreadcrumbSeparator renderiza ChevronRightIcon por padrao
```

De packages/ui/src/components/sidebar.tsx (SidebarTrigger):
```tsx
// Ja importado no layout.tsx atual:
import { SidebarInset, SidebarProvider, SidebarTrigger } from "@dashboard-leads-profills/ui/components/sidebar";
```

Layout.tsx atual (linhas 45-49) — o bloco a ser substituido:
```tsx
<SidebarInset>
  <header className="flex h-14 items-center gap-2 border-b px-4 md:hidden">
    <SidebarTrigger />
  </header>
  <div className="flex-1 p-4 md:p-6">{children}</div>
</SidebarInset>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar app-topbar.tsx com SidebarTrigger mobile + breadcrumb dinamico</name>
  <files>apps/web/src/components/app-topbar.tsx</files>
  <read_first>
    - apps/web/src/app/(app)/layout.tsx (ver o header atual que sera substituido — linhas 45-49)
    - packages/ui/src/components/breadcrumb.tsx (API completa do Breadcrumb suite)
  </read_first>
  <action>
Criar `apps/web/src/components/app-topbar.tsx` como componente `"use client"` (obrigatorio pois usa `usePathname()`).

Implementar exatamente:

```tsx
"use client";

import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@dashboard-leads-profills/ui/components/breadcrumb";
import { SidebarTrigger } from "@dashboard-leads-profills/ui/components/sidebar";
import { usePathname } from "next/navigation";

const ROUTE_LABELS: Record<string, string> = {
  dashboard: "Dashboard",
  leads: "Leads",
  new: "Novo Lead",
  admin: "Admin",
  users: "Usuarios",
  stats: "Estatisticas",
};

function isUuid(segment: string): boolean {
  return /^[0-9a-f-]{8,}$/.test(segment);
}

function buildSegments(pathname: string) {
  const parts = pathname.split("/").filter(Boolean);
  return parts.map((part, i) => ({
    label: ROUTE_LABELS[part] ?? (isUuid(part) ? "Detalhe" : part),
    href: `/${parts.slice(0, i + 1).join("/")}`,
    isLast: i === parts.length - 1,
  }));
}

export function AppTopbar() {
  const pathname = usePathname();
  const segments = buildSegments(pathname);

  return (
    <header className="flex h-14 shrink-0 items-center gap-2 border-b px-4">
      <SidebarTrigger className="-ml-1 md:hidden" />
      <Breadcrumb>
        <BreadcrumbList>
          {segments.map((seg) => (
            <BreadcrumbItem key={seg.href}>
              {seg.isLast ? (
                <BreadcrumbPage>{seg.label}</BreadcrumbPage>
              ) : (
                <>
                  <BreadcrumbLink href={seg.href}>{seg.label}</BreadcrumbLink>
                  <BreadcrumbSeparator />
                </>
              )}
            </BreadcrumbItem>
          ))}
        </BreadcrumbList>
      </Breadcrumb>
    </header>
  );
}
```

Notas criticas:
- `"use client"` DEVE ser a primeira linha — `usePathname()` e um React hook e nao funciona em Server Components
- `SidebarTrigger` recebe `className="-ml-1 md:hidden"` — oculto em desktop (>= 768px), visivel apenas mobile (per D-02)
- `BreadcrumbLink` aceita prop `href` diretamente (base-ui `useRender` com defaultTagName `"a"`)
- Rotas de unico segmento (ex: `/dashboard`) renderizam apenas `BreadcrumbPage` sem `BreadcrumbLink` nem separator
- Segmentos UUID (matches `/^[0-9a-f-]{8,}$/`) renderizam como "Detalhe"
- Importar de path-based: `@dashboard-leads-profills/ui/components/breadcrumb` e `@dashboard-leads-profills/ui/components/sidebar` — nunca do barrel
- Convencao: double quotes, tabs, sem `any`, sem `console.log`
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/app-topbar.tsx` existe
    - O arquivo contem `"use client"` na primeira linha
    - O arquivo contem `export function AppTopbar`
    - O arquivo contem `SidebarTrigger` com `className="-ml-1 md:hidden"`
    - O arquivo contem `ROUTE_LABELS` com chaves: `dashboard`, `leads`, `new`, `admin`, `users`, `stats`
    - O arquivo contem `isUuid` function com regex `/^[0-9a-f-]{8,}$/`
    - O arquivo contem `buildSegments` function
    - O arquivo contem `BreadcrumbPage` e `BreadcrumbLink` e `BreadcrumbSeparator`
    - `bun run check-types` retorna exit code 0 (sem erros TypeScript)
  </acceptance_criteria>
  <done>AppTopbar criado com SidebarTrigger mobile-only e breadcrumb dinamico para todas as rotas — TypeScript limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Substituir header md:hidden por AppTopbar no (app)/layout.tsx</name>
  <files>apps/web/src/app/(app)/layout.tsx</files>
  <read_first>
    - apps/web/src/app/(app)/layout.tsx (arquivo completo — ver importacoes existentes e bloco header a substituir)
    - apps/web/src/components/app-topbar.tsx (arquivo criado na Task 1)
  </read_first>
  <action>
Modificar `apps/web/src/app/(app)/layout.tsx`:

1. Remover a importacao de `SidebarTrigger` da linha 1 (nao sera mais usada diretamente no layout):
   ```tsx
   // ANTES:
   import {
     SidebarInset,
     SidebarProvider,
     SidebarTrigger,
   } from "@dashboard-leads-profills/ui/components/sidebar";

   // DEPOIS:
   import {
     SidebarInset,
     SidebarProvider,
   } from "@dashboard-leads-profills/ui/components/sidebar";
   ```

2. Adicionar importacao do AppTopbar apos os imports existentes:
   ```tsx
   import { AppTopbar } from "@/components/app-topbar";
   ```

3. Substituir o bloco `<header>` dentro de `<SidebarInset>`:
   ```tsx
   // ANTES (linhas 45-49):
   <SidebarInset>
     <header className="flex h-14 items-center gap-2 border-b px-4 md:hidden">
       <SidebarTrigger />
     </header>
     <div className="flex-1 p-4 md:p-6">{children}</div>
   </SidebarInset>

   // DEPOIS:
   <SidebarInset>
     <AppTopbar />
     <div className="flex-1 p-4 md:p-6">{children}</div>
   </SidebarInset>
   ```

O layout.tsx permanece Server Component (async + await getUser()) — apenas importa AppTopbar como leaf client component. Nao adicionar `"use client"` ao layout.

Apos as edicoes, rodar `bun run check` para corrigir quaisquer issues do Biome.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/sistema-coleta-de-lead && bun run check-types 2>&1 | tail -5 && grep -n "AppTopbar" apps/web/src/app/\(app\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/(app)/layout.tsx` contem `import { AppTopbar } from "@/components/app-topbar"`
    - `apps/web/src/app/(app)/layout.tsx` contem `<AppTopbar />` dentro do bloco `<SidebarInset>`
    - `apps/web/src/app/(app)/layout.tsx` NAO contem `<header className="flex h-14 items-center gap-2 border-b px-4 md:hidden">`
    - `apps/web/src/app/(app)/layout.tsx` NAO contem `SidebarTrigger` importado diretamente (foi movido para AppTopbar)
    - `bun run check-types` retorna exit code 0
    - `bun run check` retorna sem erros de Biome (pode ter auto-fix de formatacao)
  </acceptance_criteria>
  <done>layout.tsx usa AppTopbar permanente — topbar visivel em todas as viewports, breadcrumb ativo em todas as paginas autenticadas.</done>
</task>

</tasks>

<verification>
Verificacao automatizada: `bun run check-types` limpo + `bun run check` sem erros Biome.

Verificacao manual (browser):
1. Abrir http://localhost:3001/dashboard — topbar com "Dashboard" aparece em desktop E mobile
2. Abrir http://localhost:3001/leads — breadcrumb mostra "Leads"
3. Abrir http://localhost:3001/leads/new — breadcrumb mostra "Leads > Novo Lead"
4. Abrir http://localhost:3001/admin/leads — breadcrumb mostra "Admin > Leads"
5. Em viewport 320px — SidebarTrigger (hamburger) aparece no topbar
6. Em viewport 1024px — SidebarTrigger NAO aparece (apenas breadcrumb visivel)
</verification>

<success_criteria>
1. `apps/web/src/components/app-topbar.tsx` existe com `"use client"`, `AppTopbar`, `ROUTE_LABELS`, `isUuid`, `buildSegments`
2. `(app)/layout.tsx` renderiza `<AppTopbar />` sem o antigo `<header md:hidden>`
3. Breadcrumb dinamico correto para as 7 rotas: `/dashboard`, `/leads`, `/leads/new`, `/leads/[id]`, `/admin/leads`, `/admin/users`, `/admin/stats`
4. `bun run check-types` e `bun run check` retornam exit code 0
</success_criteria>

<output>
Apos a conclusao, criar `.planning/phases/11-dashboard-visual-polish/11-dashboard-visual-polish-01-SUMMARY.md`
</output>
